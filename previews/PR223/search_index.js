var documenterSearchIndex = {"docs":
[{"category":"section","location":"cn0_estimator.html#CN0-Estimator","page":"CN0 Estimator","text":"The CN0 (Carrier-to-Noise density ratio) estimator provides a measure of signal quality in dB-Hz. Each TrackedSignal on a TrackedSat holds its own CN0 estimator, so a multi-signal satellite produces one CN0 value per signal.\n\nnote: Every signal of a satellite is estimated, not just the driver\nThe estimator of each signal is fed its own prompt once per completed record — the estimator-driver signal has no privileged role here, and the per-signal values are not interchangeable: on GPS L1C the pilot carries ~75 % of the power against the data component's ~25 %, a 4.8 dB difference.Nothing inside Tracking reads estimate_cn0 — it is a reporting API, so this costs only the per-record estimator update, which is well under a per cent of the correlation work for that record (~40 ns against ~9 µs for a two-signal satellite over 4000 samples). estimate_cn0 itself is of the same order and runs only when you call it, so a signal whose C/N₀ you never read costs almost nothing either way.The reason to switch a signal off is therefore not speed but honesty. NoCN0Estimator is the per-signal opt-out.","title":"CN0 Estimator"},{"category":"section","location":"cn0_estimator.html#Choosing-an-estimator","page":"CN0 Estimator","text":"Four estimators ship, and each column below is a reason somebody picks a different one.\n\n NoiseRefCN0Estimator (default) NWPRCN0Estimator MomentsCN0Estimator NoCN0Estimator\nNeeds the band's samples (or a noise observation)? yes no no no\nNeeds bit sync / a coherent window? no yes no no\nNeeds to know the window length M? no yes no no\nWorks on GPS L1C-D, Galileo E1B, pre-sync secondary codes? yes no — falls back yes, at its floor n/a\nBias, thermal only <0.03 dB ≈+0.05 dB everywhere ≈27.6 dB-Hz floor on noise n/a\nHigh-C/N₀ bias floor ≈0.13 dB at 45, ≈0.40 at 50 (self-leakage) none none n/a\nHigh-C/N₀ σ keeps improving, until the row above dominates saturates at √(M/((M−1)K)) ≈0.5 dB — n/a\nDegenerate outputs none -Inf/Inf outside 1 < μ̂ < M — 5.6 % of estimates at 20 dB-Hz none always -Inf\nPhase-noise / residual-Doppler sensitivity immune (non-coherent) sensitive — the coherent sum loses power; −12 dB at 120 Hz of handoff Doppler, see Residual Doppler at acquisition handoff immune n/a\nCost per-record arithmetic + one despread per signal per chunk per-record arithmetic per-record arithmetic none\n\nThe \"degenerate\" row is a rate of unusable outputs, not a per-window discard rate: estimate_cn0 pools every buffered window and range-checks the pooled ratio once.","title":"Choosing an estimator"},{"category":"section","location":"cn0_estimator.html#Which-one-do-I-use?","page":"CN0 Estimator","text":"Sample-driven track! → NoiseRefCN0Estimator. This is the default and needs no configuration: the signal is provisioned a CorrelatorNoiseEstimator automatically and track! fills it.\nCorrelator outputs from hardware, with a noise observation → the same, plus one append_noise_observation! per signal per fold. See Noise Estimator for what a producer has to report; the short version is raw accumulations of an untracked PRN, with every division and all averaging done here.\nCorrelator outputs from hardware, without one → NWPRCN0Estimator(signal), accepting its fallback on the signals it cannot serve. This is the one place NWPR is still the right choice.\nA signal whose C/N₀ nobody reads (the passenger of a co-tracked pair) → NoCN0Estimator; see Not measuring a signal at all.\nMomentsCN0Estimator is a fallback component and a prompt-stream tool, not a recommended standalone default — see Why not the moment method for the ≈27.6 dB-Hz floor that rules it out.","title":"Which one do I use?"},{"category":"section","location":"cn0_estimator.html#The-default:-C/N-against-a-measured-noise-floor","page":"CN0 Estimator","text":"NoiseRefCN0Estimator computes, per record,\n\nĈ/N₀ = ⟨|P|²⟩ / N̂₀ − 1/T\n\nwith N̂₀ the band's measured noise density and T that record's own integration time, then averages the per-record terms. It is non-coherent: no bit sync, no window, no M, no fallback, no saturation ceiling — one set of characteristics on every signal.\n\nWhere the density comes from is a separate, pluggable thing: see Noise Estimator. On the sample-driven path it is automatic.","title":"The default: C/N₀ against a measured noise floor"},{"category":"section","location":"cn0_estimator.html#Getting-the-low-C/N-variance","page":"CN0 Estimator","text":"Below about 28 dB-Hz a non-coherent estimator is the noisier of the two at matched record length, and the reason is squaring loss: NWPR sums M prompts coherently before squaring, so at low SNR its σ is better by exactly √(M−1).\n\nThe way to buy that back is not a post-correlation window. A coherent sum of five 1 ms prompts is one 5 ms prompt — NWPR's narrowband window is a longer coherent integration reconstructed after the fact, not extra information. Taking the same integration in the correlator instead, with set_preferred_num_code_blocks_to_integrate!, makes the noise reference better than NWPR at every C/N₀ and helps the discriminators as well. Modelled over 100 ms of observation, 4000 trials × 9 seeds, relative σ in dB:\n\nC/N₀ NWPR (M = 5) NoiseRef, 1 ms records NoiseRef, 5 ms records\n20 2.96 4.79 2.76\n25 1.50 1.72 1.24\n30 0.90 0.75 0.65\n40 0.54 0.20 0.20\n50 0.50 0.06 0.06\n\nTwo honest caveats. The record length also sets the discriminator behaviour and the loop's update interval, so this is a trade, not a free win — the conventional estimator rescales the carrier bandwidth by 1/N to keep the loop stable, but a longer record still means a slower loop. And the length is capped at one block until bit/secondary sync is found, so pre-sync you are on the 1 ms column whatever you configure.","title":"Getting the low-C/N₀ variance"},{"category":"section","location":"cn0_estimator.html#The-one-bias-it-carries","page":"CN0 Estimator","text":"The reference despreads with a wrong PRN, so besides the thermal floor it also collects the tracked satellite's own power — ε_self/N₀ = C/f_chip. NWPR does not: it despreads with the correct code, where the satellite's power appears in both NBP and WBP and cancels in the ratio. On GPS L1 C/A:\n\nown C/N₀ C/N₀ reads low by\n35 dB-Hz 0.013 dB\n40 dB-Hz 0.042 dB\n45 dB-Hz 0.13 dB\n50 dB-Hz 0.40 dB\n\nA wider spreading bandwidth scales it down in proportion, so GPS L5 and Galileo E5a at 10.23 Mcps are ≈10× better — 0.013 dB even at 50 dB-Hz.\n\nIt is carried rather than corrected on purpose: the correction N̂₀ ← N̂₀ − Σᵢ Ĉᵢ/f_chip would make every satellite's C/N₀ a function of every other satellite's estimate, which is a cross-satellite feedback loop this design exists without. Below 40 dB-Hz the term sits under NWPR's own +0.05 dB bias, and that is the range where lock and loss decisions are made.","title":"The one bias it carries"},{"category":"section","location":"cn0_estimator.html#NWPRCN0Estimator:-the-coherent-window-estimator","page":"CN0 Estimator","text":"Van Dierendonck's narrowband/wideband power ratio. Per narrowband window of M records it forms the coherent power NBP = |Σ prompt|² and the incoherent power WBP = Σ |prompt|², divides the sums of both over as many windows as fit in num_prompts_for_cn0_estimation records, and turns that mean power ratio into a C/N₀. It was the default until the measured noise reference landed, and it is what to configure on a correlator-ingest path that cannot report a noise observation.","title":"NWPRCN0Estimator: the coherent-window estimator"},{"category":"section","location":"cn0_estimator.html#Why-not-the-moment-method","page":"CN0 Estimator","text":"The Moments Method (M2M4, MomentsCN0Estimator) was the default up to and including Tracking 5.1. It is a moment ratio, and at a finite window the sample moments fluctuate enough to manufacture signal power out of noise. Measured on synthetic prompts (amplitude √(C/N₀·T) in unit-variance complex noise), 100 one-millisecond records, 400 realizations:\n\ntrue C/N₀ M2M4 median p10 p90 NWPR (M = 20) median\nnoise only 27.6 22.6 31.1 −Inf (\"no signal\")\n20 dB-Hz 27.7 22.5 30.9 19.9\n25 dB-Hz 27.9 23.4 30.9 25.0\n30 dB-Hz 30.5 27.3 32.6 30.0\n35 dB-Hz 35.3 33.9 36.4 35.0\n45 dB-Hz 45.1 44.3 45.9 45.0\n\nM2M4 is accurate from ~35 dB-Hz up and blind below ~30: a true 20 dB-Hz signal and pure noise both read ~27.7. As a per-update detection statistic that makes pure noise clear a 30 dB-Hz threshold 19 % of the time, a 28 dB-Hz threshold 45 %, and a 25 dB-Hz threshold 76 % — so the usual code-lock hysteresis (lock at 34 dB-Hz, drop at 25) cannot be built on it. At M = 20 NWPR's false-alarm rate against any threshold from 20 to 32 dB-Hz is 0 %, and its spread at low C/N₀ is much tighter (p10–p90 of 15.8–22.2 at a true 20 dB-Hz). This mirrors the published comparisons (Falletti, Pini & Lo Presti, IEEE T-AES 47(1):420–437, 2011). See issue #217 for the full measurements.\n\nThose are the numbers for one 20-record window; the shipped default runs shorter windows (see below), which trades some of that margin for immunity against the loop's phase noise. End to end through track on a data-modulated GPS L1 C/A signal at the defaults, 600 ms per run, medians over 16 seeds:\n\ntrue C/N₀ NWPR (default) p10 p90 M2M4\nnoise only −Inf in 12 of 16 runs   27.9\n20 dB-Hz 18.3 6.0 22.2 27.6\n25 dB-Hz 23.9 16.3 26.3 28.3\n30 dB-Hz 29.0 27.4 30.3 30.8\n35 dB-Hz 34.1 33.3 35.0 35.3\n45 dB-Hz 44.3 43.7 45.2 45.4\n\nOn pure noise, pre-sync — where a code-lock detector has to make its call — the default reports a median of -Inf, clears a 20 dB-Hz threshold in 4 % of updates and a 25 dB-Hz threshold in 0 % (200 runs, highest value seen 22.4). M2M4 on the same runs clears 25 dB-Hz in 67 % of updates.\n\nThe trade-off is that NBP is a coherent sum, so it is sensitive to anything that decorrelates the prompts inside a window — see What the estimator returns.","title":"Why not the moment method"},{"category":"section","location":"cn0_estimator.html#The-window-follows-the-navigation-bits","page":"CN0 Estimator","text":"A narrowband window must not straddle a navigation-bit flip (a mid-window flip costs ~7 dB). Where the bit boundaries are is something the tracking loop knows and a downstream consumer of get_filtered_prompts does not, so each record's prompt is handed to the estimator together with the navigation-bit state in a CN0UpdateContext, and the window is derived from it:\n\nsignal state narrowband window\nsync found, data-bearing signal (GPS L1 C/A, L5I, …) num_narrowband_code_blocks (5), tiling the navigation bit from its start\nsync found, pilot (GPS L1C-P, L2CL, Galileo E1C, …) num_narrowband_code_blocks (no bit grid to respect)\nsync not found, data-bearing without secondary code num_presync_narrowband_code_blocks (5), unaligned\nsync not found, signal with a secondary code none — the unknown overlay flips every code block\none symbol per code block (GPS L1C-D, Galileo E1B) none — no window longer than one record is coherent\nrecord at least as long as its own window none — a one-record window has NBP == WBP\n\nWhere no window is admissible the estimator falls back to a MomentsCN0Estimator, which needs no coherence at all — and with it to that estimator's floor. Note the last row: with set_preferred_num_code_blocks_to_integrate! at a whole navigation bit, a window closes on a single record and NWPR reports its fallback for good.\n\nwarning: The moment ratio's noise floor is there on data-only signals\nGPS L1C-D and Galileo E1B carry one navigation symbol per code block (a 10 ms and a 4 ms code period against a 100 sym/s and 250 sym/s data rate), so no two consecutive records are guaranteed to share a sign and the longest coherent window is a single record — where NBP == WBP identically. Those two signals therefore keep the moment ratio for good, and with it its floor: pure noise still reads ~27.6 dB-Hz on L1C-D and E1B, and a true 20 dB-Hz signal is indistinguishable from it. The same applies to any signal before its sync is found if it carries a secondary code (GPS L5, L2C, Galileo E1C, E5a), where the unknown overlay flips the sign every code block.There is no fix inside a ~100-record window: without coherence only the prompt magnitudes carry information, and at a per-record SNR of 0.1–0.3 their distribution is barely distinguishable from the noise-only one at that sample count. (A phase-blind (E|z|)²/E|z|² estimator does better than M2M4 on false alarms — 42 % against 76 % at a 25 dB-Hz threshold on pure noise, 6 % against 19 % at 30 dB-Hz — but its median on noise is still ~27.5 dB-Hz, so it moves the floor rather than removing it. Its floor shrinks as N^{-1/2} against the moment ratio's N^{-1/4}, so it would need seconds of averaging, not tens of seconds.)This is what the default estimator exists to retire. NoiseRefCN0Estimator has no window at all, so those signals and phases are ordinary records to it — that is issue #217, and the reason the default moved. If you are on NWPR anyway (a correlator-ingest path with no noise observation), the mitigations are: take the lock decision from the pilot of the pair, which is the component the loops track anyway (L1C-D with L1C-P, E1B with E1C, both on one TrackedSat); give the data component a NoCN0Estimator; or give it an NWPRCN0Estimator(signal; fallback = NoCN0Estimator()), which reports -Inf dB-Hz exactly when no coherent window is available.\n\nThe pre-sync window matters: the bit-edge detector needs seconds to lock at 35 dB-Hz and does not lock below ~30 dB-Hz, so a strictly bit-aligned window would leave the low-C/N₀ regime — the one this estimator exists for — on the moment ratio. An unaligned window of M records inside an L-block bit straddles a flip with probability (M−1)/L, so the default five blocks of a 20-block GPS L1 C/A bit costs ~0.6 dB below 30 dB-Hz (and up to ~6 dB at a strong signal, for the fraction of a second until sync is found) while removing the moment ratio's noise floor completely.","title":"The window follows the navigation bits"},{"category":"section","location":"cn0_estimator.html#The-window-is-capped-by-the-loop's-coherence-time","page":"CN0 Estimator","text":"A bit-aligned window does not have to span the whole bit, and it should not: the num_narrowband_code_blocks windows tile the bit from its start, so none straddles a flip and none runs past the coherence time of the loop that produced the prompts. The longest flip-free window is not the best one — a longer coherent window buys only a little (the same num_records records are combined either way, so only their partition changes: at a true 25 dB-Hz, going from a 5-record to a 20-record window buys 0.8 dB of spread), while decoherence over a long window costs a bias, the one error more averaging cannot remove.\n\nThe case that shows it is a satellite that fades after bit sync was found, which is the normal way a receiver reaches low C/N₀. Locked in at 45 dB-Hz, then faded to a true 25 dB-Hz, medians over 96 runs:\n\ncoherent window reported C/N₀ p10 runs reading -Inf\n5 blocks (default) 23.6 19.5 1 / 96\none full 20-block bit 21.0 12.0 17 / 96\n\nA whole-bit coherent sum reports \"no signal\" on a satellite that is being tracked. Raise num_narrowband_code_blocks for a pilot, a narrow carrier loop, or a signal that is never weak; lower it if the loop is noisier than the default.","title":"The window is capped by the loop's coherence time"},{"category":"section","location":"cn0_estimator.html#Configuring-the-estimator","page":"CN0 Estimator","text":"num_prompts_for_cn0_estimation (default 100, ~100 ms for L1 C/A) sets how many records the estimate averages over — for the default estimator, and for NWPR's fallback as well:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(; signal = GPSL1CA());\n\njulia> sat = TrackedSat(GPSL1CA(), 1, 50.0, 1000.0Hz; num_prompts_for_cn0_estimation = 200);\n\njulia> track_state = add_satellite!(track_state, :default, sat);\n\njulia> get_prn(track_state, 1)\n1\n\nTo choose a different estimator — or to configure NWPR's windows explicitly — pass a cn0_estimator instance. It is a type parameter of TrackedSignal, so any AbstractCN0Estimator is stored as is:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> sat = TrackedSat(GPSL1CA(), 1, 50.0, 1000.0Hz; cn0_estimator = MomentsCN0Estimator(100));   # the default up to 5.1\n\njulia> Tracking.get_cn0_estimator(sat) isa MomentsCN0Estimator\ntrue\n\njulia> sat = TrackedSat(\n           GPSL1CA(),\n           1,\n           50.0,\n           1000.0Hz;\n           cn0_estimator = NWPRCN0Estimator(\n               GPSL1CA();\n               num_records = 400,                       # ~400 ms of memory\n               num_presync_narrowband_code_blocks = 0,  # M2M4 until bit sync\n           ),\n       );\n\njulia> Tracking.get_cn0_estimator(sat).num_records\n400\n\nPassing the signal to NWPRCN0Estimator is what sizes its coherent window for that signal's code period — 5 blocks for a 1 ms code, 2 for GPS L1C-P's 10 ms one. The window is a count of blocks, so leaving it at the bare constructor's 5 makes it 50 ms long on a 10 ms code.\n\nEach signal needs its own estimator instance — they buffer into a vector, so sharing one between two signals of a satellite corrupts both. The multi-signal TrackedSat constructor therefore takes a tuple, one estimator per signal:\n\nTrackedSat(\n    (GPSL1C_P(), GPSL1CA()),\n    prn,\n    code_phase,\n    carrier_doppler;\n    cn0_estimator = (NoiseRefCN0Estimator(), MomentsCN0Estimator(100)),\n)\n\nA signal is provisioned a noise source if its estimator reads one, so the mixed case above provisions exactly one: the noise-referenced signal gets its own density, the other neither reads nor pays for one. A signal nobody asks a density of is not provisioned at all and runs no despread — which is the answer for anyone who deliberately stays on NWPR.\n\nPer signal rather than per band because the floor is the post-correlation one and therefore a property of the despreading modulation, not of the RF band; see Why per signal and not per RF band. The cost of that is linear in the noise-referenced signals sharing a band — a despread each per chunk, against n_sats tracking correlations apiece.","title":"Configuring the estimator"},{"category":"section","location":"cn0_estimator.html#Not-measuring-a-signal-at-all","page":"CN0 Estimator","text":"NoCN0Estimator keeps no state, does no per-record work and reports -Inf dB-Hz. It is the way to say \"this signal's C/N₀ is not measured\" — which is worth saying in two situations:\n\n# the non-driver signal of a pair whose C/N₀ nobody reads\nTrackedSat(\n    (GPSL1C_P(), GPSL1C_D()),\n    prn,\n    code_phase,\n    carrier_doppler;\n    cn0_estimator = (NoiseRefCN0Estimator(), NoCN0Estimator()),\n)\n\n# NWPR with no moment-ratio floor underneath it: \"no estimate\" until a coherent\n# window exists, instead of the moment ratio's ~27.6 dB-Hz on noise\nNWPRCN0Estimator(GPSL1C_D(); fallback = NoCN0Estimator())\n\nThe second only matters if you are on NWPR at all — the default estimator has no fallback and no floor. Where you are, it turns the fallback's noise floor into an honest -Inf for every signal and phase that admits no coherent window (see the warning above). The per-record saving of the first is real but tiny — one estimator update is well under a per cent of the correlation work for that record — so choose it for clarity, not for speed.\n\nIt reports -Inf dB-Hz rather than NaN dB-Hz deliberately: with Unitful's Level comparison, NaN dB-Hz >= threshold is true for every threshold, so a NaN would clear every lock detector it met. -Inf compares false against any finite threshold, which is the safe answer to \"is this signal locked?\".","title":"Not measuring a signal at all"},{"category":"section","location":"cn0_estimator.html#What-the-estimator-returns","page":"CN0 Estimator","text":"Before the first record has been folded in — nothing has been measured and estimate_cn0 returns -Inf dB-Hz. This is the value seen immediately after add_satellite!, and it is -Inf rather than a finite number precisely so a lock detector cannot clear on it.\nDriving a noise-free signal through track — there is no noise to divide by, so the result runs off to a very large value; NWPR's coherent and incoherent powers agree exactly (μ̂ = M) and it reports Inf dB-Hz, as does the moment estimator. The Quick start shows this.\nDriving a noisy signal — the estimate converges to the underlying CN0 as the buffers fill. Real-world signals typically land in 30–50 dB-Hz.\nDriving noise only — the noise-referenced default reports whatever the averaged per-record terms come to, floored at -Inf dB-Hz once the mean fails to clear zero — which on pure noise is most of the time, and never a positive floor. NWPR reports -Inf dB-Hz whenever the mean ratio sits at the incoherent floor (μ̂ ≤ 1). Either way that is the median outcome and not a guarantee: code that thresholds the estimate needs no special case, code that averages it must handle the infinities.\nWhile the loops are struggling — this is where the two differ most. NWPR measures coherent C/N₀, so residual phase noise counts against it: at 1 ms records and the default windows it reads ~0.9 dB low at a true 35 dB-Hz, ~1.1 dB low at 25 dB-Hz and ~1.7 dB low at 20 dB-Hz, and raising num_narrowband_code_blocks past the loop's coherence time makes it much worse. The non-coherent default is immune — measured through track! at a true 45 dB-Hz it reads 44.9 at 1-block records and 44.7 at 20-block ones, where NWPR falls to 32.9.\n\nEvery estimator here reports C/N₀ against the effective noise density, including multi-access interference: the other satellites' cross-correlation adds incoherent power ε that both NWPR and the noise reference see, so both report C/(N₀+ε). That is what other receivers report and the more useful number for a lock detector. The one term they do not share is the reference's self-leakage — see The one bias it carries.\n\nwarning: The post-correlation filter is assumed to have unity noise gain\nN̂₀ is measured at the band, but the prompt the estimator sees is post-AbstractPostCorrFilter. DefaultPostCorrFilter selects one antenna (last) and has gain 1, which is fine — and the noise reference despreads that same antenna, so the two sides of the ratio match. A real beamformer changes the prompt's noise scale and silently invalidates the ratio; there is no noise_gain hook yet.","title":"What the estimator returns"},{"category":"section","location":"cn0_estimator.html#Residual-Doppler-at-acquisition-handoff","page":"CN0 Estimator","text":"Acquisition hands over a Doppler estimate good to a search-bin width — commonly ±100 Hz or so — and the loop takes tens of milliseconds to pull it in. That transient is where the coherent and non-coherent estimators differ most, and it is also exactly when a lock detector is being asked whether to keep the satellite.\n\nMeasured through track! at a true 40 dB-Hz, 1 ms records, the satellite seeded with a deliberate carrier-Doppler error, median of 5 seeds, read over the first 60 ms while the loop is still converging:\n\nΔf real within-record loss NWPR Moments NoiseRef\n0 Hz 0.00 dB 39.8 40.5 40.1\n30 Hz −0.01 dB 39.8 40.4 40.1\n60 Hz −0.05 dB 39.2 40.4 40.1\n120 Hz −0.21 dB 27.6 40.5 39.9\n250 Hz −0.91 dB −Inf 40.1 39.1\n\nAt 120 Hz NWPR under-reports by 12 dB; at 250 Hz it reports -Inf dB-Hz — \"no signal\" — on a satellite that is being tracked perfectly well.\n\nThe reason is the coherent sum. A residual Δf ramps the prompt's phase by 2π·Δf·T per record, so across the window the ramp is Δf·M·T cycles: 0.6 of a cycle at 120 Hz over the default 5 ms window, which spreads the five phasors over 216° and costs NBP ~5.9 dB. μ̂ collapses toward 1 and the inversion (μ̂−1)/(M−μ̂) falls off a cliff; past 1.25 cycles μ̂ ≤ 1 and the estimate is -Inf. This is the constraint M·T ≪ 1/(2·Δf) stated under NWPRCN0Estimator, which at the default window means Δf ≪ 100 Hz.\n\nThe non-coherent estimators see only the within-record loss, sinc²(Δf·T) — 0.21 dB at 120 Hz, 0.91 dB at 250 Hz. That is real signal loss the correlator genuinely suffers, so reporting it is correct rather than a defect; the measured 39.1 dB-Hz at 250 Hz is 40 minus exactly that.\n\nWhat makes the default robust here is that the noise reference is open-loop: it runs at the band's nominal IF with zero Doppler and measures noise, which is white. No amount of Doppler error on any satellite can perturb the denominator, so N̂₀ is trustworthy precisely when nothing derived from the loop is.\n\nOnce the loop converges, NWPR recovers — 39.2 dB-Hz at every offset up to 120 Hz after 300 ms. At 250 Hz it stays at -Inf, and the noise reference's 38.9 says why: the loop never pulled in from there, so the residual — and its real 0.9 dB loss — is still present. The two agree about the physics and disagree about whether to report it.\n\nIf you are on NWPR anyway (a correlator-ingest path with no noise observation), shorten the window that applies before bit sync — but it only goes so far, because a two-record window is also the noisiest one:\n\nnum_presync_narrowband_code_blocks Δf·M·T at 120 Hz reported\n5 (default) 0.60 cycles 27.6\n3 0.36 cycles 32.4\n2 0.24 cycles 34.9\n\nNWPRCN0Estimator(GPSL1CA(); num_presync_narrowband_code_blocks = 2)\n\nThe doctest below builds a noisy L1 C/A signal at a known 45 dB-Hz CN0 and drives 25 1-ms tracking cycles through it:\n\njulia> using Tracking, GNSSSignals, Random\n\njulia> using Tracking: Hz\n\njulia> using GNSSSignals: gen_code, get_code_frequency\n\njulia> function run_cn0_demo()\n           sys = GPSL1CA()\n           fs = 4e6Hz\n           num_samples = 4000  # 1 ms at 4 MHz\n           prn = 1\n           cn0_db_hz = 45.0\n           sigma = sqrt((fs/Hz) / 10^(cn0_db_hz/10) / 2)\n           code_freq = get_code_frequency(sys)\n           rng = MersenneTwister(0)\n           track_state = TrackState(; signal = GPSL1CA())\n           track_state =\n               add_satellite!(track_state; prn, code_phase = 0.0, carrier_doppler = 0.0Hz)\n           for _ = 1:25\n               clean = gen_code(num_samples, sys, prn, fs, code_freq, 0.0)\n               noise = sigma .* randn(rng, ComplexF64, num_samples)\n               track_state = track(clean .+ noise, track_state, fs)\n           end\n           estimate_cn0(track_state, 1)\n       end;\n\njulia> run_cn0_demo()  # 25 ms is pre-sync, so the unaligned five-block window is in use\n47.9 dB-Hz\n\nThe 1-σ noise amplitude is derived from the target CN0 by inverting C/N₀ = signal_power / (σ² / Fs). With unit-amplitude code samples this collapses to σ² = Fs / 10^(CN0_dB/10), split evenly across the complex sample's real and imaginary parts. The seeded MersenneTwister(0) keeps the doctest deterministic; 25 records only fill a quarter of the default 100-record window, so a few dB of spread is expected. The value is also a pre-sync one: five of those records land in a window that straddles a data-bit flip once the signal carries data, which this noise-free-code demo does not.","title":"Residual Doppler at acquisition handoff"},{"category":"section","location":"cn0_estimator.html#Custom-CN0-Estimators","page":"CN0 Estimator","text":"You can implement your own estimator by creating a subtype of AbstractCN0Estimator and implementing:\n\nTracking.update(cn0_estimator::MyCN0Estimator, prompt) — return a new estimator with the latest prompt added (immutable update).\nestimate_cn0(cn0_estimator::MyCN0Estimator, integration_time) — return the CN0 estimate as a dB-Hz quantity. integration_time is the integration time of the record the last prompt came from, i.e. get_last_fully_integrated_integration_time.\n\nIf the estimator can profit from the navigation-bit grid — bit boundaries, whether sync has been found, the decoded soft bits — implement the three-argument form instead, which is what the tracking loop calls:\n\nTracking.update(cn0_estimator::MyCN0Estimator, prompt, context::CN0UpdateContext)\n\nThe default three-argument method drops the context and calls the two-argument one, so implementing only the latter is fine.\n\nPlug it in with the cn0_estimator keyword of TrackedSignal or TrackedSat — the estimator is a type parameter of TrackedSignal, so your type is stored as is:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> struct MyCN0Estimator <: Tracking.AbstractCN0Estimator end\n\njulia> Tracking.update(estimator::MyCN0Estimator, prompt) = estimator;\n\njulia> Tracking.estimate_cn0(::MyCN0Estimator, integration_time) = 42.0 * Tracking.dBHz;\n\njulia> sat = TrackedSat(GPSL1CA(), 1, 50.0, 1000.0Hz; cn0_estimator = MyCN0Estimator());\n\njulia> estimate_cn0(sat)\n42.0 dB-Hz","title":"Custom CN0 Estimators"},{"category":"type","location":"cn0_estimator.html#Tracking.NoiseRefCN0Estimator","page":"CN0 Estimator","text":"C/N₀ against a measured noise reference: per record,\n\nĈ/N₀ = ⟨|P|²⟩ / N̂₀ − 1/T\n\nwith N̂₀ the signal's noise density from its AbstractNoiseEstimator and T that record's own integration time. The ring averages the per-record terms and estimate_cn0 converts the mean once.\n\nUnlike NWPRCN0Estimator this is non-coherent: it needs no bit sync, no window length M, no coherent window and no fallback, it is immune to residual carrier-phase error, and it has no saturation ceiling. It is therefore the one estimator that works uniformly on every signal — including GPS L1C-D and Galileo E1B (blocks_per_bit == 1) and any secondary-coded signal before sync, where NWPR admits no coherent window at all and defers permanently to a fallback with a different bias (issue #217).\n\nWhat it needs\n\nA noise density for its own signal — not for its RF band, because what this divides by is the post-correlation floor and that depends on the despreading modulation (see AbstractNoiseEstimator). On the sample-driven path it is automatic: TrackState provisions a CorrelatorNoiseEstimator for every signal whose estimator asks for one (see requires_noise_density), and track! measures before the fold reads. On a correlator-ingest path you configure the same type and fill it with append_noise_observation! per signal instead. With no source configured for the signal, update throws — a wiring mistake, and a silent substitution would hide a backend that fails to populate the reference. While a configured source's window is merely still empty, the update is skipped (the fold warns once per signal) and estimate_cn0 reports -Inf dB-Hz until it fills.\n\nThree deliberate properties\n\nNo fallback field. The three reasons NWPR needs one — warm-up, no admissible window pre-sync, and no window ever at one symbol per code block — all vanish. One record plus a density is a valid, if noisy, estimate on every signal.\nestimate_cn0's integration_time argument is ignored, because T is applied per record at update time, where it is actually known. The signature stays for interface uniformity; this is not an oversight.\nOnly the average is floored, never the per-record term. Individual terms go negative at low C/N₀ and that is exactly what makes the mean unbiased; clamping per record would reintroduce a noise floor of the kind MomentsCN0Estimator has.\n\nThe one bias it carries\n\nThe reference despreads with a wrong PRN, so besides the thermal floor and the other satellites' interference — both of which NWPR sees identically — it also collects the tracked satellite's own power, ε_self/N₀ = C/f_chip. NWPR does not: it despreads with the correct code, where the satellite's power appears in both NBP and WBP and cancels in the ratio. On GPS L1 C/A that is 0.013 dB at 35 dB-Hz, 0.042 at 40, 0.13 at 45 and 0.40 at 50 (≈10× smaller on the 10.23 Mcps signals), always reading low. It is carried rather than corrected, because the correction N̂₀ ← N̂₀ − Σᵢ Ĉᵢ/f_chip would make every satellite's C/N₀ a function of every other satellite's estimate — the cross-satellite feedback loop this design exists without. Below 40 dB-Hz it sits under NWPR's own +0.05 dB bias, and that is the range where lock and loss decisions are made.\n\nFields / configuration\n\nnum_records — how many records the estimate averages over, i.e. the memory of the estimator (100 by default, ~100 ms at GPS L1 C/A).\nbuffered_cn0, current_index, filled_length — the ring of per-record C/N₀ terms in linear Hz, written in place, plus its position and fill.\n\n\n\n\n\n","title":"Tracking.NoiseRefCN0Estimator"},{"category":"function","location":"cn0_estimator.html#Tracking.default_cn0_estimator","page":"CN0 Estimator","text":"default_cn0_estimator(\n    signal,\n    num_prompts_for_cn0_estimation\n)\n\n\nThe default CN0 estimator for signal: a NoiseRefCN0Estimator averaging over num_prompts_for_cn0_estimation records, against that signal's own measured noise density.\n\nIt reads a density, so TrackState provisions the signal a CorrelatorNoiseEstimator automatically (see requires_noise_density) and track! fills it from the samples — the sample-driven path needs no configuration at all.\n\nWhy this and not NWPR\n\nNWPRCN0Estimator is accurate where it applies, but it needs a coherent narrowband window and so does not apply uniformly. Measured through track! on a data-modulated GPS L1 C/A signal, 1200 code blocks, median over 9 seeds, with the fraction of runs reporting -Inf dB-Hz in brackets:\n\ntrue NWPR, 1-block records NoiseRef NWPR, 20-block records NoiseRef\n25 11.2 (56 %) 23.4 (0) — —\n30 29.3 ± 1.40 29.6 ± 0.62 — —\n40 39.7 ± 0.35 39.9 ± 0.19 27.1 ± 5.79 39.8 ± 0.37\n45 44.7 ± 0.30 44.9 ± 0.13 32.9 ± 2.00 44.7 ± 0.30\n\nThree things in that table decided the default. At 25 dB-Hz NWPR's window lands outside 1 < μ̂ < M in over half the runs and the surviving estimates read 14 dB low, which is exactly the regime a lock detector has to work in. At long coherent records NWPR collapses — a record as long as its own window has NBP ≡ WBP and no window exists at all, so it falls back — while the non-coherent reference is immune to the phase-noise wash and barely moves between 1- and 20-block records. And on GPS L1C-D, Galileo E1B and any secondary-coded signal before sync, NWPR admits no window ever and defers permanently to a fallback with a different bias (issue #217).\n\nWhat NWPR is still better at is the top of the range, where the reference carries a self-leakage bias it does not: ≈0.13 dB at 45 dB-Hz and ≈0.40 at 50 on L1 C/A. See NoiseRefCN0Estimator.\n\nWhen to pass something else\n\nNWPRCN0Estimator remains exported and is the estimator to configure explicitly for externally supplied correlator outputs without a noise observation — a correlator-ingest path that cannot also report Σ|B|² for an untracked PRN. It is the one place it is still necessary; everywhere else, prefer appending a NoiseObservation per signal with append_noise_observation!.\n\n\n\n\n\n","title":"Tracking.default_cn0_estimator"},{"category":"type","location":"cn0_estimator.html#Tracking.NWPRCN0Estimator","page":"CN0 Estimator","text":"Van Dierendonck's narrowband/wideband power ratio (NWPR) CN0 estimator.\n\nIt was the library default until the per-band noise reference landed, and remains the estimator to configure explicitly on a correlator-ingest path with no noise observation — a producer that hands over CorrelatorOutputs but cannot also report Σ|B|² for an untracked PRN. That is the one place it is still the right choice; see default_cn0_estimator for the measurements that moved the default, and NWPRCN0Estimator(::AbstractGNSSSignal) for the constructor that sizes its window for the signal.\n\nOver a narrowband window of M consecutive records it forms\n\nNBP = |Σ_M prompt|²          (coherent — narrowband)\nWBP =  Σ_M |prompt|²         (incoherent — wideband)\n\ncombines the windows that fit in num_records records into a mean power ratio μ̂, and reports\n\nμ̂    = Σ_K NBP_k / Σ_K WBP_k\nĈ/N₀ = (1 / T) · (μ̂ − 1) / (M − μ̂)\n\nwith T the record's own integration time (the integration_time argument of estimate_cn0). Reference: A. J. Van Dierendonck, \"GPS Receivers\", ch. 8 in Global Positioning System: Theory and Applications, Vol. I, ed. B. W. Parkinson & J. J. Spilker Jr.; the formulas above are reproduced on ESA Navipedia.\n\nWhy the ratio of the sums, and not the mean of the ratios\n\nThe reference spells the mean ratio as μ̂ = (1/K) Σ NBP_k / WBP_k, the mean of the per-window ratios. The inversion (μ̂ − 1) / (M − μ̂) is derived from μ = E[NBP] / E[WBP], which the ratio of the sums estimates consistently and the mean of the ratios does not: E[NBP/WBP] < E[NBP]/E[WBP] at finite M, so the mean of the ratios reads low. Measured on synthetic prompts with K → ∞, so that only the bias is left:\n\ntrue C/N₀ mean of ratios, M = 2 M = 5 M = 20 ratio of sums, any M\n20 dB-Hz 18.4 19.3 19.8 20.0\n25 dB-Hz 23.6 24.4 24.9 25.0\n30 dB-Hz 28.9 29.6 29.9 30.0\n\nThe size of the correction is a function of the window length, and at the default five-block window it is small: ~0.6 dB of the bias above is left there, and in the loop the two forms agree to a few tenths (+0.2 / −0.1 / +0.4 dB at a true 20 / 25 / 30 dB-Hz, measured from the same buffered windows). It earns its keep at shorter windows — +1.4 / +1.9 / +1.0 dB at a two-block one — and at the M = 20 of the classic GPS L1 C/A configuration the two agree to ~0.15 dB, which is why the literature's form does no harm there. The reason to prefer the ratio of the sums anyway is that it is the consistent estimator of μ at every window length, so the estimate does not depend on how the window was chosen.\n\nThe correction is a shift, not a variance trade: the spread is unchanged, and at equal false-alarm rate the ratio of the sums is the slightly better detector too — at M = 2 and a 0.1 % false-alarm rate it detects a true 25 dB-Hz signal in 22.5 % of updates against the mean of the ratios' 15.0 %.\n\nWhy it is the default\n\nNWPR is usable as a detection statistic near threshold, which MomentsCN0Estimator is not: the moment ratio's sample moments manufacture signal power out of noise at finite window length, so at its default 100-prompt window M2M4 reports a median of ~27.6 dB-Hz on pure noise and cannot separate a true 20 dB-Hz signal from noise at all (a code lock threshold below ~30 dB-Hz can never trip, and one at 30 dB-Hz has a ~19 % per-update false-alarm rate). NWPR on the identical prompt streams tracks the truth from 20 dB-Hz up and reports \"no signal\" on noise, with a visibly tighter spread below 32 dB-Hz (Falletti, Pini & Lo Presti, IEEE T-AES 47(1):420–437, 2011). See JuliaGNSS/Tracking.jl#217 for the measurements.\n\nThe coherence constraint, and why this belongs in Tracking\n\nNBP is a coherent sum, so a window must not straddle a navigation-bit flip (a mid-window flip costs ~7 dB) and must stay short against the residual Doppler (M·T ≪ 1/(2·Δf); 25 Hz over a 20 ms window is half a cycle and costs ~9 dB). Where the bit flips sit is something the tracking loop knows and a consumer of get_filtered_prompts does not, so the window is taken from the navigation-bit grid in CN0UpdateContext — that is the whole reason this estimator lives here rather than on top of the prompt stream:\n\nsignal state narrowband window\nbit / secondary sync found, data-bearing signal num_narrowband_code_blocks, tiling the navigation bit from its start\nbit / secondary sync found, pilot (no data) num_narrowband_code_blocks (no bit grid to respect)\nsync not found yet, data-bearing without secondary code num_presync_narrowband_code_blocks, unaligned\nsync not found yet, signal with a secondary code none — the unknown overlay flips sign every code block\none symbol per code block (GPS L1C-D, Galileo E1B) none — no coherent window longer than one record exists\nrecord at least as long as its own window none — a one-record window has NBP == WBP by construction\n\nThe pre-sync window matters more than it may look: the CFAR bit-edge detector needs seconds to lock at 35 dB-Hz and does not lock at all below ~30 dB-Hz, so a bit-aligned window alone would leave exactly the regime this estimator exists for on the fallback estimator. An unaligned window of M records inside an L-block bit straddles a flip with probability (M−1)/L, which at the default five blocks of a 20-block GPS L1 C/A bit costs ~0.6 dB of bias below 30 dB-Hz (and up to ~6 dB at a strong signal, for the fraction of a second until sync is found) while removing the moment ratio's noise floor entirely.\n\nThe window is capped by the loop's coherence time, not by the bit period\n\nA bit-aligned window does not have to span the whole bit: windows of num_narrowband_code_blocks tile the bit from its start, which straddles no flip either and keeps the coherent sum inside the loop's coherence time. That cap is what the length is for — the longest flip-free window is not the best one. The gain from a longer window saturates quickly, because num_records records are combined either way and only their partition changes: at a true 25 dB-Hz, going from a 5-record to a 20-record window buys 0.8 dB of spread. The cost does not saturate. Residual phase noise makes the coherent sum lose power, which is a bias — the one error more averaging cannot remove. In the loop at a true 25 dB-Hz, with the conventional PLL at 1 ms records:\n\nwindow 2 records 5 10 20 (one full L1 C/A bit)\nreported C/N₀ 24.6 22.9 19.8 15.9\n\nso the default cap is deliberately short (~5 ms of code blocks, see default_cn0_estimator). Raise it for a pilot, a narrow carrier loop or a signal that is never weak; the ideal-coherence estimate improves monotonically with it.\n\nWhere no window is admissible at all the fallback estimator's value is reported instead — a different estimator, with a different bias and, at the default MomentsCN0Estimator, a ≈27.6 dB-Hz floor on pure noise. That is what NoiseRefCN0Estimator exists to retire (issue #217). It also includes records that are themselves at least as long as the window — with set_preferred_num_code_blocks_to_integrate! at one navigation bit, say, a window closes on a single record, NBP == WBP identically, and the estimator reports its fallback for good.\n\nFields / configuration\n\nnum_records — how many records the estimate averages over, i.e. the memory of the estimator (100 by default, ~100 ms at GPS L1 C/A). The ring buffer of ratios is sized from it: num_records ÷ M ratios are averaged, so the memory stays put when M changes.\nnum_narrowband_code_blocks — window length in primary-code blocks: the cap on the coherent sum, and the window length itself for a signal with no navigation-bit grid (a pilot, or a bare prompt stream fed through the two-argument Tracking.update).\nnum_presync_narrowband_code_blocks — window length in primary-code blocks used while the bit grid is still unknown (see the table above); 0 disables the pre-sync window and reports the fallback until sync.\nbuffered_narrowband_powers, buffered_wideband_powers, ratio_current_index, filled_ratio_length, num_records_per_ratio, ratios_are_bit_aligned — the ring buffers of completed windows' NBP and WBP, the record count M they were formed with, and whether they followed the navigation-bit grid. The two powers are buffered separately because the estimate divides their sums. M enters the estimate, so a window completing with a different record count (records lengthened at bit sync, say) restarts the buffers — and so does a window completing on the other side of the sync transition, since a pre-sync window may have straddled a bit flip and must not be averaged together with clean bit-aligned ones. A window closing on a single record empties them instead of restarting them: the records have grown to the window length, so there is nothing left to average and the fallback takes over (the last row of the table above).\nnarrowband_sum, wideband_power, num_accumulated_records, num_accumulated_code_blocks — the currently open window.\nfallback — the estimator reported while no window has completed yet, and for the signals of the table above that never get one. Defaults to a MomentsCN0Estimator and is fed every prompt.\n\n\n\n\n\n","title":"Tracking.NWPRCN0Estimator"},{"category":"method","location":"cn0_estimator.html#Tracking.NWPRCN0Estimator-Tuple{AbstractGNSSSignal}","page":"CN0 Estimator","text":"NWPRCN0Estimator(\n    signal;\n    num_records,\n    num_narrowband_code_blocks,\n    kwargs...\n)\n\n\nConstruct an NWPRCN0Estimator whose coherent window is sized for signal: the whole code blocks covering about 5 ms, at least two of them — 5 blocks for a 1 ms code, 2 for GPS L1C-P's 10 ms one.\n\nAbout 5 ms is what a coherent sum survives with the default 18 Hz carrier loop at the low C/N₀ this estimator exists for; see NWPRCN0Estimator for the measurements and for when to raise it. The window is what caps the coherent sum for a data-bearing signal (whose windows tile the navigation bit) and is the window outright for a pilot, so sizing it in blocks without knowing the code period gets it wrong by the period's ratio.\n\nThis is the form to reach for on a correlator-ingest path with no noise observation, which is the one place NWPR is still the estimator to choose over the default NoiseRefCN0Estimator — see default_cn0_estimator:\n\nTrackedSat(\n    GPSL1C_P(),\n    prn,\n    code_phase,\n    doppler;\n    cn0_estimator = NWPRCN0Estimator(GPSL1C_P()),\n)\n\nEvery other keyword is forwarded unchanged.\n\n\n\n\n\n","title":"Tracking.NWPRCN0Estimator"},{"category":"type","location":"cn0_estimator.html#Tracking.MomentsCN0Estimator","page":"CN0 Estimator","text":"MomentsCN0Estimator to estimate the CN0\n\n\n\n\n\n","title":"Tracking.MomentsCN0Estimator"},{"category":"type","location":"cn0_estimator.html#Tracking.NoCN0Estimator","page":"CN0 Estimator","text":"A CN0 estimator that estimates nothing: it keeps no state, does no work per record, and reports -Inf dB-Hz. Use it to say \"do not measure this signal's C/N₀\" — either to skip the per-record work on a signal whose C/N₀ nobody reads, or, more importantly, to avoid publishing a number that cannot be trusted.\n\nTwo places where that matters:\n\nthe non-driver signals of a multi-signal TrackedSat whose C/N₀ is never read: cn0_estimator = (NWPRCN0Estimator(), NoCN0Estimator()) (the saving is small — the per-record update is well under a per cent of a correlation — so reach for this for the reason below, not for speed);\nas NWPRCN0Estimator's fallback, where it replaces the MomentsCN0Estimator's ~27.6 dB-Hz noise floor with an honest \"no estimate\" for the signals and phases that admit no coherent window (a secondary-coded signal before sync, or GPS L1C-D / Galileo E1B at any time): NWPRCN0Estimator(; fallback = NoCN0Estimator()).\n\n-Inf dB-Hz rather than NaN dB-Hz, even though \"not measured\" is what is meant: NaN dB-Hz >= threshold is true for every threshold with Unitful's Level comparison, so a NaN would clear every lock detector it met. -Inf compares false against any finite threshold, which is the safe answer to \"is this signal locked?\".\n\n\n\n\n\n","title":"Tracking.NoCN0Estimator"},{"category":"function","location":"cn0_estimator.html#Tracking.estimate_cn0","page":"CN0 Estimator","text":"estimate_cn0(estimator, integration_time)\n\n\nEstimates the CN0 based on the struct MomentsCN0Estimator.\n\n\n\n\n\nestimate_cn0(_, integration_time)\n\n\nAlways -Inf dB-Hz; see NoCN0Estimator for why that value and not NaN.\n\n\n\n\n\nestimate_cn0(estimator, integration_time)\n\n\nEstimate the CN0 from the buffered narrowband and wideband powers, dividing by integration_time — the record's integration time, which is the predetection integration time T of Van Dierendonck's formula (see NWPRCN0Estimator).\n\nThe mean power ratio is μ̂ = Σ NBP_k / Σ WBP_k — the ratio of the sums, not the mean of the per-window ratios, which is biased low at short windows; see NWPRCN0Estimator.\n\nUntil the first narrowband window has completed — and for good on a signal that admits no window at all — the fallback estimator's value is returned instead.\n\nThe mean ratio μ̂ lies in [1, M] by construction and both ends are reported as the limits of the expression rather than clamped to a magic number: μ̂ ≤ 1 means the coherent sum holds no more power than the incoherent one, i.e. no detectable signal, and yields -Inf dB-Hz; μ̂ ≥ M means no detectable noise and yields Inf dB-Hz (the same value a noise-free signal gets out of MomentsCN0Estimator). A consumer thresholding the estimate needs no special case for either; one averaging it does, which is why the infinities are documented rather than hidden behind an epsilon.\n\n\n\n\n\nestimate_cn0(estimator, integration_time)\n\n\nMean of the buffered per-record terms, converted once with dBHz.\n\nintegration_time is ignored: T was applied per record in update, where each record's own value was known — a record lengthened by set_preferred_num_code_blocks_to_integrate! is therefore handled correctly even when it sits in the ring beside shorter ones. The argument stays for interface uniformity with the other estimators.\n\nAn empty ring, and a mean that has not cleared zero, both report -Inf dB-Hz — the house convention that a missing estimate is -Inf and never NaN (see NoCN0Estimator for why).\n\n\n\n\n\n","title":"Tracking.estimate_cn0"},{"category":"type","location":"cn0_estimator.html#Tracking.AbstractCN0Estimator","page":"CN0 Estimator","text":"Abstract supertype for CN0 (carrier-to-noise-density ratio) estimators. Each TrackedSignal holds one estimator instance, stored in a type parameter — pass any subtype instance as the cn0_estimator keyword of TrackedSignal / TrackedSat to replace the default NoiseRefCN0Estimator; see default_cn0_estimator for which to pick when. Custom estimators subtype this and implement Tracking.update and estimate_cn0, plus requires_noise_density if they read a measured noise floor.\n\n\n\n\n\n","title":"Tracking.AbstractCN0Estimator"},{"category":"type","location":"cn0_estimator.html#Tracking.CN0UpdateContext","page":"CN0 Estimator","text":"Per-record side information handed to Tracking.update alongside the prompt: everything the tracking loop knows about the record that an estimator cannot recover from the prompt stream alone. Its reason for existing is the navigation-bit grid — where the data-bit boundaries sit and whether they are known yet — which is what lets an estimator sum prompts coherently over exactly one symbol (NWPRCN0Estimator does) instead of straddling a bit flip.\n\nFields:\n\nsignal — the signal the record belongs to.\nnum_code_blocks — primary-code blocks this record spanned. One for the default configuration; more when the correlate step was lengthened by set_preferred_num_code_blocks_to_integrate! or an external producer handed over longer records.\nnum_code_blocks_per_bit — blocks that form one navigation bit (symbol) of signal; 20 for GPS L1 C/A, 1 for GPS L1C-D / Galileo E1B, and 0 for a pilot, which carries no data and therefore has no bit grid — post-sync its prompts stay coherent for as long as the loops hold them.\nbit_code_block_index — blocks already accumulated into the currently open navigation bit before this record, i.e. this record's own offset inside the bit; 0 means the record starts a fresh bit. It is -1 whenever this record's prompt cannot be summed coherently against the grid: before the signal's bit / secondary-code sync has been found, and — for a secondary-coded signal only — for a record of the fold that follows a sync detected in that same fold, which was correlated without the overlay wipe-off the sync just established (see _apply_correlator_output). Such a record's blocks still count towards the grid, so the offset stays right for the records after it.\nbit_buffer — the signal's BitBuffer as of before this record: the soft bits decoded so far, the open coherent bit accumulator, the lock polarity and the secondary-code phase.\nnoise_density — this signal's measured noise density N₀ (dimension 1/Hz), for an estimator that divides the prompt's power by a measured floor instead of inferring one from the prompt stream (NoiseRefCN0Estimator does). Per signal and not per band because the floor is the post-correlation one — see AbstractNoiseEstimator. It is a type parameter, not a sentinel: Nothing says no AbstractNoiseEstimator is configured for this signal at all, which is a static property of the setup, so every call site monomorphises and the estimators stay allocation-free. A configured source whose window is merely still empty never reaches here — the fold skips the update instead, so this field is unconditionally a plain scalar whenever it is not Nothing.\nintegration_time — this record's own T, so an estimator that works per record does not have to be told one T for a whole ring of records integrated at different lengths. nothing when the caller did not supply one (the bare-prompt-stream path).\n\n\n\n\n\n","title":"Tracking.CN0UpdateContext"},{"category":"method","location":"cn0_estimator.html#Tracking.update-Tuple{MomentsCN0Estimator, Any}","page":"CN0 Estimator","text":"update(estimator, prompt)\n\n\nBuffers the prompts such that they can be used to estimate the CN0. Returns a new estimator with the latest prompt added (immutable update). The moment ratio needs no side information, so this estimator implements only the prompt-only form of Tracking.update; see update(::AbstractCN0Estimator, ::Any, ::CN0UpdateContext) for the extension point that also receives the navigation-bit context.\n\n\n\n\n\n","title":"Tracking.update"},{"category":"method","location":"cn0_estimator.html#Tracking.update-Tuple{AbstractCN0Estimator, Any, CN0UpdateContext}","page":"CN0 Estimator","text":"update(estimator, prompt, _)\n\n\nFold one completed record's prompt into estimator and return the updated estimator (immutable update). This three-argument form is the one the tracking loop calls and the extension point for custom AbstractCN0Estimators that profit from the navigation-bit information in context (see CN0UpdateContext).\n\nThe default implementation drops context and calls the two-argument Tracking.update(estimator, prompt), so an estimator that only needs the prompt stream — like MomentsCN0Estimator — implements just that one.\n\n\n\n\n\n","title":"Tracking.update"},{"category":"method","location":"cn0_estimator.html#Tracking.update-Tuple{NWPRCN0Estimator, Any, CN0UpdateContext}","page":"CN0 Estimator","text":"update(estimator, prompt, context)\n\n\nAccumulate one record's prompt into the open narrowband window, taking the window length and its alignment from the navigation-bit grid in context (see NWPRCN0Estimator for the per-signal-state table). A window that follows the bit grid is only started where a window may start — the bit's own boundary, or a multiple of the window length inside the bit — and an open window is dropped rather than closed whenever it is no longer admissible: no window at all applies to this record (sync lost, or never established), or the grid moved under it when sync was found.\n\nA closed window contributes to the estimate only if it held at least two records; with one record NBP == WBP identically and the window carries no information.\n\n\n\n\n\n","title":"Tracking.update"},{"category":"method","location":"cn0_estimator.html#Tracking.update-Tuple{NWPRCN0Estimator, Any}","page":"CN0 Estimator","text":"update(estimator, prompt)\n\n\nAdvance the estimator on a bare prompt stream, with no navigation-bit context: each prompt counts as a one-block record and windows run back to back at num_narrowband_code_blocks, aligned to the first prompt. This is the form to use when folding a captured prompt stream by hand; inside track the three-argument form above is called, which aligns the window to the navigation-bit grid instead.\n\n\n\n\n\n","title":"Tracking.update"},{"category":"section","location":"vector_tracking.html#Vector-Tracking","page":"Vector Tracking","text":"VectorPLLAndDLL is a Doppler estimator for vector tracking (a vector delay/frequency-lock loop, VDFLL). Where the ConventionalPLLAndDLL closes each satellite's carrier and code loops independently with per-satellite loop filters, vector tracking closes them centrally: an external navigation filter — living outside Tracking.jl, e.g. in GNSSReceiver.jl — combines every satellite's measurements with the receiver dynamics and feeds back per-satellite NCO corrections. A satellite in a deep fade keeps being steered by the solution the healthy satellites constrain, which is what gives vector tracking its robustness under weak signal and high dynamics.\n\nTracking.jl owns only the signal-path half of that loop: it correlates, forms discriminators, and applies the corrections the navigation filter hands back. VectorPLLAndDLL is the interface between the two.","title":"Vector Tracking"},{"category":"section","location":"vector_tracking.html#The-per-integration-contract","page":"Vector Tracking","text":"VectorPLLAndDLL is configuration-only; the per-satellite state lives on each TrackedSat as a SatVectorPLLAndDLL (seeded through init_estimator_state, like every estimator — see Custom Doppler Estimator). Each time a satellite's estimator-driver signal (signals[1]) completes an integration, VectorPLLAndDLL does one of two things depending on that satellite's vt_on flag:\n\nvt_on = false — scalar fallback. The satellite runs an ordinary FLL-assisted PLL and DLL, identical to ConventionalAssistedPLLAndDLL (same default loop-filter types and the same auto-sized loop bandwidths). This is the pull-in mode a freshly acquired satellite tracks in until enable_vt! puts it into the vector loop.\nvt_on = true — vector closure. The navigation filter drives the NCOs instead of the local loop filters:\nthe code Doppler follows code_freq_update directly (the DLL loop filter is bypassed and holds its state);\nthe carrier's FLL branch is driven by carrier_freq_update while the PLL branch still runs on the satellite's own phase discriminator — so the carrier is a vector frequency lock loop with a retained local phase lock. (This needs the FLL-assisted ThirdOrderAssistedBilinearLF, the default carrier filter; with a plain PLL filter the carrier_freq_update has no input path and the vector carrier closure degrades to PLL-only.)\nIn this mode the DLL and FLL discriminator outputs and the prompt magnitude are accumulated on the per-sat state for the navigation filter to read and reset.\n\nThe satellite-shared carrier/code Doppler is always updated through the same carrier-aiding (aid_dopplers) used by the conventional estimator, and the same effective-bandwidth handling applies when a signal integrates N primary code blocks coherently: 1/N on the carrier loop, a stability cap against the actual integration time on the code loop.","title":"The per-integration contract"},{"category":"section","location":"vector_tracking.html#The-receiver-side-loop","page":"Vector Tracking","text":"A navigation filter drives VectorPLLAndDLL through the exported state managers. A typical iteration, after track! has produced new correlations:\n\n# 1. Put the satellites that have pulled in into the vector loop.\n#    `prns_in_lock` is whatever the receiver decides is usable (e.g. a\n#    C/N0 threshold); re-issuing the same set every iteration is a no-op.\nenable_vt!(track_state, prns_in_lock)\n\n# 2. Read the accumulated discriminator outputs the estimator collected\n#    for each vector-loop satellite, then reset the accumulators so the\n#    next block accumulates afresh.\nfor (prn, sat) in pairs(get_sat_states(track_state))\n    state = get_doppler_estimator_state(sat)\n    state.vt_on || continue\n    code_err = mean_code_discr(state)      # chips, or `nothing` if no data\n    carrier_err = mean_carrier_discr(state) # Hz, or `nothing` if no data\n    # … feed the mean measurements into the navigation filter …\nend\nreset_code_discr_acc!(track_state)\nreset_carrier_discr_acc!(track_state)\n\n# 3. Run the navigation filter, then feed its per-satellite NCO\n#    corrections back. Both setters take anything indexable by PRN\n#    (e.g. a Dictionaries.Dictionary) with an entry for every vector-loop\n#    satellite; satellites still in the scalar fallback are skipped.\nset_code_freq_updates!(track_state, code_freq_updates)\nset_carrier_freq_updates!(track_state, carrier_freq_updates)\n\nThe accumulators are stored as (count, sum) tuples; mean_code_discr / mean_carrier_discr apply the averaging convention (sum / count, returning nothing when nothing has accumulated) in one place, so consumers don't each re-implement the divide and the count == 0 guard. Reading and resetting are deliberately separate calls so the filter can read at its own (typically slower) rate than track!.","title":"The receiver-side loop"},{"category":"section","location":"vector_tracking.html#Multi-constellation-addressing","page":"Vector Tracking","text":"Each state manager has an all-groups form and a group-scoped form that takes a group selector (Symbol, Integer, or Val) as its first argument after track_state:\n\nenable_vt!(track_state, :gps, gps_prns_in_lock)\nset_code_freq_updates!(track_state, :galileo, galileo_code_updates)\n\nIn a multi-constellation receiver a PRN alone is ambiguous — GPS PRN 5 and Galileo PRN 5 are different satellites in different groups — so address each constellation's group explicitly. The all-groups form matches a PRN in every group and is only unambiguous for a single-group TrackState.","title":"Multi-constellation addressing"},{"category":"section","location":"vector_tracking.html#Loop-membership","page":"Vector Tracking","text":"enable_vt! and disable_vt! are the only way loop membership changes; the estimator never joins or drops a satellite on its own. Membership is not a lock indicator: a satellite in an outage stays in the loop and keeps being steered by the navigation filter from the shared solution. Deciding whether its (now uninformative) discriminator outputs should feed the measurement update is the navigation filter's responsibility, tracked on the receiver side rather than on the estimator state — disable_vt! is for satellites the filter gives up on entirely, and wants handed back to their own scalar loop (follow it with reset_loop_filters! for a transient-free handoff).","title":"Loop membership"},{"category":"section","location":"vector_tracking.html#Resetting","page":"Vector Tracking","text":"reset_loop_filters! zeroes the loop-filter integrators, the discriminator accumulators, and the NCO corrections, re-seeding the loop from the satellite's current (converged) Doppler while preserving the vt_on flag and any per-satellite bandwidth override. The NCO corrections are zeroed because the re-seeded Doppler already contains the last correction — keeping it would apply it twice. After a reset the navigation filter must re-issue its corrections via set_code_freq_updates! / set_carrier_freq_updates! before the next vector-closed integration.","title":"Resetting"},{"category":"section","location":"vector_tracking.html#API-reference","page":"Vector Tracking","text":"","title":"API reference"},{"category":"type","location":"vector_tracking.html#Tracking.VectorPLLAndDLL","page":"Vector Tracking","text":"Vector-tracking Phase-Locked Loop (PLL) and Delay-Locked Loop (DLL) Doppler estimator. Configuration-only — per-satellite state lives in each TrackedSat wrapper as a SatVectorPLLAndDLL, produced via init_estimator_state.\n\nIn vector tracking, the per-satellite tracking loops are closed centrally by a navigation filter (living outside this package, e.g. in GNSSReceiver.jl) instead of by per-satellite loop filters. The division of labor per integration:\n\nThis estimator accumulates each satellite's DLL / FLL discriminator outputs for the navigation filter to consume (and reset via reset_code_discr_acc! / reset_carrier_discr_acc!).\nThe navigation filter feeds NCO corrections back via set_code_freq_updates! / set_carrier_freq_updates!. While a satellite's vt_on flag is set, its code Doppler follows the navigation filter's code_freq_update directly and the FLL branch of the FLL-assisted carrier loop filter is driven by the navigation filter's carrier_freq_update (a vector delay / frequency lock loop) while the PLL branch still runs on the satellite's own discriminator.\nSatellites with vt_on unset (fresh from acquisition, before enable_vt! puts them in the loop) run a conventional scalar PLL/DLL as a fallback.\n\nType parameters CA and CO select the carrier and code loop filter types. The FLL-assisted ThirdOrderAssistedBilinearLF carrier filter is the default — with any non-assisted filter the navigation filter's carrier_freq_update has no input path into the carrier loop.\n\nEach bandwidth field is Maybe{typeof(1.0Hz)}: a nothing field (the default) means auto — init_estimator_state sizes the bandwidth per satellite from that sat's estimator-driver signal (signals[1]) via default_carrier_loop_filter_bandwidth / default_code_loop_filter_bandwidth — the same sizing as the conventional estimator, which the scalar fallback loop is. Like the conventional estimator, the effective bandwidth is scaled by 1/N at filter time when a signal coherently integrates N primary code blocks.\n\n\n\n\n\n","title":"Tracking.VectorPLLAndDLL"},{"category":"type","location":"vector_tracking.html#Tracking.SatVectorPLLAndDLL","page":"Vector Tracking","text":"Per-satellite state for the vector PLL and DLL Doppler estimator (VectorPLLAndDLL).\n\nOn top of the conventional loop-filter state it carries the vector-tracking (VT) interface to an external navigation filter (e.g. GNSSReceiver.jl's VDFLL):\n\ncode_discr_acc / carrier_discr_acc: (count, sum) accumulators of the DLL discriminator (chips) and the FLL discriminator (Hz) since the navigation filter last read and reset them (reset_code_discr_acc! / reset_carrier_discr_acc!). Only accumulated while vt_on.\ncode_freq_update / carrier_freq_update: the NCO corrections the navigation filter feeds back (set_code_freq_updates!, set_carrier_freq_updates!). While vt_on, they replace the scalar DLL loop-filter output and the FLL branch of the carrier loop filter respectively.\nvt_on: whether the navigation filter controls this satellite's NCOs. While false the satellite runs a conventional (scalar) PLL/DLL as a fallback and nothing is accumulated. Set by enable_vt! / disable_vt!.\n\n\n\n\n\n","title":"Tracking.SatVectorPLLAndDLL"},{"category":"function","location":"vector_tracking.html#Tracking.enable_vt!","page":"Vector Tracking","text":"enable_vt!(track_state, prns)\n\n\nPut every satellite whose PRN is in prns into the vector loop by setting its vt_on flag: from the next integration on, the navigation filter's NCO corrections drive its loops (set_code_freq_updates! / set_carrier_freq_updates!) and its DLL/FLL discriminators are accumulated for the filter to read. Satellites outside prns are left untouched, and re-enabling one already in the loop changes nothing — pass the currently usable set (e.g. the satellites in lock) every cycle rather than only the newly eligible ones.\n\nThe two-argument form walks every group; pass a group (Symbol or index) to address a single group — required in a multi-constellation receiver, where a PRN alone is ambiguous across groups.\n\nMutates track_state in place and returns it. disable_vt! is the inverse.\n\n\n\n\n\n","title":"Tracking.enable_vt!"},{"category":"function","location":"vector_tracking.html#Tracking.disable_vt!","page":"Vector Tracking","text":"disable_vt!(track_state, prns)\n\n\nTake every satellite whose PRN is in prns out of the vector loop by clearing its vt_on flag — the inverse of enable_vt!, for satellites the navigation filter drops (unhealthy, diverged, no longer visible). Each falls back to its own scalar PLL/DLL, whose loop filters still carry their pre-vector state and whose Doppler now contains the navigation filter's last NCO correction; follow up with reset_loop_filters! to re-seed the scalar loop from the current Doppler and make the handoff transient-free.\n\nAddressed exactly like enable_vt!: every group, or only group when one is given. Mutates track_state in place and returns it.\n\n\n\n\n\n","title":"Tracking.disable_vt!"},{"category":"function","location":"vector_tracking.html#Tracking.set_code_freq_updates!","page":"Vector Tracking","text":"set_code_freq_updates!(track_state, code_freq_updates)\n\n\nFeed the navigation filter's code-frequency NCO corrections back into the tracking loops. code_freq_updates maps PRN to the correction (anything indexable by PRN, e.g. a Dictionary) and must have an entry for every satellite in the vector loop; satellites with vt_on unset are skipped. Walks every group, or only group when one is given — the group-scoped form is required in a multi-constellation receiver, where each group carries its own corrections and PRNs collide across groups. Mutates track_state in place and returns it.\n\n\n\n\n\n","title":"Tracking.set_code_freq_updates!"},{"category":"function","location":"vector_tracking.html#Tracking.set_carrier_freq_updates!","page":"Vector Tracking","text":"set_carrier_freq_updates!(track_state, carrier_freq_updates)\n\n\nFeed the navigation filter's carrier-frequency NCO corrections back into the tracking loops. carrier_freq_updates maps PRN to the correction (anything indexable by PRN, e.g. a Dictionary) and must have an entry for every satellite in the vector loop; satellites with vt_on unset are skipped. Walks every group, or only group when one is given — the group-scoped form is required in a multi-constellation receiver, where each group carries its own corrections and PRNs collide across groups. Mutates track_state in place and returns it.\n\n\n\n\n\n","title":"Tracking.set_carrier_freq_updates!"},{"category":"function","location":"vector_tracking.html#Tracking.reset_code_discr_acc!","page":"Vector Tracking","text":"reset_code_discr_acc!(track_state)\n\n\nReset the code (DLL) discriminator accumulator of every satellite in the vector loop — called by the navigation filter after it has consumed the accumulated values via mean_code_discr. Satellites outside the vector loop are left untouched (they accumulate nothing).\n\nThe one-argument form walks every group; pass a group (Symbol or index) to reset a single group's satellites. Mutates track_state in place and returns it.\n\n\n\n\n\n","title":"Tracking.reset_code_discr_acc!"},{"category":"function","location":"vector_tracking.html#Tracking.reset_carrier_discr_acc!","page":"Vector Tracking","text":"reset_carrier_discr_acc!(track_state)\n\n\nReset the carrier (FLL) discriminator accumulator of every satellite in the vector loop — called by the navigation filter after it has consumed the accumulated values via mean_carrier_discr. Addressed exactly like reset_code_discr_acc!: every group, or only group when one is given. Mutates track_state in place and returns it.\n\n\n\n\n\n","title":"Tracking.reset_carrier_discr_acc!"},{"category":"function","location":"vector_tracking.html#Tracking.mean_code_discr","page":"Vector Tracking","text":"mean_code_discr(state)\n\n\nMean DLL (code) discriminator accumulated on state since the last reset_code_discr_acc!, in chips, or nothing if nothing has been accumulated yet (the (count, sum) accumulator's count is 0). This is the single place the accumulator's averaging convention lives — read it here rather than dividing code_discr_acc by hand, so a change to how the accumulator is stored can't silently diverge between consumers.\n\n\n\n\n\n","title":"Tracking.mean_code_discr"},{"category":"function","location":"vector_tracking.html#Tracking.mean_carrier_discr","page":"Vector Tracking","text":"mean_carrier_discr(state)\n\n\nMean FLL (carrier) discriminator accumulated on state since the last reset_carrier_discr_acc!, in Hz, or nothing if nothing has been accumulated yet (count == 0). The carrier counterpart to mean_code_discr.\n\n\n\n\n\n","title":"Tracking.mean_carrier_discr"},{"category":"section","location":"custom_doppler_estimator.html#Custom-Doppler-Estimator","page":"Custom Doppler Estimator","text":"Tracking.jl ships ConventionalPLLAndDLL (and the FLL-assisted variant ConventionalAssistedPLLAndDLL), but you can plug in a different Doppler-estimation algorithm — e.g. a Kalman filter or a joint-channel estimator — by implementing a small set of methods.","title":"Custom Doppler Estimator"},{"category":"section","location":"custom_doppler_estimator.html#Where-state-lives","page":"Custom Doppler Estimator","text":"A TrackedSat carries everything per-satellite: the shared carrier/code Doppler and phase, a tuple of TrackedSignals (one per signal being tracked), and the per-satellite estimator state in doppler_estimator_state. The estimator object itself (<: AbstractDopplerEstimator) is configuration plus any shared state that spans satellites or systems. Cleanly separating per-sat state from shared state makes the signal-path code (downconvert, correlate, sample-bookkeeping) estimator-agnostic — it touches the per-signal and per-sat fields directly and rewraps doppler_estimator_state unchanged.","title":"Where state lives"},{"category":"section","location":"custom_doppler_estimator.html#What-you-implement","page":"Custom Doppler Estimator","text":"An estimator type subtyping AbstractDopplerEstimator. It carries configuration and any cross-satellite or cross-system shared state (filter parameters, joint-state vectors, …). Per-satellite state does not live here.\nA per-satellite state struct (any name and shape you like). For the conventional estimator it is SatConventionalPLLAndDLL holding the loop filters and seed Dopplers; for a Kalman filter it might be the Kalman state vector.\nAn init_estimator_state method for your estimator. It produces the initial per-sat state for a sat entering the track set (acquisition → tracking handoff):\nTracking.init_estimator_state(::MyEstimator, sat::TrackedSat) = MyPerSatState(...)\nIt must be pure (no observable side effects): besides seeding real sats, it is also called on the throwaway PRN-0 template sat that fixes a group's slot type at TrackState construction, as a type probe when validating pre-built sats, and by reset_loop_filters!. Register sats into shared state in update_estimator_on_handoff (below), never here.\n(Optional) An update_estimator_on_handoff method, if your estimator carries cross-satellite or cross-system shared state that has to change when sats join the track set (joint-channel covariance, per-batch normalization terms, …):\nTracking.update_estimator_on_handoff(est::MyEstimator, new_sats) = ...\nIt is called once per add_satellite! / merge_sats call with the dictionary of incoming sats and runs after per-sat seeding, so init_estimator_state sees the pre-update shared state. The default returns est unchanged, so estimators with no shared state need not implement it.\nType constraint: the returned estimator must have the same concrete type as the input. TrackState is parameterized on the estimator type, and changing it would break inference. Keep the estimator an immutable struct and put any growing shared state in resizable containers (e.g. Vector, Matrix) you push!/resize! in place; if scalar fields need replacing, rebuild the estimator with Setfield.@set or a copying constructor.\nUse the return value of the handoff functions. Every entry point carries the estimator you return in the TrackState it gives back. Since TrackState is immutable, even add_satellite! can only honor a rebuilt estimator through its return value — write track_state = add_satellite!(track_state; ...), not a bare add_satellite!(track_state; ...), if your estimator rebuilds itself on handoff. (For in-place estimators, including the conventional ones, the returned TrackState is the input itself.)\nAn estimate_dopplers_and_filter_prompt method dispatched on TrackState{<:Any, <:MyEstimator}. This is where the actual update logic runs, once per chunk (see Chunked Doppler updates). It walks each group in track_state.groups, reads the band's BandMeasurement from the measurements::BandMeasurements NamedTuple via get_band_id(group.band), and produces new TrackedSats with updated carrier_doppler/code_doppler and updated per-sat estimator state.\nEach signal's correlator outputs completed during the chunk are in its correlator_outputs::Vector{CorrelatorOutput} (a chunk may hold zero, one, or several per signal), each carrying the raw correlator, its integrated-sample count, and the end sample index. Fold over them in order — threading whatever filter state you carry — and write the NCO Doppler once (typically from the last output). Empty each signal's correlator_outputs when done, and remember the NCO Doppler is held fixed across the whole chunk, so a discriminator that needs the replica's Doppler should read the satellite's code_doppler/carrier_doppler (the value that generated the chunk), not an intermediate per-output estimate.\nThe matching mutating method estimate_dopplers_and_filter_prompt!(track_state, measurements) is what track! calls. To support real-time loops, define both — the mutating version walks each group's satellites.values::Vector{TrackedSat} and reassigns slots in place.\nThe estimator only needs the per-band sampling frequency out of measurements (to turn each output's integrated_samples into an integration time and to normalize the DLL discriminator). The shipped ConventionalPLLAndDLL therefore also accepts a bare per-band sampling-frequency source in place of measurements — a NamedTuple/Dict keyed by get_band_id — so an external correlator producer can run the estimator with no sample buffer (see External correlator producers). A custom estimator that likewise reads only the rate is encouraged to offer the same overload.","title":"What you implement"},{"category":"section","location":"custom_doppler_estimator.html#Skeleton","page":"Custom Doppler Estimator","text":"The skeleton below is the smallest possible working estimator: every method returns a constant. Real estimators replace these bodies with the actual algorithm, but the structure — five methods, two structs — is what the rest of Tracking.jl dispatches on.\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: AbstractDopplerEstimator, TrackedSat, TrackState,\n                       SignalGroup, BandMeasurements, get_band_id\n\njulia> # 1. Estimator type — config + any shared state\n       struct MyEstimator <: AbstractDopplerEstimator end\n\njulia> # 2. Per-sat state struct\n       struct SatMyEstimator end\n\njulia> # 3. Seed each sat\n       Tracking.init_estimator_state(::MyEstimator, ::TrackedSat) = SatMyEstimator();\n\njulia> # 4. (Optional) shared-state update on handoff — default returns\n       # `est` unchanged, so estimators with no shared state may skip this.\n       Tracking.update_estimator_on_handoff(est::MyEstimator, new_sats) = est;\n\njulia> # 5a. Immutable form — walks each group, looks up its band's\n       # `BandMeasurement`, returns a fresh `TrackState`. Real implementations\n       # read `sat.signals[*].correlator` and compute new dopplers; here\n       # we just return the state unchanged.\n       function Tracking.estimate_dopplers_and_filter_prompt(\n           track_state::TrackState{<:Any, <:MyEstimator},\n           measurements::BandMeasurements,\n       )\n           return track_state\n       end;\n\njulia> # 5b. In-place form — what `track!` actually calls. Real\n       # implementations write back into `g.satellites.values[i]`.\n       function Tracking.estimate_dopplers_and_filter_prompt!(\n           track_state::TrackState{<:Any, <:MyEstimator},\n           measurements::BandMeasurements,\n       )\n           return track_state\n       end;\n\nPlug it into a TrackState like any other estimator:\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(; signal = GPSL1CA(), doppler_estimator = MyEstimator());\n\njulia> track_state = add_satellite!(track_state; prn = 1, code_phase = 0.0, carrier_doppler = 1000.0Hz);\n\njulia> track_state = track(zeros(ComplexF64, 4000), track_state, 4e6Hz);\n\njulia> get_doppler_estimator_state(get_sat_state(track_state, 1))\nSatMyEstimator()\n\nThe existing ConventionalPLLAndDLL implementation in src/conventional_pll_and_dll.jl shows the full pattern, including how the immutable and in-place forms share a _update_tracked_sat_doppler helper so they cannot drift, and how the per-signal walk distinguishes the estimator-driver signal (signals[1], which drives the conventional PLL/DLL) from the other signals (which only have their prompts filtered). That split is a convention ConventionalPLLAndDLL chooses — your own estimator can use every signal's state any way you like.","title":"Skeleton"},{"category":"section","location":"custom_doppler_estimator.html#What-stays-generic","page":"Custom Doppler Estimator","text":"You do not override the downconvert/correlate path. The per-sat update after correlation reads only estimator-agnostic fields (code/carrier phase, integrated samples, correlator) and rewraps the existing doppler_estimator_state unchanged. add_satellite! and merge_sats call init_estimator_state with whatever estimator the TrackState was built with.","title":"What stays generic"},{"category":"section","location":"custom_doppler_estimator.html#API-reference","page":"Custom Doppler Estimator","text":"","title":"API reference"},{"category":"type","location":"custom_doppler_estimator.html#Tracking.AbstractDopplerEstimator","page":"Custom Doppler Estimator","text":"Abstract supertype for doppler estimators. Concrete subtypes carry estimator configuration (and any cross-satellite or cross-system shared state). The per-satellite state used by the estimator lives in each TrackedSat wrapper — see init_estimator_state for the extension point.\n\n\n\n\n\n","title":"Tracking.AbstractDopplerEstimator"},{"category":"function","location":"custom_doppler_estimator.html#Tracking.init_estimator_state","page":"Custom Doppler Estimator","text":"Build the per-satellite Doppler-estimator state used by estimator for the given satellite. A custom doppler estimator must define this method for its AbstractDopplerEstimator subtype.\n\nThis function must be pure (free of observable side effects): besides seeding each real satellite on entry, it is also called to build the throwaway PRN-0 template sat that fixes a group's dictionary slot type at TrackState construction, as a type probe when validating pre-built sats, and by reset_loop_filters! to re-seed existing satellites. Estimators with cross-satellite shared state must therefore not register satellites here — perform shared-state registration in update_estimator_on_handoff, which is called exactly once per handoff with the real incoming satellites.\n\n\n\n\n\n","title":"Tracking.init_estimator_state"},{"category":"function","location":"custom_doppler_estimator.html#Tracking.update_estimator_on_handoff","page":"Custom Doppler Estimator","text":"update_estimator_on_handoff(estimator, _new_sats)\n\n\nOptionally update an estimator's cross-satellite or cross-system shared state when new satellites enter the track set. Called once per handoff entry point (merge_sats, add_satellite, add_satellite!) with the dictionary of incoming satellites, after per-sat seeding via init_estimator_state.\n\nThe default returns estimator unchanged, so estimators with no shared state need not implement it.\n\nThe returned estimator must have the same concrete type as the input — TrackState is parameterized on the estimator type, and changing it would break inference. For growing shared state, hold the storage in a resizable container (e.g. Vector, Matrix) on an otherwise immutable estimator and push!/resize! it in place; rebuild the estimator with Setfield.@set or a copying constructor when fields need replacing.\n\nEvery entry point honors the return value: the TrackState it returns carries the returned estimator. TrackState is immutable, so even the in-place add_satellite! can only honor a rebuilt estimator through its return value — callers must keep using the returned TrackState rather than the one they passed in (for in-place estimators the two are identical).\n\n\n\n\n\n","title":"Tracking.update_estimator_on_handoff"},{"category":"function","location":"custom_doppler_estimator.html#Tracking.estimate_dopplers_and_filter_prompt","page":"Custom Doppler Estimator","text":"estimate_dopplers_and_filter_prompt(\n    track_state,\n    sampling_frequencies\n)\n\n\nEstimate Dopplers and filter prompts for all satellites where the correlation has reached the end of the code or multiples of that. This function uses the conventional PLL and DLL implementation to estimate Dopplers for carrier and code. Those Doppler estimations will be used to create the next replicas to downconvert and decode the incoming signal. In addition to the Doppler estimation it will also filter the prompt with the configured post correlation filter. In the case that the that the correlation hasn't reached the end, e.g. in the case the incoming signal did not provide enough samples, it will return struct with zeroed values.\n\nThe sampling-frequency argument may be either a BandMeasurements NamedTuple (the track path, from which the per-band rate is read) or a bare per-band sampling-frequency source — a NamedTuple/Dict keyed by get_band_id mapping each band to its sampling frequency. The latter is the entry point for an external correlator producer (e.g. an FPGA): it needs no sample buffer, only the per-signal correlator_outputs and the rate that maps integrated_samples to an integration time. See External correlator producers.\n\n\n\n\n\nestimate_dopplers_and_filter_prompt(\n    track_state,\n    sampling_frequencies\n)\n\n\nEstimate Dopplers and filter prompts for all satellites where the correlation has reached the end of the code or multiples of that, using the vector PLL and DLL implementation — see VectorPLLAndDLL for how the per-satellite loops are closed. In the case that the correlation hasn't reached the end, e.g. in the case the incoming signal did not provide enough samples, the state is passed through unchanged.\n\n\n\n\n\n","title":"Tracking.estimate_dopplers_and_filter_prompt"},{"category":"function","location":"custom_doppler_estimator.html#Tracking.estimate_dopplers_and_filter_prompt!","page":"Custom Doppler Estimator","text":"estimate_dopplers_and_filter_prompt!(\n    track_state,\n    sampling_frequencies\n)\n\n\nIn-place version of estimate_dopplers_and_filter_prompt. Walks each group's Vector{TrackedSat} backing storage and overwrites slots with the new immutable TrackedSat value. Returns the same track_state object — allocation-free in steady state when track!'s preconditions are met.\n\nAs with the immutable form, the second argument is either a BandMeasurements NamedTuple or a bare per-band sampling-frequency source keyed by get_band_id. The estimator only reads the per-band sampling frequency (to turn each output's integrated_samples into an integration time and to normalize the DLL discriminator); it consumes each signal's correlator_outputs and clears them, whether they were produced by the software correlate phase or appended by an external producer via append_correlator_output!.\n\n\n\n\n\nestimate_dopplers_and_filter_prompt!(\n    track_state,\n    sampling_frequencies\n)\n\n\nIn-place version of estimate_dopplers_and_filter_prompt for the vector PLL and DLL estimator. Like the conventional form, the second argument is a BandMeasurements NamedTuple or a bare per-band sampling-frequency source keyed by get_band_id; each signal's correlator_outputs are consumed and cleared.\n\n\n\n\n\n","title":"Tracking.estimate_dopplers_and_filter_prompt!"},{"category":"section","location":"correlator.html#Correlator","page":"Correlator","text":"The correlator computes the correlation between the incoming signal and the locally generated replica code at multiple code-phase offsets (early, prompt, late, …). Each TrackedSignal on a TrackedSat holds its own correlator, so a multi-signal satellite carries one correlator per signal.","title":"Correlator"},{"category":"section","location":"correlator.html#Why-early/late-spacing-matters","page":"Correlator","text":"The prompt accumulator is what the PLL discriminator and CN0 estimator consume; the surrounding early/late accumulators exist purely to give the DLL a slope estimate of the code-correlation triangle. The early-minus-late amplitude crosses zero exactly when the replica is aligned with the incoming code, and its sign on either side tells the DLL which direction to push.\n\nNotation used below:\n\nd — early-to-late spacing, expressed as a fraction of one PRN chip (so d = 0.5 means E and L sit ½ chip apart).\nε — code-phase error: the offset between the locally generated replica and the incoming code, in chips. ε = 0 is perfect lock.\nE, P, L — early, prompt, and late correlator outputs (complex accumulators).\nB_fe — pre-correlation (front-end) bandwidth in Hz: the analog + ADC bandwidth that limits how sharp the correlation peak can be.\nT_chip — one PRN chip period in seconds (1 / f_code); equivalently B_fe · T_chip is the front-end bandwidth measured in chip-rate units.\nFs — sampling frequency in Hz.\nf_code — chip rate in Hz (1.023 MHz for GPS L1 C/A).\n\nWith those names settled, the spacing d determines several DLL properties together:\n\nThermal-noise jitter decreases as d shrinks, because the noise on the early and late accumulators is correlated and partially cancels in the early-minus-late difference. This is the central result of Van Dierendonck, Fenton & Ford (\"Theory and Performance of Narrow Correlator Spacing in a GPS Receiver\", NAVIGATION 39(3), 1992) and is contrary to the intuition that wider should give a smoother discriminator — it only does so when E and L are time-multiplexed (dither-DLL), which Tracking.jl does not do; here E, P, L are computed simultaneously from the same sample stream.\nMultipath error also decreases as d shrinks: a narrower discriminator samples the correlation function nearer the peak, where multipath distortion is smallest. Same source, same paper.\nRobustness to dynamics decreases as d shrinks. The discriminator is only linear in ε over |ε| < d/2 (the Springer Handbook of GNSS, §14.4.4, calls this the pull-in range T_DLL); outside it the slope drops to zero with the sign still correct, so the loop converges but no longer behaves as designed. The lock rule of thumb (Springer eq. 14.57) is 3σ_DLL + ε_D ≤ T_DLL ≈ d/2, so a narrow d shrinks the budget for dynamic stress error ε_D and the receiver is more prone to losing lock under fast LOS acceleration or scintillation. This is why NovAtel's original narrow-correlator design widens d during acquisition and only narrows once in steady-state lock.\nFront-end bandwidth interaction. The benefits above saturate (and eventually reverse) once d drops below roughly 1 / (B_fe · T_chip): a band-limited correlation peak is rounded, so the early-minus-late slope flattens and noise jitter starts to grow again. Van Dierendonck reports d ≈ 0.1 is optimal at 8 MHz front-end bandwidth on L1 C/A.\n\nSo \"narrower is better, up to a point that the front-end bandwidth allows\" — there is no fundamental noise penalty until the band-limit kicks in.","title":"Why early/late spacing matters"},{"category":"section","location":"correlator.html#Tracking.jl-specific-constraint:-sample-rate-sets-the-minimum-d","page":"Correlator","text":"Tracking.jl expresses the spacing as a preferred_early_late_to_prompt_code_shift fraction-of-chip; the actual sample shift is the nearest integer at the current sampling rate, with a floor of one sample. The realized d is therefore max(1, round(d_pref · Fs / f_code)) · f_code / Fs chips. To realize the recommended narrow d ≈ 0.1 on L1 C/A (1.023 MHz chip rate) the sampling rate has to be at least 10 · f_code ≈ 10.23 MHz; at 4 MHz the nearest integer shift saturates the spacing at 1 / (4e6 / 1.023e6) ≈ 0.26 chips regardless of what preferred_early_late_to_prompt_code_shift is set to. So narrow-correlator operation in Tracking.jl is bottlenecked by both the front-end bandwidth (the physical argument above) and the ADC/sampling rate (this implementation detail).\n\nThe VeryEarlyPromptLateCorrelator adds a second outer pair (very-early / very-late) to handle the multi-peaked code-correlation function of BOC-modulated signals like Galileo E1B — the inner pair locks the central peak and the outer pair disambiguates from the side-peaks.","title":"Tracking.jl-specific constraint: sample rate sets the minimum d"},{"category":"section","location":"correlator.html#Default-Correlators","page":"Correlator","text":"The default correlator depends on the GNSS signal type and is returned by get_default_correlator:\n\nEarlyPromptLateCorrelator for GPS L1 C/A, GPS L2CM, GPS L2CL, GPS L5I, GPS L5Q, Galileo E5a-I, Galileo E5a-Q (BPSK modulation)\nVeryEarlyPromptLateCorrelator for GPS L1C-D, GPS L1C-P, Galileo E1B and Galileo E1C (BOC / CBOC / TMBOC modulation)","title":"Default Correlators"},{"category":"section","location":"correlator.html#Correlator-Types","page":"Correlator","text":"","title":"Correlator Types"},{"category":"section","location":"correlator.html#Accessing-Correlator-Values","page":"Correlator","text":"","title":"Accessing Correlator Values"},{"category":"section","location":"correlator.html#Correlator-outputs","page":"Correlator","text":"Each completed integration within a processing chunk is recorded as a CorrelatorOutput, collected per signal and consumed by the Doppler estimator after the chunk (see Chunked Doppler updates). An external correlator producer (e.g. an FPGA) can build these itself and feed them straight to the estimator — see External correlator producers.","title":"Correlator outputs"},{"category":"section","location":"correlator.html#Sample-shifts","page":"Correlator","text":"The downconvert/correlate inner loop walks the incoming sample buffer once and asks the correlator where to lay down each accumulator relative to the prompt — that mapping is get_correlator_sample_shifts. The spacing in samples between the outermost shifts is get_early_late_sample_spacing, which is what the DLL uses to scale its discriminator output back to chips.","title":"Sample shifts"},{"category":"section","location":"correlator.html#Antenna-and-accumulator-counts","page":"Correlator","text":"","title":"Antenna and accumulator counts"},{"category":"section","location":"correlator.html#Post-correlation-filter","page":"Correlator","text":"A post-correlation filter is applied to the correlator output before the Doppler estimator sees it. The default is a passthrough; for multi-antenna tracking a beamformer is the natural override.","title":"Post-correlation filter"},{"category":"section","location":"correlator.html#Integration-sizing","page":"Correlator","text":"","title":"Integration sizing"},{"category":"section","location":"correlator.html#Adjusting-the-spacing-of-the-stock-correlator","page":"Correlator","text":"The stock EarlyPromptLateCorrelator already exposes its spacing as a constructor knob — you do not need a custom correlator type just to change d. Pass the desired chip fraction via preferred_early_late_to_prompt_code_shift and hand it to a TrackedSat:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(; signal = GPSL1CA());\n\njulia> sat = TrackedSat(GPSL1CA(), 1, 50.0, 1000.0Hz;\n                        correlator = EarlyPromptLateCorrelator(\n                            preferred_early_late_to_prompt_code_shift = 0.1,\n                        ));\n\njulia> track_state = add_satellite!(track_state, :default, sat);\n\njulia> get_correlator(track_state, :default, 1, 1).preferred_early_late_to_prompt_code_shift\n0.1\n\nLikewise VeryEarlyPromptLateCorrelator exposes both the inner (preferred_early_late_to_prompt_code_shift, default 0.15) and the outer (preferred_very_early_late_to_prompt_code_shift, default 0.6) spacing.\n\nRemember the sample-rate floor: the actual realized d is the nearest integer sample shift at the current sampling rate (minimum 1), so on low-Fs configurations the preferred value may be rounded to a coarser realized value (see the Tracking.jl-specific constraint above).","title":"Adjusting the spacing of the stock correlator"},{"category":"section","location":"correlator.html#Changing-the-spacing-of-a-satellite-that's-already-tracking","page":"Correlator","text":"TrackedSat, TrackedSignal, and EarlyPromptLateCorrelator are all immutable structs, so changing the spacing means producing a replacement sat with the new value and writing it into the satellites-dictionary slot. The cleanest way is Accessors.@set for the deeply-nested field update, followed by Dictionaries.set! for the dictionary write:\n\njulia> using Accessors, Dictionaries\n\njulia> sat     = track_state.groups[:default].satellites[1];\n\njulia> new_sat = @set sat.signals[1].correlator.preferred_early_late_to_prompt_code_shift = 0.7;\n\njulia> Dictionaries.set!(track_state.groups[:default].satellites, 1, new_sat);\n\njulia> get_correlator(track_state, :default, 1, 1).preferred_early_late_to_prompt_code_shift\n0.7\n\n@set preserves everything else on the correlator (including the in-flight accumulator), only swapping the one field. The dictionary slot is reassigned in place, so the TrackState's concrete type doesn't change and the hot path stays type-stable across the swap.","title":"Changing the spacing of a satellite that's already tracking"},{"category":"section","location":"correlator.html#Custom-Correlators","page":"Correlator","text":"Roll your own correlator when you need a layout the stock types don't provide — extra taps, asymmetric shifts, a non-standard get_correlator_sample_shifts mapping, or a different get_prompt_index arrangement. Subtype AbstractCorrelator (or AbstractEarlyPromptLateCorrelator to inherit the early/late accessors) and implement a small interface.","title":"Custom Correlators"},{"category":"section","location":"correlator.html#What-to-implement","page":"Correlator","text":"Method Purpose\nget_accumulators(c) Return the backing storage of accumulators (e.g. an SVector of ComplexF64).\nget_num_accumulators(c) Return how many accumulators the correlator holds. Defaults to size(get_accumulators(c), 1) — only override if your layout differs.\nupdate_accumulator(c, accumulators) Construct a fresh correlator instance with the given accumulators (immutable update).\nget_correlator_sample_shifts(c, sampling_frequency, code_frequency) Return an SVector{N,Int} of sample offsets relative to the prompt, ordered from latest replica to earliest.\n\nThe get_prompt_index default places the prompt at the middle of the accumulator vector — if you want a different layout (e.g. asymmetric shifts) override that too.","title":"What to implement"},{"category":"section","location":"correlator.html#Skeleton","page":"Correlator","text":"A 7-tap correlator with three early and three late accumulators (e.g. as a building block for a custom multipath-mitigation discriminator that fits a slope over multiple early/late samples rather than using a single E−L difference):\n\njulia> using Tracking, GNSSSignals, StaticArrays\n\njulia> using Tracking: AbstractEarlyPromptLateCorrelator,\n                       get_initial_accumulator,\n                       calc_preferred_code_shift_to_sample_shift,\n                       NumAnts, NumAccumulators\n\njulia> struct MultiTapCorrelator{M,T} <: AbstractEarlyPromptLateCorrelator{M}\n           accumulators::SVector{7,T}\n           tap_spacing_chips::Float64\n           # Dispatch on accumulator-vector element type to pin M, mirroring\n           # the pattern in `src/correlators/early_prompt_late.jl`.\n           function MultiTapCorrelator(\n               accumulators::AbstractVector{SVector{M,T}}, tap_spacing_chips,\n           ) where {M,T<:Complex}\n               new{M,SVector{M,T}}(accumulators, tap_spacing_chips)\n           end\n           function MultiTapCorrelator(\n               accumulators::AbstractVector{T}, tap_spacing_chips,\n           ) where {T<:Complex}\n               new{1,T}(accumulators, tap_spacing_chips)\n           end\n       end\n\njulia> # Convenience kwarg-only constructor — matches the EPL surface\n       function MultiTapCorrelator(; num_ants::NumAnts = NumAnts(1),\n                                     tap_spacing_chips = 0.1)\n           MultiTapCorrelator(\n               get_initial_accumulator(num_ants, NumAccumulators(7)),\n               tap_spacing_chips,\n           )\n       end;\n\njulia> Tracking.update_accumulator(c::MultiTapCorrelator, accumulators) =\n           MultiTapCorrelator(accumulators, c.tap_spacing_chips);\n\njulia> function Tracking.get_correlator_sample_shifts(\n           c::MultiTapCorrelator, sampling_frequency, code_frequency,\n       )\n           s = calc_preferred_code_shift_to_sample_shift(\n               c.tap_spacing_chips, sampling_frequency, code_frequency,\n           )\n           SVector(-3s, -2s, -s, 0, s, 2s, 3s)\n       end;\n\nTo use it, plug it into a TrackedSat and build the TrackState from that sat (so the slot type is taken from the custom correlator rather than the default EarlyPromptLateCorrelator):\n\njulia> using Tracking: Hz\n\njulia> sat = TrackedSat(GPSL1CA(), 1, 50.0, 1000.0Hz;\n                        correlator = MultiTapCorrelator(tap_spacing_chips = 0.1));\n\njulia> track_state = TrackState(GPSL1CA(), sat);\n\njulia> get_num_accumulators(get_correlator(track_state, 1))\n7\n\nThe default DLL discriminator only reads the outermost early/late pair, so a multi-tap correlator like this is most useful in combination with a custom Doppler estimator (see Custom Doppler Estimator) that walks every accumulator. The reference implementations in src/correlators/early_prompt_late.jl and src/correlators/very_early_prompt_late.jl show the full pattern including the multi-antenna SVector element type.","title":"Skeleton"},{"category":"function","location":"correlator.html#Tracking.get_default_correlator","page":"Correlator","text":"get_default_correlator(gpsl1)\nget_default_correlator(gpsl1, num_ants)\n\n\nGet the default correlator for the given GNSS system. Returns an EarlyPromptLateCorrelator for GPS L1 or a VeryEarlyPromptLateCorrelator for systems like Galileo E1B that use BOC modulation.\n\n\n\n\n\n","title":"Tracking.get_default_correlator"},{"category":"type","location":"correlator.html#Tracking.EarlyPromptLateCorrelator","page":"Correlator","text":"EarlyPromptLateCorrelator holding a user defined number of correlation values. The code is shifted in samples. Hence, the specified code shift is actually a preferred code shift, because depending on sampling frequency and code frequency the specified code shift might not be the actual code shift. It is as close as possible, though. The algorithm makes sure that at least one sample is shifted.\n\n\n\n\n\n","title":"Tracking.EarlyPromptLateCorrelator"},{"category":"type","location":"correlator.html#Tracking.VeryEarlyPromptLateCorrelator","page":"Correlator","text":"VeryEarlyPromptLateCorrelator holding a user defined number of correlation values. The code is shifted in samples. Hence, the specified code shift is actually a preferred code shift, because depending on sampling frequency and code frequency the specified code shift might not be the actual code shift. It is as close as possible, though. The algorithm makes sure that at least one sample is shifted.\n\n\n\n\n\n","title":"Tracking.VeryEarlyPromptLateCorrelator"},{"category":"function","location":"correlator.html#Tracking.get_early","page":"Correlator","text":"get_early(correlator)\n\n\nGet early correlator\n\n\n\n\n\n","title":"Tracking.get_early"},{"category":"function","location":"correlator.html#Tracking.get_prompt","page":"Correlator","text":"get_prompt(correlator)\n\n\nGet prompt correlator\n\n\n\n\n\n","title":"Tracking.get_prompt"},{"category":"function","location":"correlator.html#Tracking.get_late","page":"Correlator","text":"get_late(correlator)\n\n\nGet late correlator\n\n\n\n\n\n","title":"Tracking.get_late"},{"category":"function","location":"correlator.html#Tracking.get_accumulators","page":"Correlator","text":"get_accumulators(correlator)\n\n\nGet all correlator accumulators\n\n\n\n\n\n","title":"Tracking.get_accumulators"},{"category":"function","location":"correlator.html#Tracking.get_num_accumulators","page":"Correlator","text":"get_num_accumulators(correlator)\n\n\nGet number of accumulators\n\n\n\n\n\n","title":"Tracking.get_num_accumulators"},{"category":"method","location":"correlator.html#Tracking.get_num_ants-Tuple{AbstractCorrelator}","page":"Correlator","text":"get_num_ants(correlator)\n\n\nGet number of antennas from correlator\n\n\n\n\n\n","title":"Tracking.get_num_ants"},{"category":"type","location":"correlator.html#Tracking.CorrelatorOutput","page":"Correlator","text":"A single completed correlator output produced within one processing chunk.\n\ntrack! processes each measurement in fixed-size time chunks. Every time a signal's coherent integration completes inside a chunk, the (raw, un-normalized) accumulator is snapshotted into a [CorrelatorOutput] and appended to that signal's correlator_outputs array; the Doppler estimator then folds over those records after the chunk. Storing the raw correlator plus integrated_samples lets the estimator normalize it (dividing by the sample count) and matches last_fully_integrated_correlator.\n\nAn external correlator producer (e.g. an FPGA) can build these itself and feed them straight to the estimator with append_correlator_output!; see External correlator producers.\n\nFields:\n\ncorrelator: the raw accumulated correlator at completion (not normalized). Fill it with the accumulator scaling normalize expects — the raw sum-of-products over integrated_samples — which an external producer gets for free by reusing EarlyPromptLateCorrelator / update_accumulator.\nintegrated_samples: samples integrated into this output (for normalize, the loop-filter integration_time, and the bit-buffer block count). For an external producer this is the true sample count of that integration.\nsample_index: sample index at which this integration ended, on the time grid the Doppler estimator and vector tracking read. The software correlate phase writes it buffer-relative — the end sample within the current track! measurement (signal_start_sample returns to 1 at the top of every track! call). An external producer with a free-running global sample counter must therefore map its global timestamp onto the same per-chunk origin before storing it here: subtract the sample index of the current chunk/epoch origin so the value is relative to the chunk the estimator is folding, keeping every satellite on one consistent time grid. The estimator itself does not read sample_index (the loop filters key off integrated_samples); it is preserved for downstream vector/Kalman tracking.\n\n\n\n\n\n","title":"Tracking.CorrelatorOutput"},{"category":"function","location":"correlator.html#Tracking.get_correlator_outputs","page":"Correlator","text":"get_correlator_outputs(t)\n\n\nThe CorrelatorOutputs this signal completed during the most recent processing chunk, in order. Populated by the correlate phase and consumed + cleared by the Doppler estimator after each chunk, so it is empty between track! calls; read it inside a custom estimator, or right after a bare downconvert_and_correlate!. See Chunked Doppler updates.\n\n\n\n\n\n","title":"Tracking.get_correlator_outputs"},{"category":"function","location":"correlator.html#Tracking.append_correlator_output!","page":"Correlator","text":"append_correlator_output!(t, output)\n\n\nAppend an externally built CorrelatorOutput to signal's per-chunk correlator_outputs buffer and return signal.\n\nThis is the blessed ingest path for an external correlator producer (e.g. an FPGA streaming correlator dumps): build a CorrelatorOutput from the producer's raw accumulator, sample count and chunk-relative end index, append it here per signal in sample_index order, then run estimate_dopplers_and_filter_prompt! with a per-band sampling-frequency source to fold the batch and update the NCOs — no sample buffer or downconvert_and_correlate! needed. The estimator consumes and clears the buffer as part of that call, so between chunks it is empty again (same contract as the software correlate phase). Prefer this over mutating the vector returned by get_correlator_outputs directly — it documents intent and is type-checked (the output's correlator type must match the signal's).\n\nSee External correlator producers for the full offload contract.\n\n\n\n\n\nappend_correlator_output!(s, output, id)\n\n\nAppend an externally built CorrelatorOutput to the buffer of the addressed signal and return track_state. The output is followed by the same satellite/signal addressing forms as the per-signal accessors (e.g. get_correlator_outputs):\n\nappend_correlator_output!(track_state, output) — one group, one sat, one signal.\nappend_correlator_output!(track_state, output, prn) — one group, one signal.\nappend_correlator_output!(track_state, output, group, prn) — multi-group, one signal.\nappend_correlator_output!(track_state, output, group, prn, sig) — per-signal.\n\nThe signal-level method (append_correlator_output!(::TrackedSignal, output)) carries the contract; see it and External correlator producers.\n\n\n\n\n\n","title":"Tracking.append_correlator_output!"},{"category":"function","location":"correlator.html#Tracking.get_correlator_sample_shifts","page":"Correlator","text":"get_correlator_sample_shifts(\n    correlator,\n    sampling_frequency,\n    code_frequency\n)\n\n\nCalculate the replica phase offset required for the correlator with respect to the prompt correlator, expressed in samples. The shifts are ordered from latest to earliest replica.\n\n\n\n\n\nget_correlator_sample_shifts(\n    correlator,\n    sampling_frequency,\n    code_frequency\n)\n\n\nCalculate the replica phase offset required for the correlator with respect to the prompt correlator, expressed in samples. The shifts are ordered from latest to earliest replica.\n\n\n\n\n\n","title":"Tracking.get_correlator_sample_shifts"},{"category":"function","location":"correlator.html#Tracking.get_early_late_sample_spacing","page":"Correlator","text":"get_early_late_sample_spacing(\n    correlator,\n    sampling_frequency,\n    code_frequency\n)\n\n\nCalculate the total spacing between early and late correlator in samples.\n\n\n\n\n\n","title":"Tracking.get_early_late_sample_spacing"},{"category":"method","location":"correlator.html#Tracking.update_accumulator-Tuple{EarlyPromptLateCorrelator, Any}","page":"Correlator","text":"update_accumulator(correlator, accumulators)\n\n\nUpdate the correlator with new accumulators\n\n\n\n\n\n","title":"Tracking.update_accumulator"},{"category":"method","location":"correlator.html#Tracking.update_accumulator-Tuple{VeryEarlyPromptLateCorrelator, Any}","page":"Correlator","text":"update_accumulator(correlator, accumulators)\n\n\nUpdate the correlator with new accumulators\n\n\n\n\n\n","title":"Tracking.update_accumulator"},{"category":"type","location":"correlator.html#Tracking.NumAnts","page":"Correlator","text":"Type parameter wrapper for specifying the number of antennas in the system. Use NumAnts(n) to create an instance.\n\n\n\n\n\n","title":"Tracking.NumAnts"},{"category":"type","location":"correlator.html#Tracking.NumAccumulators","page":"Correlator","text":"Type parameter wrapper for specifying the number of correlator accumulators. Use NumAccumulators(n) to create an instance.\n\n\n\n\n\n","title":"Tracking.NumAccumulators"},{"category":"type","location":"correlator.html#Tracking.AbstractPostCorrFilter","page":"Correlator","text":"Abstract post correlation filter for the prompt signal.\n\n\n\n\n\n","title":"Tracking.AbstractPostCorrFilter"},{"category":"type","location":"correlator.html#Tracking.DefaultPostCorrFilter","page":"Correlator","text":"This is the default post correlation filter. For a single antenna channel it will just return the prompt value as is. For multi antenna systems it will return the first antenna's value.\n\n\n\n\n\n","title":"Tracking.DefaultPostCorrFilter"},{"category":"function","location":"correlator.html#Tracking.calc_signal_samples_to_integrate","page":"Correlator","text":"calc_signal_samples_to_integrate(\n    signal,\n    signal_start_sample,\n    sampling_frequency,\n    code_doppler,\n    code_phase,\n    preferred_num_code_blocks_to_integrate,\n    secondary_code_or_bit_found,\n    num_samples_signal\n)\n\n\nCalculate the number of signal samples to integrate and whether the integration will complete a full code period. Returns a tuple of (samplestointegrate, isintegrationcompleted). Takes into account available signal samples and code block boundaries.\n\n\n\n\n\n","title":"Tracking.calc_signal_samples_to_integrate"},{"category":"section","location":"bit_sync.html#Bit-and-Secondary-Code-Sync","page":"Bit and Secondary-Code Sync","text":"Once a satellite is being tracked, the next milestone is locking onto its bit/symbol boundary (and, for pilots like GPS L1C-P, the secondary-code phase). These are what let the receiver:\n\nSwitch from 1-ms coherent integrations to full-symbol coherent integrations (e.g. 20 ms for L1 C/A), driving the PLL phase-noise floor down.\nHand over a decoded bit stream to a navigation-message parser.\nReconstruct the absolute code phase modulo the longest secondary-code cycle, so downstream consumers (e.g. PositionVelocityTime.jl) can pseudorange.\n\nThis page is a deep dive into the mechanism. The high-level user only needs to know: call has_bit_or_secondary_code_been_found per signal (see the per-signal accessor table) and get_soft_bits once it returns true. The rest of the machinery is internal.","title":"Bit and Secondary-Code Sync"},{"category":"section","location":"bit_sync.html#Code-phase-wrap-period","page":"Bit and Secondary-Code Sync","text":"The shared TrackedSat.code_phase wraps at two distinct values depending on the per-signal sync state:\n\nBefore any signal has synced — wrap is the largest primary code length across the sat's signals. For a sat tracking only L1 C/A this is 1023 chips. The wrap is intentionally narrow at this stage because we don't yet know which data bit or secondary-code chip the current primary period belongs to.\nAfter a signal syncs — its contribution to the wrap widens to one full symbol period: primary × secondary_code_length for a pilot, or primary × blocks_per_data_bit for a data-bearing signal. The shared wrap is then the max across signals, so the longest synced signal pins it.\n\nConcrete values:\n\nSat tracks Before sync After full sync\nGPS L1 C/A only 1023 1023 × 20 = 20460 (one 50 Hz data bit)\nGPS L5I only 10230 10230 × 10 = 102300 (one NH10 cycle / one data bit)\nGPS L1C-P only 10230 10230 × 1800 ≈ 18.4 M (one overlay-code cycle, ≈ 18 s)\nL1C-P + L1C-D + L1CA 10230 (longest primary) 18.4 M (L1C-P dominates)\n\nThe post-sync widening is what lets downstream consumers (e.g. PositionVelocityTime.jl) distinguish which primary-code period within the symbol they're currently in — mod(code_phase, primary) gives the per-signal replica phase, while div(code_phase, primary) gives the symbol-internal position.\n\nmax_code_length returns the upper bound (the post-full-sync value) at compile time. current_code_wrap returns the runtime value honoring the current per-signal sync state — this is what the inner loop's mod actually uses.","title":"Code-phase wrap period"},{"category":"section","location":"bit_sync.html#Bit-sync-and-secondary-code-sync-detection","page":"Bit and Secondary-Code Sync","text":"Each per-signal BitBuffer runs an detect_bit_or_secondary_code_sync detector against the running buffer of primary-code-block signs. The detector returns a SyncResult containing whether sync was found, the secondary-code phase (chip offset within the secondary code, used for code-phase seeding — see below), and the locked polarity (±1).\n\nThere are three detector families:\n\nSoft, maximum-energy CFAR — GPS L1 C/A (bit-edge, _detect_bit_edge_cfar) and the short-secondary-code signals GPS L5I/L5Q and Galileo E1C/E5a-I/E5a-Q (overlay-rotation, _detect_secondary_code_cfar). These accumulate per-hypothesis, coherently-summed bin energy in PhaseAccumulators and lock when the peak hypothesis beats its runner-up with a Student-t confidence (get_bit_edge_detection_confidence, default 0.999). They self-pace with C/N₀ and fire only at the winning hypothesis's own boundary — so they report phase = 0 (the upcoming integration starts a fresh data bit / secondary-code period). Selected by uses_soft_bit_edge_detection / uses_soft_secondary_code_detection.\nHard-decision rotation/Hamming sweep (_secondary_code_search) — among the currently implemented signals, GPS L1C-P alone. It matches packed prompt signs against the known overlay at every rotation, accepting the best within get_bit_edge_or_secondary_code_tolerance; it locks in one full period worst case (matches at any alignment) and reports the recovered secondary-chip phase.\nTrivial / none — Galileo E1B, GPS L1C-D and GPS L2CM broadcast one channel symbol per primary period, so their detector returns SyncResult(true, 0, +1) immediately; GPS L2CL is a dataless pilot with no sync.\n\nMin-to-fire is the smallest num_code_blocks the detector accepts before it can lock. The soft CFAR detectors need 2 × period blocks before a runner-up (and hence a decision) exists, then fire at the next winning-hypothesis boundary; the hard sweep needs 1 × period. Buffer width is get_code_block_buffer_type(signal); note it sizes the packed prompt-sign buffer that only the hard sweep consults — the soft detectors read PhaseAccumulators instead, so for them the packed buffer is vestigial.\n\nSignal Detector Min-to-fire Buffer width Accept rule Phase Blocks per symbol\nGPS L1 C/A soft bit-edge CFAR 40 blocks (2 × 20) UInt64 (vestigial) confidence 0.999 0 20\nGalileo E1B trivial n/a UInt8 (unused) always 0 1\nGPS L5I soft secondary CFAR 20 blocks (2 × NH10) UInt32 (vestigial) confidence 0.999 0 10\nGPS L5Q soft secondary CFAR 40 blocks (2 × NH20) UInt32 (vestigial) confidence 0.999 0 20 (pilot)\nGPS L1C-D trivial n/a UInt8 (unused) always 0 1\nGPS L1C-P hard rotation sweep 1800 blocks UInt1800 (exact width) 45 errors (2.5 %) 0..1799 n/a (pilot)\nGPS L2CM trivial n/a UInt8 (unused) always 0 1\nGPS L2CL never fires UInt8 (unused) none (dataless pilot) n/a 0 n/a (pilot)\nGalileo E1C soft secondary CFAR 50 blocks (2 × CS25) UInt32 (vestigial) confidence 0.999 0 25 (pilot)\nGalileo E5a-I soft secondary CFAR 40 blocks (2 × CS20) UInt32 (vestigial) confidence 0.999 0 20\nGalileo E5a-Q soft secondary CFAR 200 blocks (2 × CS100) UInt128 (vestigial) confidence 0.999 0 100 (pilot)\n\nThe buffer-width type threads through BitBuffer{B} and TrackedSignal{Sig, B, C, PCF, CN0} as a type parameter. The L1C-P case uses an exact-width UInt1800 defined via BitIntegers.@define_integers 1800. The soft secondary CFAR detectors take the ±1 overlay chip directly from get_secondary_code(signal) per rotation, so a new short-secondary-code signal needs no bespoke template — only for uses_soft_secondary_code_detection to return true (secondary length 1 < N ≤ 100).\n\nThe soft CFAR confidence is package-wide and adjustable per signal via get_bit_edge_detection_confidence; the hard-sweep Hamming tolerance is adjustable per (hard-path) signal via get_bit_edge_or_secondary_code_tolerance:\n\njulia> using Tracking, GNSSSignals\n\njulia> Tracking.get_bit_edge_or_secondary_code_tolerance(GPSL1C_P())  # default\n0.025\n\njulia> # Loosen the L1C-P ceiling to 5 % (= 90 errors over 1800 blocks) for low-C/N₀ work.\n       Tracking.get_bit_edge_or_secondary_code_tolerance(::GPSL1C_P) = 0.05;\n\njulia> Tracking.get_bit_edge_or_secondary_code_tolerance(GPSL1C_P())  # after override\n0.05\n\nThe hard sweep reads the tolerance at its call site and converts to an integer error budget via floor(Int, tolerance × window_size), so the override picks up the next time detect_bit_or_secondary_code_sync runs — no TrackState rebuild needed. It has no effect on the soft-detector signals (tune their confidence instead); the trivial detectors ignore both.","title":"Bit-sync and secondary-code-sync detection"},{"category":"section","location":"bit_sync.html#Lifecycle-of-a-BitBuffer","page":"Bit and Secondary-Code Sync","text":"Two distinct phases, separated by the found::Bool flag:\n\nPre-sync search (found = false). Each completed integration folds the prompt into the running state and the detector is called; while it returns SyncResult(false, ...) the loop keeps integrating one primary code period at a time. The soft CFAR detectors (GPS L1 C/A bit-edge; GPS L5I/L5Q and Galileo E1C/E5a-I/E5a-Q overlay) fold each prompt into PhaseAccumulators and lock only at the winning hypothesis's own boundary — so the call that flips found lands exactly on a data-bit / secondary-code-period boundary and reports phase = 0 (the upcoming integration starts a fresh period). The hard rotation-sweep detector (GPS L1C-P) instead shifts each prompt sign into code_block_buffer::B and locks as soon as one full overlay period has been buffered — at any alignment — reporting the recovered phase (the upcoming integration's secondary chip); the integration cadence then re-aligns to that boundary (see below).\nPost-sync accumulation (found = true). The post-sync branch in Tracking.buffer ignores code_block_buffer and instead accumulates the complex prompt into prompt_accumulator. (Pilot signals such as GPS L1C-P carry no data bits, so for them this branch is a no-op — the buffer just retains found / secondary_phase / polarity.) Because each post-sync integration spans to the next secondary-code (data-bit) boundary, bit decoding stays aligned even when sync fired mid-period. Each integration also bumps prompt_accumulator_integrated_code_blocks. Once that counter reaches the per-signal \"blocks per symbol\" value above — _calc_num_code_blocks_that_form_a_bit(signal) = get_code_frequency(signal) / (get_code_length(signal) * get_data_frequency(signal)) — one decoded bit is committed as a soft bit — the polarity-corrected coherent prompt sum, sign = hard decision, magnitude = confidence — pushed onto the unbounded soft_bits vector (a hard decision is just the sign), and the accumulator resets to zero. For Galileo E1B and GPS L1C-D the counter is 1, so one symbol commits per integration; for GPS L1 C/A it's 20, so the loop counts 20 primary-code periods (≈ 20 × 1023 chips = 20460 chips of code_phase advance, modulo wrap) per data bit; for GPS L5I it's 10. The polarity flag flips the accumulator's sign at commit time when the detector locked at negative polarity, so downstream consumers always see 1 = data symbol 0.\n\nPilot signals (get_data_frequency = 0 Hz, e.g. GPS L1C-P) never enter the post-sync accumulation branch — their bit_buffer carries the recovered secondary-code phase but no decoded bits, and the post-sync work is purely the code-phase seeding described next.","title":"Lifecycle of a BitBuffer"},{"category":"section","location":"bit_sync.html#Code-phase-seeding-from-the-secondary-code-phase","page":"Bit and Secondary-Code Sync","text":"When a signal with a secondary code (secondary_code_length > 1) syncs — GPS L5I/L5Q, Galileo E1C/E5a-I/E5a-Q, or GPS L1C-P — its secondary_phase seeds TrackedSat.code_phase so subsequent wrap-mod-current_code_wrap arithmetic gives the absolute position in the longest secondary-code cycle. The seeding follows a fallback chain: the synced signal with the largest (primary × secondary) code length wins. For the soft-CFAR signals secondary_phase is always 0 (they fire on a period boundary, so the upcoming integration is at chip 0); for the hard-sweep L1C-P it is the recovered chip offset. Either way it is a multiple-of-primary snap into the correct secondary window.\n\nSignals with secondary_code_length == 1 (bit-edge only, e.g. GPS L1 C/A) do not carry an explicit secondary_phase to snap — there's no per-PRN overlay to recover a chip offset from. Instead, their post-sync bit-edge alignment is captured by two complementary mechanisms:\n\nThe BitBuffer.prompt_accumulator_integrated_code_blocks counter tracks \"how many of the next N primary periods have I integrated since the last bit commit.\" reset(bit_buffer) preserves it, so the bit cadence survives intra-call resets without re-syncing.\nThe wrap returned by current_code_wrap widens from primary to primary × blocks_per_data_bit (e.g. 1023 → 20460 for L1 C/A) the moment bit_buffer.found flips to true. From that point on mod(code_phase, primary) continues to give the replica phase, while div(code_phase, primary) reads off which primary period within the data bit we're in. The transition is one-shot: on the call that flips found, code_phase is implicitly the start of a fresh data bit because the detector only matches at a true bit boundary (the [0, primary) range of code_phase then represents primary period 0 of the new bit), so no explicit snap is needed.","title":"Code-phase seeding from the secondary-code phase"},{"category":"function","location":"bit_sync.html#Tracking.max_code_length","page":"Bit and Secondary-Code Sync","text":"max_code_length(signals)\n\n\nUpper bound on the shared sat.code_phase wrap period, in chips — the least common multiple of the per-signal wrap periods once every signal on the sat has synced. For a sat tracking only GPS L1 C/A this is 1023 × 20 = 20460 (one full data bit); for one tracking L1C-P this is 10230 × 1800 ≈ 18.4 M (one full secondary-code cycle). For every shipped multi-signal pairing the shorter wraps divide the longer ones, so the lcm coincides with the longest signal's wrap.\n\nThis is the compile-time bound. The actual runtime wrap shrinks to get_code_length(signal) × 1 for any signal whose bit/secondary-code sync hasn't been found yet — see current_code_wrap for the runtime value used by the inner loop.\n\nImplemented via tuple recursion (not @generated) so the heterogeneous walk unrolls at type-inference time; the result folds to a literal in the calling site for any concrete signals tuple type.\n\n\n\n\n\n","title":"Tracking.max_code_length"},{"category":"function","location":"bit_sync.html#Tracking.current_code_wrap","page":"Bit and Secondary-Code Sync","text":"current_code_wrap(signals)\n\n\nThe runtime wrap period for the shared sat.code_phase, in chips — the value the inner loop uses to wrap code_phase modulo each integration step.\n\nUnlike max_code_length (which is the worst-case bound), this honors the current per-signal sync state. For each signal:\n\nIf bit_buffer.found = true, the signal contributes primary × max(secondary_code_length, blocks_per_data_bit) — i.e. the full secondary-code period for pilots, or the full data-bit period for data-bearing signals.\nIf bit_buffer.found = false, the signal contributes just its primary code length — we don't yet know which bit / secondary chip we're in, so wrapping at the primary length is the most we can legitimately do.\n\nThe shared wrap is the least common multiple of the per-signal contributions, so it stays an integer multiple of every signal's own replica wrap — per-signal phases are re-derived as mod(code_phase, replica_wrap), which a non-common-multiple wrap would silently corrupt (issue #129). For all shipped signal pairings the shorter wraps divide the longer ones, so the lcm coincides with the longest synced signal's wrap and shorter signals just ride along.\n\n\n\n\n\n","title":"Tracking.current_code_wrap"},{"category":"section","location":"loop_filter.html#Loop-Filter","page":"Loop Filter","text":"The loop filters are provided by TrackingLoopFilters.jl. This includes:\n\nfirst order loop filter FirstOrderLF\nsecond order bilinear loop filter SecondOrderBilinearLF\nsecond order boxcar loop filter SecondOrderBoxcarLF\nthird order bilinear loop filter ThirdOrderBilinearLF\nthird order boxcar loop filter ThirdOrderBoxcarLF\nthird order assisted bilinear loop filter ThirdOrderAssistedBilinearLF (combines PLL and FLL)","title":"Loop Filter"},{"category":"section","location":"loop_filter.html#Default-Configuration","page":"Loop Filter","text":"The default Doppler estimator is ConventionalAssistedPLLAndDLL which uses:\n\nThirdOrderAssistedBilinearLF for the carrier loop (FLL-assisted PLL for improved dynamics)\nSecondOrderBilinearLF for the code loop\n\nWhen TrackState builds the default estimator implicitly from a signal-tuple declaration, the carrier bandwidth is sized per signal from the signal's primary code period T, at BL · T ≈ 0.018 — ~10× margin from the BL · T < 0.18 stability edge of the bilinear third-order filter. The code bandwidth is a flat 1 Hz for every signal. The values fall out to:\n\nSignal Primary period Carrier BL Code BL\nGPS L1 C/A 1 ms 18 Hz 1 Hz\nGPS L5I 1 ms 18 Hz 1 Hz\nGalileo E1B 4 ms 4.5 Hz 1 Hz\nGPS L1C-D 10 ms 1.8 Hz 1 Hz\nGPS L1C-P 10 ms 1.8 Hz 1 Hz\nGPS L2 CM 20 ms 0.9 Hz 1 Hz\nGPS L2 CL 1.5 s 0.012 Hz 1 Hz\n\nThe 1-ms-primary-period signals (L1 C/A, L5I) keep the historical 18 Hz / 1 Hz default; longer-period signals get appropriately tighter carrier loops so the PLL stays stable. The DLL does not follow the carrier loop down: being carrier-aided it has almost no dynamic stress to track, so its bandwidth is a thermal-noise-versus-pull-in choice that does not scale with the symbol rate.\n\nThe two are also treated differently at filter time, since only the carrier bandwidth is a per-code-period reference. Integrating N primary blocks coherently scales the carrier bandwidth to BL/N, holding its BL · Δt product at the single-period value, while the code bandwidth is left alone and merely capped by the same product against the record's actual integration time (Tracking.effective_code_loop_filter_bandwidth). That cap is what pulls the L2 C primaries down in practice — 0.9 Hz for a 20 ms L2 CM integration, 0.012 Hz for a 1.5 s L2 CL one — while every integration shorter than 18 ms, whatever its signal or block count, runs the DLL at the full 1 Hz.\n\nReusing the third-order carrier filter's 0.018 product to cap the second-order code filter is conservative: transform-designed digital loops of this kind only destabilize around BL · Δt ≈ 0.4 (S. A. Stephens and J. B. Thomas, \"Controlled-Root Formulation for Digital Phase-Locked Loops\", IEEE Trans. Aerospace and Electronic Systems 31(1), 1995 — the standard treatment of digital-loop stability at large BL · Δt), so the code loop's cap carries even more stability margin than the carrier loop's.\n\nOverride per signal by defining methods of default_carrier_loop_filter_bandwidth / default_code_loop_filter_bandwidth, or override at construction time by passing your own doppler_estimator = to TrackState.","title":"Default Configuration"},{"category":"section","location":"loop_filter.html#Doppler-Estimators","page":"Loop Filter","text":"","title":"Doppler Estimators"},{"category":"section","location":"loop_filter.html#Resetting-loop-filters","page":"Loop Filter","text":"When you change a signal's coherent-integration length mid-track with set_preferred_num_code_blocks_to_integrate!, reset the affected loop filters for a clean handoff so the previous integration length's filter state does not leak into the new one.","title":"Resetting loop filters"},{"category":"section","location":"loop_filter.html#Custom-Configuration","page":"Loop Filter","text":"You can customize the loop filters and bandwidths when creating the Doppler estimator:\n\njulia> using Tracking, GNSSSignals, TrackingLoopFilters\n\njulia> using Tracking: Hz\n\njulia> # Use non-assisted PLL with custom loop filter types\n       doppler_estimator = ConventionalPLLAndDLL(\n           ThirdOrderBilinearLF,      # carrier loop filter type\n           SecondOrderBilinearLF;     # code loop filter type\n           carrier_loop_filter_bandwidth = 15.0Hz,\n           code_loop_filter_bandwidth = 0.5Hz\n       );\n\njulia> track_state = TrackState(; signal = GPSL1CA(), doppler_estimator);\n\njulia> track_state = add_satellite!(track_state; prn = 1, code_phase = 50.0, carrier_doppler = 1000.0Hz);","title":"Custom Configuration"},{"category":"section","location":"loop_filter.html#Custom-Loop-Filters","page":"Loop Filter","text":"You can implement a custom loop filter MyLoopFilter <: AbstractLoopFilter. In this case, a specialized filter_loop function is needed. For more information refer to TrackingLoopFilters.jl.","title":"Custom Loop Filters"},{"category":"section","location":"loop_filter.html#Custom-Doppler-Estimator","page":"Loop Filter","text":"To replace the loop-filter-based estimator with a different algorithm (Kalman filter, joint-channel estimator, …), see the dedicated guide in custom_doppler_estimator.md.","title":"Custom Doppler Estimator"},{"category":"type","location":"loop_filter.html#Tracking.ConventionalPLLAndDLL","page":"Loop Filter","text":"Conventional Phase-Locked Loop (PLL) and Delay-Locked Loop (DLL) Doppler estimator. Configuration-only — per-satellite state lives in each TrackedSat wrapper, produced via init_estimator_state.\n\nType parameters CA and CO select the carrier and code loop filter types; the bandwidth fields configure the loop bandwidths used when seeding new satellites. Each bandwidth field is Maybe{typeof(1.0Hz)}: a nothing field (the default) means auto — init_estimator_state sizes the bandwidth per satellite from that sat's estimator-driver signal (signals[1]) via default_carrier_loop_filter_bandwidth / default_code_loop_filter_bandwidth: the carrier loop is sized for the signal's own integration period (18 Hz for GPS L1 C/A, 4.5 Hz for Galileo E1B, 1.8 Hz for L1C-D / L1C-P, …), the code loop takes a flat 1 Hz. Pass an explicit bandwidth to override the auto-sizing for every satellite this estimator seeds.\n\nThe two bandwidths are referenced differently, because only the carrier loop's tuning tracks the update rate. The carrier bandwidth is referenced to a one-primary-code-period integration: when a signal coherently integrates N primary blocks (its per-TrackedSignal preferred_num_code_blocks_to_integrate, set via set_preferred_num_code_blocks_to_integrate!), it is automatically scaled to BL/N at filter time so the loop's BL·Δt stability product stays at its single-period value. This keeps the loop stable across integration lengths without the caller re-tuning the bandwidth — e.g. a 1 ms→10 ms switch needs no bandwidth change. The code bandwidth is an absolute value that longer integration does not narrow; it is only capped by the same stability product against the record's actual integration time — see effective_code_loop_filter_bandwidth.\n\n\n\n\n\n","title":"Tracking.ConventionalPLLAndDLL"},{"category":"function","location":"loop_filter.html#Tracking.ConventionalAssistedPLLAndDLL","page":"Loop Filter","text":"ConventionalAssistedPLLAndDLL(; ...)\nConventionalAssistedPLLAndDLL(\n    ;\n    carrier_loop_filter_bandwidth,\n    code_loop_filter_bandwidth\n)\n\n\nCreate a ConventionalPLLAndDLL with FLL-assisted carrier tracking. This is the default Doppler estimator used by TrackState. Uses a ThirdOrderAssistedBilinearLF for the carrier loop filter which combines PLL and FLL discriminators for improved tracking under high dynamics.\n\nBandwidths default to nothing (auto): each satellite is seeded with the loop bandwidth recommended for its own estimator-driver signal — see ConventionalPLLAndDLL. Pass explicit bandwidths to override.\n\n\n\n\n\n","title":"Tracking.ConventionalAssistedPLLAndDLL"},{"category":"function","location":"loop_filter.html#Tracking.default_carrier_loop_filter_bandwidth","page":"Loop Filter","text":"default_carrier_loop_filter_bandwidth(signal)\n\n\nRecommended carrier-loop-filter bandwidth for signal's primary integration period. Sized so that the PLL time-bandwidth product BL * T lands at about 0.018 (≈10× margin from the 0.18 stability edge of the bilinear third-order filter). Used by TrackState(; signal=…) when the user doesn't pass an explicit doppler_estimator.\n\nOverride by defining a method for your signal type, or by constructing ConventionalAssistedPLLAndDLL yourself with explicit carrier_loop_filter_bandwidth = / code_loop_filter_bandwidth = kwargs.\n\nT = get_code_length(signal) / get_code_frequency(signal)   # primary period\nBL = 0.018 / T                                              # this default\n\nT here is the primary-code period, not the chosen coherent integration length. For GPS L1 C/A (T = 1 ms) and GPS L5I (T = 1 ms, a 10230-chip code at 10.23 MHz) this returns 18 Hz — matching the historical hand-picked default. For L1C-D / L1C-P (T = 10 ms) it returns 1.8 Hz, and for Galileo E1B (T = 4 ms) 4.5 Hz — the well-inside-stability values the multi-signal flagship use case needs.\n\nThis value is the reference bandwidth for a one-primary-code-period integration; it is not the bandwidth that ends up in the loop when you integrate longer. Coherently integrating N primary blocks grows the loop update interval to N·T, which would push BL·N·T toward the ~0.18 stability edge of the bilinear filter. To avoid that, the conventional estimator automatically scales the effective loop bandwidth by 1/N at filter time (see ConventionalPLLAndDLL), holding the BL·Δt stability product fixed at its single-period value. So you set this reference bandwidth once and the loop stays stable at any integration length — no manual 1/N adjustment is needed.\n\n\n\n\n\n","title":"Tracking.default_carrier_loop_filter_bandwidth"},{"category":"function","location":"loop_filter.html#Tracking.default_code_loop_filter_bandwidth","page":"Loop Filter","text":"default_code_loop_filter_bandwidth(signal)\n\n\nRecommended code-loop-filter (DLL) bandwidth for signal: a flat 1 Hz for every signal.\n\nA carrier-aided DLL has almost no dynamic stress to track — the code Doppler is handed to it by the PLL (see aid_dopplers) — so its bandwidth is a thermal-noise-versus-pull-in trade that scales with neither the symbol rate (the old 18:1 carrier:code ratio starved the long-primary signals' pull-in) nor the coherent integration length. 1 Hz sits inside the 0.25–2 Hz the reference software receivers (GNSS-SDR, SoftGNSS, PocketSDR) use across signals.\n\nUnlike the carrier bandwidth this is an absolute value, not a per-primary-code-period reference. Only the loop's own BL · Δt stability product caps it, at filter time, against each record's actual integration time — see effective_code_loop_filter_bandwidth; the cap binds only past 18 ms (0.9 Hz for a 20 ms L2 CM integration, 0.012 Hz for a 1.5 s L2 CL one).\n\nOverride by defining a method for your signal type.\n\n\n\n\n\n","title":"Tracking.default_code_loop_filter_bandwidth"},{"category":"function","location":"loop_filter.html#Tracking.effective_code_loop_filter_bandwidth","page":"Loop Filter","text":"effective_code_loop_filter_bandwidth(\n    bandwidth,\n    integration_time\n)\n\n\nEffective code-loop bandwidth for a record that integrated for integration_time: the configured bandwidth, capped so the code loop's BL · Δt product stays inside MAX_LOOP_BANDWIDTH_TIME_PRODUCT.\n\nThe carrier loop takes a 1/N scaling instead, because its configured bandwidth is a per-primary-code-period reference — see ConventionalPLLAndDLL. The DLL's is an absolute value: carrier-aided, it has no dynamic stress that grows with the integration length, and neither its pull-in time nor its thermal-noise floor does either, so integrating longer must not narrow it. Only stability may, and stability depends on the update interval the record actually had — hence the cap against integration_time rather than a scaling by the block count. A 1/N here would take a 20 ms L1 C/A integration down to 0.05 Hz where stability allows 0.9 Hz, re-introducing through the integration length exactly the pull-in sag that sizing the DLL off the carrier default used to cause by signal.\n\nFor a single-block integration of any signal at or below the 18 ms period where the cap starts to bind, this returns the configured bandwidth unchanged.\n\n\n\n\n\n","title":"Tracking.effective_code_loop_filter_bandwidth"},{"category":"function","location":"loop_filter.html#Tracking.reset_loop_filters!","page":"Loop Filter","text":"reset_loop_filters!(track_state)\n\n\nRe-seed the Doppler-estimator state of every satellite (or one addressed satellite) from its current Doppler, giving each a freshly initialized loop filter. For the conventional PLL/DLL estimator this zeroes the carrier and code loop-filter integrators while preserving the converged carrier_doppler / code_doppler — and any per-satellite loop-bandwidth override carried on the SatConventionalPLLAndDLL state — so the loop continues from the converged frequency with a clean filter. Each signal's last_fully_integrated_filtered_prompt is cleared as well, so the first FLL update after the reset doesn't measure a prompt rotation that spans the old integration interval.\n\nThis is the recommended handoff when a signal's coherent-integration length changes mid-track — e.g. promoting GPS L5I from 1 ms to 10 ms via set_preferred_num_code_blocks_to_integrate!. The bilinear loop filter's integrator state is not portable across the change in update interval (Δt grows by the integration factor), so resetting it avoids a transient that can drag the loop out of lock; the converged Doppler is the right seed for the new, longer integration.\n\nFor VectorPLLAndDLL the re-seed additionally zeroes the externally-supplied NCO corrections (code_freq_update / carrier_freq_update) and the discriminator accumulators — the converged Doppler already folds in the last correction, so keeping it would apply it twice. A navigation filter must therefore re-issue its corrections via set_code_freq_updates! / set_carrier_freq_updates! after resetting a vector-loop satellite. The vt_on flag and any per-satellite bandwidth override are preserved.\n\nAddressed like the per-signal accessors — no satellite id resets every satellite in track_state; (group, prn) or (single-group) prn resets one. Mutates track_state in place and returns it. Works for any AbstractDopplerEstimator through its init_estimator_state hook.\n\nset_preferred_num_code_blocks_to_integrate!(track_state, 1, GPSL5I, 10)\nreset_loop_filters!(track_state, 1)          # clean handoff for PRN 1\nreset_loop_filters!(track_state)             # …or reset every satellite\n\n\n\n\n\n","title":"Tracking.reset_loop_filters!"},{"category":"section","location":"track.html#Track","page":"Track","text":"track is the main entry point: it takes an incoming measurement and a TrackState, runs downconversion + correlation for every tracked satellite, drives the Doppler estimator, and returns the updated TrackState. track! is the in-place counterpart for hard real-time loops where GC pauses must be avoided — after one warmup call (to seat the filtered_prompts buffer's capacity), the single-threaded track! path is fully allocation-free; the threaded path keeps a small irreducible per-call residual from Polyester's @batch closure capture (160 B per GNSS system).","title":"Track"},{"category":"section","location":"track.html#Optional-parameters","page":"Track","text":"downconvert_and_correlator — the downconversion and correlation implementation. Defaults to CPUThreadedDownconvertAndCorrelator(). For real-time loops, hoist this outside the loop (see below).\nintermediate_frequency — the IF of the signal. Defaults to 0.0Hz. Only accepted on the bare-buffer form track!(buf, state, fs; intermediate_frequency = ...); on the BandMeasurement and multi-band forms the IF lives on each BandMeasurement.\ndoppler_update_interval — the Doppler-estimation / NCO-update interval, a time (e.g. 1u\"ms\"). Defaults to nothing ⇒ auto = the smallest primary-code period across all tracked signals (1 ms for GPS L1 C/A). Each measurement is processed in fixed-size chunks of this length: within a chunk the NCO Doppler is held fixed and every correlator output that completes is collected, then the estimator processes them in order and updates every satellite's NCO once, at a common epoch (see Chunked Doppler updates). Pick a longer interval to reduce Doppler-estimation cost at the expense of update rate.\n\nThe coherent-integration length is not a track! argument — it is a per-signal setting on each TrackedSignal (its preferred_num_code_blocks_to_integrate field), changed with set_preferred_num_code_blocks_to_integrate!. It defaults to 1, is capped per integration by the signal's bit/secondary-code period, and only takes effect once bit/secondary-code synchronization has been achieved. For data-bearing signals the length must evenly divide the number of code blocks that form one bit (e.g. a divisor of 20 for GPS L1 C/A, of 10 for GPS L5I) so integrations stay aligned to bit boundaries; other values throw an ArgumentError. With the conventional estimator the carrier loop bandwidth auto-scales by 1/N so longer integration stays stable without re-tuning; the code loop keeps its absolute bandwidth and is capped only where the longer update interval would otherwise threaten stability.","title":"Optional parameters"},{"category":"section","location":"track.html#Chunked-Doppler-updates","page":"Track","text":"track / track! walk each measurement in fixed-size time chunks of length doppler_update_interval (default: the smallest code period across all signals). Each chunk runs one correlate pass and one estimate:\n\nCorrelate to the last completed boundary — each satellite integrates from wherever it stands up to its last coherent-integration boundary inside the chunk; every completed integration is collected into that signal's correlator_outputs buffer, tagged with the sample index at which it ended (a CorrelatorOutput; the sample index is important for vector tracking). A 1 ms-code signal in a 1 ms chunk yields 0, 1, or 2 outputs; a signal whose coherent integration is longer than the chunk yields outputs only on the chunks where it completes.\nEstimate — the Doppler estimator processes the collected outputs in order (threading the loop-filter state across them) and writes the resulting Doppler to the NCO once per chunk — all satellites' NCOs update at the same point in the processing, a common epoch.\n\nThe chunk's trailing partial — from each satellite's last completed boundary to the chunk end — is not integrated separately: the next chunk's pass starts right at that boundary, so each integration runs boundary → boundary in one kernel window, entirely at the freshly updated Doppler. Every completed integration is therefore produced by a single NCO Doppler and each correction takes effect right at the boundary where its integration completed — the same loop timing as a classic per-code-period update. A final pass after the last chunk drains the buffer's trailing partial into each satellite's live accumulator so it carries into the next track! call.\n\nRead the collected outputs for the most recent chunk with get_correlator_outputs (they are cleared after each chunk's estimate).\n\nChoosing a larger doppler_update_interval batches more correlator outputs per NCO update, trading Doppler-tracking bandwidth for lower estimation cost. At the default interval the behavior is numerically very close to updating the NCO once per code period.","title":"Chunked Doppler updates"},{"category":"section","location":"track.html#Real-time-loops","page":"Track","text":"A typical receiver loop builds the TrackState once, hoists the correlator outside the loop, and calls track! per chunk:\n\ntrack_state = TrackState(; signal = GPSL1CA())\ntrack_state = add_satellite!(track_state; prn = 1, code_phase = 0.0, carrier_doppler = 1000.0Hz)\n# ... more sats ...\n\ndc = CPUThreadedDownconvertAndCorrelator()  # hoist outside the loop\n\nwhile got_signal_chunk(rx)\n    chunk = read_chunk!(rx)\n    track!(chunk, track_state, sampling_frequency;\n           downconvert_and_correlator = dc)\n    # ... read per-sat state e.g. via get_sat_state(track_state, group, prn) ...\nend\n\nBoth CPUDownconvertAndCorrelator and CPUThreadedDownconvertAndCorrelator hold long-lived per-thread scratch buffers that grow on first use and are reused thereafter. Relying on the default kwarg value rebuilds them on every call, which defeats the allocation-free design. This applies to both track (immutable) and track! (in-place).\n\ntrack! writes back into the existing Vector{TrackedSat} slots of each per-group dictionary, so the tracking loop runs without GC pressure once the sat set is steady. The first track! call may grow each signal's filtered_prompts buffer via push!; from the second call onwards the capacity is settled.","title":"Real-time loops"},{"category":"section","location":"track.html#External-correlator-producers","page":"Track","text":"The correlate phase and the Doppler estimator are decoupled: the estimator consumes each signal's correlator_outputs buffer and never touches the sample buffer directly. That lets an external producer — e.g. an FPGA/hardware correlator that streams completed correlator dumps — supply the outputs and run only the loop filters on the host. The FPGA does downconversion + correlation; the host folds the dumps through the estimator and streams the resulting NCO Dopplers back. (The DMA transport, wire format and NCO marshaling live in GNSSReceiver.jl; this section is only the Tracking.jl-side contract.)\n\nThe offload loop, per processing chunk (epoch):\n\nIngest. For each satellite/signal, build a CorrelatorOutput from the producer's raw accumulator and append it with append_correlator_output! — appended per signal in sample_index order:\nappend_correlator_output!(track_state, output, group, prn, sig)\nPrefer this over mutating the vector get_correlator_outputs returns: it documents intent and type-checks the correlator against the signal's.\nEstimate. Fold the batch and update every satellite's NCO once by calling estimate_dopplers_and_filter_prompt! with a per-band sampling-frequency source instead of a sample buffer — a NamedTuple/Dict keyed by the band's get_band_id (e.g. :L1, :L5):\nestimate_dopplers_and_filter_prompt!(track_state, (L1 = 25e6Hz, L5 = 25e6Hz))\n# or: estimate_dopplers_and_filter_prompt!(track_state, Dict(:L1 => 25e6Hz))\nThis skips downconvert_and_correlate! entirely. The rate must be per band (the estimator walks groups that may be on different bands); passing the original BandMeasurements works too and reads the rate off each band's BandMeasurement, so the CPU track! path is unchanged. The estimator consumes and clears each signal's correlator_outputs as part of this call — it is empty again afterwards, ready for the next chunk. This is the public contract external producers rely on.\nMarshal the updated code_doppler/carrier_doppler (read with get_code_doppler/get_carrier_doppler) back to the hardware NCOs.","title":"External correlator producers"},{"category":"section","location":"track.html#Caller-contract","page":"Track","text":"CorrelatorOutput.correlator — the raw accumulator (sum-of-products over integrated_samples, matching what normalize expects). Reusing EarlyPromptLateCorrelator / update_accumulator on the producer side satisfies this by construction.\nCorrelatorOutput.integrated_samples — the producer's true sample count for that integration.\nCorrelatorOutput.sample_index — the chunk-relative end sample. The software path writes it buffer-relative (signal_start_sample returns to 1 each track!); a producer with a free-running global sample counter must subtract the current chunk/epoch origin so every satellite reads a consistent per-chunk time grid (the estimator itself does not read it — it is preserved for downstream vector/Kalman tracking).","title":"Caller contract"},{"category":"section","location":"track.html#Transport-delay","page":"Track","text":"The DLL discriminator uses the satellite's pre-update code_doppler as the value in effect during the integration — correct for a one-epoch feedback pipeline (the NCO written from this chunk's estimate takes effect on the next chunk). If your hardware pipeline has deeper feedback delay, that is the caller's responsibility: tag outputs with their epoch and schedule each NCO update to a known future epoch so the loop stays consistent with when the Doppler actually reaches the correlator.","title":"Transport delay"},{"category":"section","location":"track.html#BandMeasurement","page":"Track","text":"One band's incoming sample buffer plus the front-end metadata needed to process it. Bundles samples with sampling_frequency and intermediate_frequency — these are inseparable in practice, and the bundle removes the chance of mismatched parallel NamedTuples in a multi-band track call.\n\nFor single-band tracking, the bare-buffer form track!(buf, state, fs) and the single-BandMeasurement form track!(BandMeasurement(buf, fs), state) both auto-wrap into a one-entry NamedTuple internally — see the Quick start for the bare-buffer form.\n\nFor multi-band tracking, build one BandMeasurement per band and pass them as a NamedTuple keyed by the band's GNSSSignals.get_band_id (e.g. :L1, :L5):\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(;\n           signals = (legacy_gps_l1 = (GPSL1CA(),), gps_l5 = (GPSL5I(),)),\n       );\n\njulia> track_state = add_satellite!(track_state; prn = 1, group = :legacy_gps_l1, code_phase = 0.0, carrier_doppler = 0.0Hz);\n\njulia> track_state = add_satellite!(track_state; prn = 1, group = :gps_l5,        code_phase = 0.0, carrier_doppler = 0.0Hz);\n\njulia> buf_l1 = zeros(ComplexF64, 4000);   # 1 ms at  4 MHz\n\njulia> buf_l5 = zeros(ComplexF64, 25000);  # 1 ms at 25 MHz\n\njulia> track!((L1 = BandMeasurement(buf_l1, 4e6Hz),\n               L5 = BandMeasurement(buf_l5, 25e6Hz)), track_state);\n\nSee Multi-band tracking for the full setup (group declaration, per-band antenna counts, duration matching).","title":"BandMeasurement"},{"category":"section","location":"track.html#Downconversion-and-correlation","page":"Track","text":"The default CPUThreadedDownconvertAndCorrelator runs a Float32 pipeline and accepts any complex sample type. For Complex{Int16} (integer ADC) sample buffers there is an opt-in integer backend, Int16ThreadedDownconvertAndCorrelator (and its single-threaded sibling Int16DownconvertAndCorrelator), which is typically ~1.3–2.9× faster. Select it explicitly via the downconvert_and_correlator keyword; it errors on a non-Complex{Int16} measurement. Its constructor takes one required positional argument, max_meas — your front end's full-scale (the largest |real|/|imag| a sample can take, e.g. 2^11 for a 12-bit ADC) — from which it sizes the carrier replica so the integer carrier wipe cannot overflow. There is no default: see Int16DownconvertAndCorrelator for why under-declaring it is catastrophic.\n\nFor an even faster bit-wise option on Complex{Int16} captures of BPSK signals, OneBitThreadedDownconvertAndCorrelator (and its single-threaded sibling OneBitDownconvertAndCorrelator) hard-limits the measurement, carrier and code to a single sign bit, so downconversion becomes XOR and the tap accumulate becomes popcount — measured ~1.5–5.6× faster than the Float32 backend (the gap grows with sampling rate). It trades ≈2–3 dB of SNR for that speed; since the discriminators, C/N0 and bit buffer are ratio-normalised, the coarse amplitude is immaterial. Bit-wise correlation is awkward for non-binary modulations, so this backend is BPSK-only and errors on CBOC/BOC code types.\n\nBetween the two sits TwoBitThreadedDownconvertAndCorrelator (and its single-threaded sibling TwoBitDownconvertAndCorrelator): the same bit-plane XOR + popcount machinery, but with a second magnitude bit for the measurement and the carrier, making both 4-level {±1, ±3} quantities (the carrier's sign and magnitude bit planes come straight off SinCosLUT's 2-bit NCO). That recovers ≈2 dB of the one-bit backend's SNR loss (leaving only ≈0.8 dB vs Float32) at roughly Int16 speed — so pick one-bit when raw speed matters most, two-bit when you want near-Int16 sensitivity with the bit-wise memory/layout advantages, and Int16 when quantisation loss must be negligible. The threshold keyword sets the measurement's magnitude split point in ADC counts (≈1σ of the front end's input is the classic near-optimal choice; the default 512 suits a properly-AGC'd 12-bit capture). Same scope as one-bit: Complex{Int16} samples, binary (BPSK) codes only.\n\nCorrelator sample shifts and the early/late spacing are documented in Correlator.","title":"Downconversion and correlation"},{"category":"function","location":"track.html#Tracking.track","page":"Track","text":"track(measurements, track_state; kwargs...)\n\n\nMain tracking function that processes one or more BandMeasurements and updates the tracking state. Performs downconversion, correlation, and Doppler estimation for all satellites in the track state. Returns an updated TrackState with new phase/Doppler estimates and decoded bits.\n\nThree input shapes for the first positional argument:\n\nArgument Meaning\nAbstractVecOrMat Bare sample buffer. Single-band TrackState only.\nBandMeasurement One band's bundled buffer + sample rate. Single-band TrackState.\nNamedTuple{...} of BandMeasurements Multi-band: one BandMeasurement per band id (see GNSSSignals.get_band_id).\n\nThe bare-buffer form track(buf, state, fs; intermediate_frequency = ...) is preserved as a thin wrapper that builds a single-entry NamedTuple{(get_band_id(band),)} internally. The two-phase inner loop (downconvert+correlate across all groups, then estimate across the whole TrackState) is the same shape regardless of how many measurements are passed.\n\nThe returned TrackState is structurally detached from the input: each group's key set and slot vector are copied, so add_satellite! / remove_satellite! and tracking itself on either state never affect the other's satellites. The copy is shallow, however — per-satellite scratch vectors (each signal's filtered_prompts and correlator_outputs, the soft-bit buffer, and the CN0 estimator's prompt buffer) are shared with the input and are overwritten by the next track call on either state. Treat the input as a stale handle after the call; deepcopy it first if you need to snapshot those buffers. The same applies to a bare downconvert_and_correlate: the returned state's correlator_outputs alias the input's, so reuse of one input state across several calls appends to the same buffers.\n\nFor real-time loops processing many chunks of signal in sequence, construct the correlator once outside the loop and pass it via the downconvert_and_correlator keyword argument:\n\ndc = CPUThreadedDownconvertAndCorrelator()\nwhile got_chunk(rx)\n    chunk = read_chunk!(rx)\n    track_state =\n        track(chunk, track_state, sampling_frequency; downconvert_and_correlator = dc)\nend\n\nThe default kwarg value builds a fresh correlator (with fresh per-thread scratch buffers) on every call, which is fine for one-shot use but defeats the allocation-free design in tight loops. See also track! for the in-place variant that avoids rebuilding track_state per call.\n\nThe coherent-integration length is a per-signal setting that lives on each TrackedSignal (its preferred_num_code_blocks_to_integrate field, addressed by (group, prn, signal)), not a track! argument. Set it with set_preferred_num_code_blocks_to_integrate!; the actual length is capped per integration by the signal's bit/secondary-code period and held at 1 until bit/secondary sync. Defaults to 1 (1 ms for GPS L5I / L1 C/A). Different satellites — and different signals on one satellite — can therefore integrate for different lengths:\n\nset_preferred_num_code_blocks_to_integrate!(track_state, :gps_l5, 1, GPSL5I, 10)  # PRN 1 L5I: 10 ms\n\nThe conventional estimator auto-scales each signal's carrier loop bandwidth by 1/N for its integration length N, so longer integration stays stable without re-tuning; the code loop keeps its absolute bandwidth, capped only where stability requires (see ConventionalPLLAndDLL).\n\n\n\n\n\n","title":"Tracking.track"},{"category":"function","location":"track.html#Tracking.track!","page":"Track","text":"track!(\n    measurements,\n    track_state;\n    downconvert_and_correlator,\n    doppler_update_interval\n)\n\n\nIn-place version of track. Mutates track_state by overwriting the Vector{TrackedSat} slots inside each group instead of rebuilding new immutable wrappers. Returns the same track_state object.\n\nAfter one warmup call (which seats each satellite's filtered_prompts buffer capacity), the single-threaded path is fully allocation-free.\n\nThe threaded path (CPUThreadedDownconvertAndCorrelator) keeps a small residual — about 64 B per completed code-block integration per group — but only when the process runs with more than one thread and the group holds more than one satellite (so Polyester's @batch actually distributes work). track! launches one @batch per code block, so this residual scales with the chunk length rather than staying flat per call; for a real-time loop with fixed-size chunks it is bounded per call and, in practice, dwarfed by the input sample buffer the caller allocates each chunk. The single-threaded backend has none of it.\n\nThe root cause is not the per-satellite state per se — it is that the GNSS signal object is not an isbits type: GPSL1CA, for example, holds a Matrix{Int16} code table and a (also non-isbits) SignalLUT. Polyester roots bare Arrays and isbits values into its worker tasks for free (that is why a Vector{Float64} kernel is allocation-free), but it pays a small per-launch allocation to root any other non-isbits object touched inside the @batch region. Each satellite's downconvert_and_correlate reaches its signal (for code-replica generation), so the parallel loop touches that non-isbits object once per launch. It could be removed by generating the code from the LUT's bare arrays (a prototype doing so inside the loop measures 0 B at full parallel throughput) — which needs a GNSSSignals-side bare-array gen_code! — or, in-tree, by a serial code-gen pre-pass (allocation-free but slower, since it serializes ~30 % of the work). Neither is currently worth ~64 B/block. See CPUThreadedDownconvertAndCorrelator for the full analysis.\n\nFor real-time loops, construct the correlator once outside the loop and pass it via the downconvert_and_correlator keyword argument:\n\ntrack_state = TrackState(signal, initial_sats)\ndc = CPUThreadedDownconvertAndCorrelator()      # hoist!\n\nwhile got_chunk(rx)\n    chunk = read_chunk!(rx)\n    track!(chunk, track_state, sampling_frequency; downconvert_and_correlator = dc)\nend\n\nThe correlator holds long-lived per-thread scratch buffers that grow on first use; rebuilding it via the default kwarg value would re-grow them every call.\n\n\n\n\n\n","title":"Tracking.track!"},{"category":"function","location":"track.html#Tracking.set_preferred_num_code_blocks_to_integrate!","page":"Track","text":"set_preferred_num_code_blocks_to_integrate!(\n    track_state,\n    group,\n    sat_id,\n    sig,\n    num_code_blocks\n)\n\n\nSet the preferred coherent-integration length, in primary code blocks, for one signal on one satellite — the preferred_num_code_blocks_to_integrate field of the addressed TrackedSignal. The actual length is still capped per integration by the signal's bit/secondary-code period and held at 1 until bit/secondary sync (see calc_num_code_blocks_to_integrate); with the conventional estimator the carrier loop bandwidth auto-scales by 1/N so the loop stays stable at any length, and the code loop bandwidth is left as configured unless stability caps it.\n\nFor data-bearing signals the length must evenly divide the number of code blocks that form one bit (e.g. a divisor of 20 for GPS L1 C/A, of 10 for GPS L5I) so integrations stay aligned to bit boundaries; an ArgumentError is thrown otherwise (issue #128). Pilot signals accept any length of at least one block.\n\nThe satellite is addressed exactly like the per-signal accessors (e.g. estimate_cn0) — in particular, the per-signal form always names the group explicitly, even on a single-group TrackState:\n\nset_preferred_num_code_blocks_to_integrate!(ts, :gps_l5, 1, GPSL5I, 10)  # (group, prn, signal)\nset_preferred_num_code_blocks_to_integrate!(ts, :gps_l5, 1, 10)          # single-signal sat\nset_preferred_num_code_blocks_to_integrate!(ts, 1, 10)                   # single-group state\nset_preferred_num_code_blocks_to_integrate!(ts, 10)                      # 1 group, 1 sat, 1 signal\n\nMutates track_state in place and returns it.\n\n\n\n\n\n","title":"Tracking.set_preferred_num_code_blocks_to_integrate!"},{"category":"type","location":"track.html#Tracking.BandMeasurement","page":"Track","text":"One band's incoming sample buffer plus the front-end metadata needed to process it. Bundles samples with the sampling_frequency and intermediate_frequency they were captured at — these are inseparable in practice, and the bundle removes the chance of mismatched parallel NamedTuples in a multi-band track call.\n\nFields:\n\nsamples::S: complex sample buffer (Vector for one antenna, Matrix with rows = samples and columns = antennas for an antenna array). Must be densely laid out in memory (unit row stride, columns packed back-to-back) — the SIMD downconvert/correlate kernels read the buffer through raw pointers with dense column-stride math, so a non-contiguous strided view would silently correlate the wrong samples. The constructor validates this and rejects non-dense buffers with an ArgumentError; contiguous views (e.g. view(buf, 1:4000)) remain fine.\nsampling_frequency::F: the buffer's sample rate (e.g. 4e6Hz)\nintermediate_frequency::F: the band's IF (defaults to 0.0Hz)\n\nIn a multi-band call, one BandMeasurement is built per band; a NamedTuple of BandMeasurements keyed by band feeds track. For the single-band case a plain buffer + scalar sample-rate keeps working unchanged.\n\nBandMeasurement(buf, 4e6Hz)                              # IF defaults to 0.0Hz\nBandMeasurement(buf, 4e6Hz, 1.575e6Hz)                   # explicit IF\nBandMeasurement(buf; sampling_frequency = 4e6Hz)         # kwarg form\n\n\n\n\n\n","title":"Tracking.BandMeasurement"},{"category":"type","location":"track.html#Tracking.BandMeasurements","page":"Track","text":"Type alias for a NamedTuple of BandMeasurements — the multi-band input shape of track / track!. Keys are the bands' GNSSSignals.get_band_id symbols (e.g. :L1, :L5) — nameof of the band type, folding to a compile-time constant, so the per-call NamedTuple lookup is free and new bands work without any Tracking-side registration.\n\n\n\n\n\n","title":"Tracking.BandMeasurements"},{"category":"type","location":"track.html#Tracking.CPUDownconvertAndCorrelator","page":"Track","text":"CPU-based implementation of downconversion and correlation. Holds one ScratchBuffers — three long-lived Vector{UInt8} byte buffers, one per scratch role (code replica + the fused kernel's two tile halves). Buffers grow lazily on first use and are reused thereafter, so a hoisted instance has zero allocations per track! call in steady state.\n\nFor real-time loops, construct the correlator once outside the track! loop and pass it via the downconvert_and_correlator keyword argument — the default value rebuilds the buffers on every call.\n\n\n\n\n\n","title":"Tracking.CPUDownconvertAndCorrelator"},{"category":"type","location":"track.html#Tracking.CPUThreadedDownconvertAndCorrelator","page":"Track","text":"Multi-threaded CPU downconvert and correlate, parallelized over the satellites (PRNs) of each group. Holds one ScratchBuffers per thread, indexed by Threads.threadid() inside @batch (which pins each iteration to a fixed thread). Buffers grow lazily on first use and are reused thereafter, so a hoisted instance's scratch is allocation-free in steady state.\n\nOne @batch is launched per code block (once per downconvert_and_correlate! call inside track!'s inner loop). When the process runs with more than one thread and the group has more than one satellite, each launch keeps a small Polyester allocation (~64 B), so the total scales with the number of completed code blocks in the chunk rather than staying flat per track! call. With a single thread, a single satellite, or the single-threaded CPUDownconvertAndCorrelator, there is no such residual.\n\nWhy it allocates at all — Polyester's @batch roots only bare Arrays and isbits values into its worker tasks for free (that is why a Vector{Float64} kernel allocates nothing). It pays a small per-launch allocation to root any non-isbits struct referenced inside the region — and it is the struct-ness that costs, not the contents: capturing even a plain struct whose only fields are Matrixes and isbits scalars measures the same ~64 B/launch, whereas capturing those same bare arrays measures 0. The culprit here is the GNSS signal: GPSL1CA, for instance, is not isbits — it wraps a Matrix{Int16} code table and a (also non-isbits) SignalLUT. Each satellite's code-replica generation needs its signal, so every @batch launch touches that non-isbits object once. (The per-satellite TrackedSat is likewise non-isbits — it carries the signal plus the CN0/bit buffers — and the loop reaches the signal through it, so iterating Vector{TrackedSat} is not free the way iterating a Vector{Float64} is.)\n\nNote that merely hoisting the SignalLUT out of the signal does not help — a SignalLUT is itself a non-isbits struct (it wraps Matrix{Int8} fields), so capturing it costs the same as capturing the whole GPSL1CA. What removes the cost entirely is a code-generation entry point that takes the LUT's bare arrays and isbits fields as separate arguments (padded, secondary, subchip_factor, …) rather than a struct: the @batch closure then captures only bare Arrays and isbits values, which Polyester roots for free. A prototype of exactly this — generating from the raw padded matrix, done inside the parallel loop — is bit-faithful and measures 0 B/launch, and because generation stays in the @batch region it keeps the full parallel throughput. Realizing it needs a GNSSSignals-side API that threads the bare arrays down to the resample kernel without re-wrapping them in a struct inside the region (reconstructing one there sends the generator dynamic and allocates far more). Absent that, the only in-tree way to reach 0 is a serial code-gen pre-pass, which is allocation-free but serializes ~30 % of the work and slows the threaded pipeline — the inferior fallback. Neither is currently worth ~64 B/block, which is bounded per call and dwarfed by the caller's input buffer.\n\nFor real-time loops, construct the correlator once outside the track! loop and pass it via the downconvert_and_correlator keyword argument — the default value rebuilds the per-thread buffers on every call.\n\n\n\n\n\n","title":"Tracking.CPUThreadedDownconvertAndCorrelator"},{"category":"type","location":"track.html#Tracking.Int16DownconvertAndCorrelator","page":"Track","text":"Integer (Complex{Int16}) hybrid-blocked CPU downconvert + correlate backend (single-threaded). Opt-in alternative to [CPUDownconvertAndCorrelator] for Complex{Int16} sample buffers; errors on any other sample element type. Construct once outside the track! loop and pass it via the downconvert_and_correlator keyword for an allocation-free steady state on the static correlator path (EPL/VEPL and any SVector-shifts correlator). The runtime AbstractVector-shifts fallback additionally allocates the small Vector it returns each integration, but reuses the same thread-local scratch.\n\nArguments\n\nmax_meas (the first positional argument, required — no default) is the largest |real|/|imag| any measurement sample will take, i.e. your front end's full-scale (e.g. 2^11 for a 12-bit ADC). From it the constructor picks the LARGEST carrier-replica amplitude whose carrier wipe still fits Int16, and the wipe arithmetic type (Int16 on the fast path, else Int32). The carrier wipe is a complex multiply DI = mᵣ·cos + mᵢ·sin, DQ = mᵢ·cos − mᵣ·sin — two products summed per output — so the amplitude is sized against 2·max_meas·amplitude ≤ typemax(Int16), bounding both the products and their sum. There is deliberately no default: under-declaring max_meas silently overflows the Int16 wipe and corrupts the correlation catastrophically, so you must state it explicitly. Over-declaring is safe and only coarsens the carrier quantisation.\n\nnote: Performance: keep `max_meas < 2^14`\nFor max_meas ≥ 2^14 no Int16-safe carrier amplitude ≥ 1 exists, so the backend falls back to an exact Int32 carrier wipe — the on-x86 vpmaddwd Int16 fast path no longer applies — and shrinks the strip-mine block to keep the correlation accumulators from overflowing. This stays correct but is measurably slower. This backend is tuned for ≤12-bit sample buffers; keep max_meas below 2^14 to stay on the fast path.\n\nThe blk keyword sets the strip-mine block length (samples). It must be ≥ 1, validated at construction — blk ≤ 0 would make the strip-mine loop never advance and hang track! (issue #169). A blk larger than the overflow-safe block is accepted and simply clamped for the flush (see _int16_flush_len / _int16_safe_blk), so the correlation accumulators never wrap.\n\n\n\n\n\n","title":"Tracking.Int16DownconvertAndCorrelator"},{"category":"type","location":"track.html#Tracking.Int16ThreadedDownconvertAndCorrelator","page":"Track","text":"Multi-threaded integer (Complex{Int16}) hybrid-blocked backend. One Int16ScratchBuffers per thread (indexed by Threads.threadid() inside @batch); the carrier table is immutable and shared. See Int16DownconvertAndCorrelator for the max_meas amplitude argument.\n\n\n\n\n\n","title":"Tracking.Int16ThreadedDownconvertAndCorrelator"},{"category":"type","location":"track.html#Tracking.OneBitDownconvertAndCorrelator","page":"Track","text":"One-bit (hard-limited) bit-wise CPU downconvert + correlate backend (single-threaded). Opt-in alternative to [CPUDownconvertAndCorrelator] for Complex{Int16} sample buffers: it 1-bit-quantises the measurement, carrier and code and correlates with XOR + popcount. Construct once outside the track! loop and pass it via the downconvert_and_correlator keyword for an allocation-free steady state. 1-bit quantisation trades ≈2–3 dB of SNR for bit-wise speed; downstream consumers are ratio-normalised, so the coarse amplitude is immaterial.\n\n\n\n\n\n","title":"Tracking.OneBitDownconvertAndCorrelator"},{"category":"type","location":"track.html#Tracking.OneBitThreadedDownconvertAndCorrelator","page":"Track","text":"Multi-threaded one-bit bit-wise backend. One OneBitScratchBuffers per thread (indexed by Threads.threadid() inside @batch). See OneBitDownconvertAndCorrelator.\n\n\n\n\n\n","title":"Tracking.OneBitThreadedDownconvertAndCorrelator"},{"category":"type","location":"track.html#Tracking.TwoBitDownconvertAndCorrelator","page":"Track","text":"Two-bit (sign + magnitude) bit-wise CPU downconvert + correlate backend (single-threaded). Opt-in alternative to [CPUDownconvertAndCorrelator] for Complex{Int16} sample buffers, sitting between the OneBitDownconvertAndCorrelator and Int16DownconvertAndCorrelator: measurement and carrier are 4-level {±1, ±3} (2-bit sign + magnitude, the carrier bit planes straight off SinCosLUT's NCO), the code stays 1-bit, correlated with XOR + masked popcount. Recovers ≈2 dB of correlation SNR over the one-bit backend (≈0.8 dB total quantisation loss vs Float32) while keeping the bit-wise speed advantage over the integer/float backends. Construct once outside the track! loop and pass it via the downconvert_and_correlator keyword for an allocation-free steady state.\n\nthreshold is the measurement magnitude split point in ADC counts (|component| ≥ threshold ⇒ the ±3 level); set it near 1σ of your front end's input for the near-optimal 4-level quantiser. Downstream consumers are ratio-normalised, so the absolute scale is immaterial.\n\n\n\n\n\n","title":"Tracking.TwoBitDownconvertAndCorrelator"},{"category":"type","location":"track.html#Tracking.TwoBitThreadedDownconvertAndCorrelator","page":"Track","text":"Multi-threaded two-bit bit-wise backend. One TwoBitScratchBuffers per thread (indexed by Threads.threadid() inside @batch). See TwoBitDownconvertAndCorrelator.\n\n\n\n\n\n","title":"Tracking.TwoBitThreadedDownconvertAndCorrelator"},{"category":"type","location":"track.html#Tracking.AbstractDownconvertAndCorrelator","page":"Track","text":"Abstract downconverter and correlator type. Structs for downconversion and correlation must have this abstract type as a parent.\n\nThe per-sat correlation loop, per-group body, and public downconvert_and_correlate(!) entry points are defined once on this abstract type (see downconvert_and_correlate_cpu.jl); a subtype customises behaviour by overriding the dispatch hooks it needs — _correlate_signals / _scratch_buffers (kernel + scratch), _threading (serial vs. Polyester @batch, default serial), and _check_sample_type (per-backend sample-type check, default no-op). A subtype that overrides none inherits the single-threaded CPU plumbing rather than getting a MethodError.\n\n\n\n\n\n","title":"Tracking.AbstractDownconvertAndCorrelator"},{"category":"section","location":"index.html#Tracking.jl","page":"Tracking.jl","text":"This package implements the tracking functionality of GNSS satellites that's part of the larger GNSS receiver. Tracking.jl primarily consists of two main blocks:\n\nSignal down-conversion and correlation\nCode and carrier estimation to generate replicas and close the loop\n\nTracking.jl provides defaults for both blocks, but it provides mechanisms to hook in your own implementation (by using multiple dispatch). For signal down-conversion and correlation Tracking.jl provides a highly optimized CPU implementation using hand-tuned SIMD intrinsics. With respect to the second block Tracking.jl provides conventional DLLs and PLLs with FLL-assisted carrier tracking as the default.\n\nDown-conversion and correlation is done in full code blocks meaning from code start to code end or multiples of that (e.g. in GPS L1 C/A from 0 to N*1023). The factor N can be specified, but will be 1 as long as the bit start is unknown — Tracking.jl uses single-code-period integrations to locate the bit edge. Once the bit start is known for every tracked satellite, longer coherent integrations become available and the result is handed over to the code-and-carrier estimation block.\n\nMoreover, Tracking.jl allows tracking of signals from phased antenna arrays meaning that they are down-converted and correlated by the very same replica to conserve phase relationships. Multi-signal tracking is supported: a single satellite can be tracked on several signals at once (e.g. GPS L1 C/A together with L1C-D and L1C-P) sharing one carrier downconvert per outer iteration, with a per-signal correlator each.","title":"Tracking.jl"},{"category":"section","location":"index.html#Supported-signals","page":"Tracking.jl","text":"GPS L1 C/A\nGPS L1C-D (data)\nGPS L1C-P (pilot, with 1800-chip overlay-code sync)\nGPS L2CM (data)\nGPS L2CL (pilot, dataless — no secondary code)\nGPS L5I\nGPS L5Q (pilot, with NH20 secondary-code sync)\nGalileo E1B (and its BOC(1,1) approximation)\nGalileo E1C (pilot, with CS25 secondary-code sync; and its BOC(1,1) approximation)\nGalileo E5a-I (data, with CS20 secondary-code sync)\nGalileo E5a-Q (pilot, with per-PRN CS100 secondary-code sync)\n\nPages = [\n  \"track.md\",\n  \"tracking_state.md\",\n  \"bit_sync.md\",\n  \"loop_filter.md\",\n  \"custom_doppler_estimator.md\",\n  \"correlator.md\",\n  \"cn0_estimator.md\"\n]\nDepth = 1","title":"Supported signals"},{"category":"section","location":"index.html#Installation","page":"Tracking.jl","text":"julia> ]\npkg> add Tracking","title":"Installation"},{"category":"section","location":"index.html#Quick-start","page":"Tracking.jl","text":"The minimum to track a single GPS L1 C/A satellite: build a TrackState, seed it from an acquisition handoff via add_satellite!, then call track on each measurement.\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> using GNSSSignals: gen_code, get_code_frequency, get_code_center_frequency_ratio\n\njulia> track_state = TrackState(; signal = GPSL1CA());\n\njulia> track_state = add_satellite!(track_state; prn = 1, code_phase = 50.0, carrier_doppler = 1000.0Hz);\n\njulia> sampling_frequency = 4e6Hz;\n\njulia> num_samples = 4000;\n\njulia> signal = GPSL1CA();\n\njulia> code_frequency = 1000.0Hz * get_code_center_frequency_ratio(signal) + get_code_frequency(signal);\n\njulia> measurement = cis.(2π .* 1000.0Hz .* (0:num_samples-1) ./ sampling_frequency) .*\n           gen_code(num_samples, signal, 1, sampling_frequency, code_frequency, 50.0);\n\njulia> track_state = track(measurement, track_state, sampling_frequency);\n\njulia> get_carrier_doppler(track_state, 1)\n999.9999883655299 Hz\n\njulia> get_code_phase(track_state, 1)\n50.00064935064897\n\nestimate_cn0(track_state, prn) returns the CN0 estimate in dB-Hz. With a noise-free test signal it is Inf dB-Hz; real signals typically land in 30–50 dB-Hz.\n\nEverything beyond this minimal case — multi-satellite, multi-system, multi-signal, multi-band, phased arrays, acquisition handoff, removing satellites, the accessor ladder — is covered in Tracking State. Real-time loop patterns (hoisting the correlator, track!, allocation behavior) are covered in Track.\n\ntip: Real-time loops: hoist the correlator\nFor loops processing many chunks in sequence, construct CPUThreadedDownconvertAndCorrelator (or CPUDownconvertAndCorrelator) once outside the loop and pass it via downconvert_and_correlator =. See Track.","title":"Quick start"},{"category":"section","location":"index.html#Q/A","page":"Tracking.jl","text":"Why are the correlator values zero?\n\nThe correlator output given by get_last_fully_integrated_correlator is the correlation result after the code phase has reached the full code length or multiples of the code length. If the current tracked signal does not include the end of the PRN sequence (or multiples of that), the correlator from the last complete integration will be returned. At the very start of tracking, before any complete integration has occurred, the correlator values will be zero.","title":"Q/A"},{"category":"section","location":"noise_estimator.html#Noise-Estimator","page":"Noise Estimator","text":"NoiseRefCN0Estimator — the default C/N₀ estimator — divides each record's prompt power by a measured noise floor instead of inferring one from the prompt's own statistics. This page is about where that floor comes from.\n\nThe floor is estimated once per signal, never per satellite: every satellite tracking a signal shares its floor, and averaging once per signal is what makes the reference's own variance negligible against the per-record prompt statistics. The estimators live in TrackState's noise_estimators field, a NamedTuple keyed by signal id (GNSSSignals.get_signal_id — :GPSL1CA, :GalileoE1B, …).\n\nOn the sample-driven path none of this needs configuring — TrackState provisions a CorrelatorNoiseEstimator for every signal asking for a density, and track! fills it. Read on if you feed correlator outputs from hardware, or if you want to know what the measurement actually does.","title":"Noise Estimator"},{"category":"section","location":"noise_estimator.html#Why-per-signal-and-not-per-RF-band","page":"Noise Estimator","text":"Because what a record divides by is the post-correlation floor. For an interferer of PSD S_I(f) on top of the thermal floor that is\n\nN₀,eff = N₀,thermal + ∫ S_I(f) · |G(f)|² df\n\n— the spectral separation coefficient — weighted by the despreading modulation's own spectrum |G(f)|². So two signals sharing one band, one antenna and one front end do not share a noise floor the moment the interference is not white, and the gap is not subtle. BPSK(1) peaks at band centre and nulls at ±1.023 MHz; BOC(1,1) is the reverse. A CW tone at the chip rate is rejected by GPS L1 C/A and lands squarely on Galileo E1B; move it into C/A's main lobe and the order reverses. Measured through track! on one shared set of samples, 13 dB J/N tone, densities relative to the thermal floor (4 MHz, pure BOC(1,1) E1B):\n\ntone GPS L1 C/A Galileo E1B E1B / C/A\nnone 1.0 1.0 1.0\n1.023 MHz 1.4 39 28×\n0.4 MHz 35 26 0.75×\n\nReporting C/A's figure to E1B in the middle row overstates E1B's C/N₀ by ≈14.5 dB; the reversal in the last row is what makes this a spectral result and not a scale factor. The real CBOC E1B shows the same reversal about four times larger. Front-end tilt, filter roll-off at the band edge and adjacent-band leakage colour the floor the same way, more quietly.\n\nThe payoff of despreading rather than metering power is that this integral is measured, never modelled: the reference runs the consumer's own code, so its spectral weighting is the consumer's by construction, with no jammer model and no spectrum analysis anywhere. It is the same argument that makes the measurement backend-free, extended from the quantiser to the interference environment.\n\nIt also matches the hardware: a noise channel is a tracking channel with a wrong PRN, and a tracking channel is configured with a code.\n\nThe samples remain a band property — one front end feeds every signal on it, and BandMeasurements stays keyed by band id. Only the despread, and therefore the measured floor, is per signal.","title":"Why per signal and not per RF band"},{"category":"section","location":"noise_estimator.html#Why-a-density-and-not-a-power","page":"Noise Estimator","text":"For a correlator normalised the way Tracking normalises — by integrated_samples * code_amplitude — white input noise of per-sample variance σ² gives\n\nE|P|² = σ²/N = N₀/T\n\nso N₀ = σ²/f_s is independent of the integration time. That is the whole reason a density is stored rather than a power, and it buys two things:\n\nOne per-signal figure serves records of any length. A satellite still on 1-block records pre-sync and one promoted to 20-block records post-sync divide by the same N̂₀, each subtracting its own 1/T. Mixed record lengths are handled by construction rather than by guarding.\nThe reference needs no alignment to correlator records. It can run on whatever grid is convenient and be averaged over a much longer window than any satellite's prompt buffer, because N₀ is a slowly-varying front-end property.\n\nN₀ carries Unitful dimensions of 1/Hz, so ⟨|P|²⟩/N₀ and 1/T are both in Hz and the arithmetic is dimension-checked rather than trusted.","title":"Why a density and not a power"},{"category":"section","location":"noise_estimator.html#The-two-fill-paths","page":"Noise Estimator","text":" software hardware\nfills the window via track! → downconvert_and_correlate! → update_noise! append_noise_observation!\nenters downconvert_and_correlate!? yes no\nneeds sample buffers? yes no\n\nThey live on disjoint call graphs — a hardware producer never calls downconvert_and_correlate!, it injects correlator outputs and folds — so one concrete type serves both and nothing needs to distinguish them. You configure CorrelatorNoiseEstimator either way; you simply fill it differently.","title":"The two fill paths"},{"category":"section","location":"noise_estimator.html#The-software-source","page":"Noise Estimator","text":"Three properties are worth knowing about it.\n\nIt is open-loop. The reference runs at its band's nominal IF with zero Doppler and an off-peak code phase from a rotating PRN. There is no discriminator, no loop filter and no NCO update ever written to it — so it is correct with zero satellites tracked, which is also what would make it usable as an acquisition CFAR floor.\n\nIt is model-free, and that is the reason it despreads rather than measuring Σ|x|². The reference traverses the identical quantise → downconvert → despread → accumulate path as the prompt, on the same kernel, so the measured N̂₀ already contains every imperfection of that chain — the quantisation loss, the quantiser's operating point under load, the input scaling an AGC step moves, a CBOC replica's code amplitude — with no per-backend analysis and no closed-form correction. It shows up directly: the same 45 dB-Hz sky reports ≈45 dB-Hz on the float and Int16 backends, ≈44 on two-bit and ≈43 on one-bit, i.e. each backend's own quantisation loss, without a line of per-backend correction anywhere.\n\nIts sub-integration length is derived, not configured. Coherent integration buys nothing for noise-power estimation: for a complex-Gaussian accumulation Var(|B|²) = (E|B|²)², so one dump carries 100 % relative error however long it is, and all the information about σ² lives in the number of independent looks. Shortening the dump is therefore the only way to buy more of them — and three things say not to. SIMD (64 kernel calls per 1 ms chunk instead of one, and a shorter dump would need a new kernel variant, forfeiting the bit-identical arithmetic the approach rests on). DC balance (a full-period Gold code is balanced, E[(Σc)²] ≈ 1; a 16-chip window behaves like iid signs at ≈16, so an ADC offset biases a short despread and not a full-period one). And cadence parity with a hardware correlator, which naturally dumps on the code epoch. The variance is bought back with window_duration instead, which is nearly free — a 1 s window is ~1000 Float64 densities per signal.","title":"The software source"},{"category":"section","location":"noise_estimator.html#Hardware-producers-(FPGA-/-ASIC)","page":"Noise Estimator","text":"The question a hardware implementer asks first is whether the noise measurement should reuse the existing downconvert-and-correlate datapath or get a dedicated, minimal one. Reuse it — allocate a channel. Not because it is less work, but because on the architectures FPGA correlators actually use it is also cheaper:\n\nOn a time-multiplexed engine — one physical datapath across N channels at a high clock, per-channel state in BRAM — a noise channel is one more slot in the schedule. A handful of state words, essentially zero logic. A dedicated path is a whole second datapath.\nOn a fully parallel array it costs less than one channel out of N, because the reference is open-loop: fixed NCO increments, no discriminator, no loop-filter interface, no dump-on-command synchronisation. A dedicated path would have to re-implement the carrier NCO, code generator and complex multiplier to save accumulators you were not using — so it is larger, not smaller.\nNo divider and no float on chip. Report the raw integer accumulation with its weight and sample count; every division, the A_c² and f_s scaling, and all averaging happen in Tracking. That is both the cheapest option on-chip and the mechanism that forces the two paths to agree.\n\nAnd reuse is what buys the model-freeness above: identical quantisation and rounding, so the 1-bit/2-bit loss cancels in the ratio; identical input scaling, so an AGC step moves numerator and denominator together; identical code generator, so A_c matches exactly. A path tapping at a different point — before a decimation filter, at a different bit width — breaks all three silently.","title":"Hardware producers (FPGA / ASIC)"},{"category":"section","location":"noise_estimator.html#What-the-producer-must-match","page":"Noise Estimator","text":"Only two things, because everything else is computed here:\n\nReport raw accumulations, through one of the builders below. They ride the same return path as the correlator outputs, so no new wire interface is needed: append_noise_observation! sits beside append_correlator_output! on the host.\nUse an off-peak code phase and rotate the PRN. A PRN not currently tracked on that signal is simplest, since then any phase is safe — a PRN tracked on another signal of the same band is a different code and stays available.\n\nA producer that can only dump on the code epoch matches the software default exactly, so in the normal case there is nothing to reconcile. Where a producer does differ it still does not conflict: \"the two paths behave the same\" means computed by the same code, on the same scale, with the same bias, which the shared window guarantees because it is M-weighted and bounded in time. Only the precision differs — a code-epoch dump gives K_n ≈ 1000 looks per second per tap, which is indistinguishable from anything finer below 40 dB-Hz.","title":"What the producer must match"},{"category":"section","location":"noise_estimator.html#The-observation-and-its-builders","page":"Noise Estimator","text":"All three reduce to the same N₀ on the same white input, which is what makes the paths interchangeable.\n\nThe weight is M, the number of independent looks — not the sample count. For Σ_m |B_m|² over M sub-integrations of N samples each the density needs only M·N, but its relative variance is 1/M: a producer handing back one 1 ms coherent despread and one handing back 64 16-chip despreads report the same total samples and wildly different precision. Weighting the window by M is what makes observations from different producers combinable at all.\n\nA worked ingest, per fold:\n\n# taps, per signal, exactly as before\nappend_correlator_output!(track_state, CorrelatorOutput(correlator, n, sample_index), prn)\n\n# the noise channel: Σ|B|² pooled over its taps, one signal at a time. A noise\n# channel is configured with a code, so this is the granularity a producer\n# already has.\n# `accumulated_power` is the raw integer sum off the wire; `num_taps` is how many\n# accumulators it pooled, and the sample count is per tap times that.\nappend_noise_observation!(\n    track_state,\n    noise_observation_from_correlator(\n        accumulated_power,\n        num_taps,\n        num_taps * samples_per_dump,\n        sampling_frequency;\n        prn = noise_prn,\n        # the taps are simultaneous, not consecutive — they all integrate the\n        # same samples, so the window must count that span once\n        duration = samples_per_dump / sampling_frequency,\n    ),\n    :GPSL1CA,\n)\n\nestimate_dopplers_and_filter_prompt!(track_state, (L1 = sampling_frequency,))\n\nIf a signal has a noise estimator and nothing ever fills it, the fold warns once per signal and every C/N₀ on it reads -Inf dB-Hz. That is the likeliest integration mistake and the warning names the fix; it never fires on the sample-driven path.","title":"The observation and its builders"},{"category":"section","location":"noise_estimator.html#How-append_noise_observation!-differs-from-append_correlator_output!","page":"Noise Estimator","text":"They are deliberately not the same mechanism:\n\n append_correlator_output! append_noise_observation!\nscope one signal of one satellite one signal, across every satellite tracking it\npayload the whole correlator: every tap kept separate, complex, per antenna the taps pooled into one power sum, plus M, the span and the PRN\ndrives discriminators, both loop filters, bit sync, the NCO update, the C/N₀ prompt the noise floor only\ntiming sample_index is load-bearing — vector tracking needs a common grid none: N₀ is slowly varying, so only \"recent\" matters\nlifetime drained by the fold each chunk, buffer reused sliding window bounded in time, never drained\ncount one per completed coherent integration per signal any granularity; M carries the weight\n\nThe payload row is the sharpest difference. Both traverse a multi-tap correlator, for opposite reasons: the prompt path needs the taps kept apart because their differences are the code and carrier discriminants, while the reference pools them, because at ≥1 chip spacing they are three independent looks at one scalar and nothing about their relative values means anything.","title":"How append_noise_observation! differs from append_correlator_output!"},{"category":"section","location":"noise_estimator.html#Writing-your-own-source","page":"Noise Estimator","text":"AbstractNoiseEstimator and its three methods are public, so a source that is neither of the shipped paths — a front-end power monitor read over a sideband, say — is a subtype away:\n\nstruct MyPowerMonitor <: Tracking.AbstractNoiseEstimator\n    densities::Vector{typeof(1.0 / 1.0Hz)}\nend\n\n# fed from outside, so the sample-driven path is a no-op (that is the default,\n# and it is why `update_noise!` need not be implemented at all here)\nTracking.append_noise_observation!(e::MyPowerMonitor, obs) =\n    (push!(e.densities, obs.noise_density); e)\nTracking.get_noise_density(e::MyPowerMonitor) =\n    isempty(e.densities) ? nothing : sum(e.densities) / length(e.densities)\n\nPass it in through TrackState's noise_estimators keyword:\n\nTrackState(; signal = GPSL1CA(), noise_estimators = (GPSL1CA = MyPowerMonitor(...),))\n\nTwo contracts to keep. The window must be mutated in place and the struct returned unchanged — TrackState is immutable and never rebuilt for a noise update. And Tracking.noise_density_type must name the concrete type get_noise_density returns, so the fold can split off the nothing once per signal per chunk and keep everything below it monomorphic; it defaults to typeof(1.0/1.0Hz), which every shipped builder produces.","title":"Writing your own source"},{"category":"type","location":"noise_estimator.html#Tracking.AbstractNoiseEstimator","page":"Noise Estimator","text":"Abstract supertype for per-signal noise estimators — the source of the noise density N₀ that NoiseRefCN0Estimator divides each record's prompt power by.\n\nOne instance is held per signal in TrackState's noise_estimators NamedTuple (keyed by GNSSSignals.get_signal_id), never per satellite: every satellite of a signal shares one floor, and averaging it once per signal is what makes the reference's own variance negligible against the per-record prompt statistics.\n\nWhy per signal and not per RF band\n\nWhat a record actually divides by is the post-correlation density. For an interferer of PSD S_I(f) on top of the thermal floor that is\n\nN₀,eff = N₀,thermal + ∫ S_I(f) · |G(f)|² df\n\n— the spectral separation coefficient, weighted by the despreading modulation's own spectrum |G(f)|². Two signals sharing one band, one antenna and one front end therefore see different floors the moment the interference is not white, and the difference is not small: BPSK(1) has its peak at DC and a null at ±1.023 MHz, BOC(1,1) is the reverse, so a CW tone at band centre is rejected by Galileo E1B and lands squarely in GPS L1 C/A, while a tone at ±1.023 MHz does the opposite. Front-end tilt, filter roll-off at the band edge and adjacent-band leakage all colour the floor the same way, more quietly.\n\nBecause CorrelatorNoiseEstimator despreads rather than metering power, keying by signal makes that integral measured rather than modelled: the reference runs the consumer's own code, so its spectral weighting is the consumer's by construction. This is the same argument that makes the reference backend-free — it traverses the identical path as the prompt — extended from the quantiser to the interference environment.\n\nIt also matches the hardware. A noise channel is a tracking channel with a wrong PRN, and a tracking channel is configured with a code; per-signal is what an FPGA would build anyway.\n\nWhy a density and not a power\n\nFor a correlator normalised the way Tracking.normalize normalises — by integrated_samples * code_amplitude — white input noise of per-sample variance σ² gives E|P|² = σ²/N = N₀/T. So N₀ = σ²/f_s is independent of the integration time, which is what lets one per-signal figure serve records of any length. It is stored as a Unitful quantity of dimension 1/Hz, so the consumer's ⟨|P|²⟩/N₀ − 1/T is dimension-checked rather than trusted.\n\nThe interface\n\nThree methods, all with a default on this abstract type:\n\nupdate_noise! — measure one signal's slice of its band's samples and append the resulting observations. This is the software fill path; it has exactly one call site, inside downconvert_and_correlate!.\nappend_noise_observation! — append an observation built elsewhere. This is the hardware fill path (FPGA/ASIC correlator or a front-end power monitor), parallel to append_correlator_output!.\nget_noise_density — the signal's current density, or nothing while nothing has been measured yet. A read, not a drain: the window keeps sliding.\n\nThe two fill paths live on disjoint call graphs — a hardware producer never calls downconvert_and_correlate! — so one concrete type serves both and no type parameter distinguishes them. See CorrelatorNoiseEstimator, the only shipped implementation.\n\nA source that must not measure from samples (a pure power-monitor integration, say) subtypes this and gives update_noise! a no-op method; that is why the interface is exported.\n\n\n\n\n\n","title":"Tracking.AbstractNoiseEstimator"},{"category":"function","location":"noise_estimator.html#Tracking.update_noise!","page":"Noise Estimator","text":"update_noise!(\n    estimator,\n    measurement,\n    first_sample,\n    last_sample,\n    context\n)\n\n\nMeasure one signal's noise on its band's samples and append the resulting observations to estimator's window, returning estimator.\n\nmeasurement is the band's BandMeasurement — the samples are a band property, one front end feeding every signal on it; only the despreading code, and therefore the measured floor, is per signal. first_sample and last_sample bound the slice of it this call may consume (the current chunk). context is a NoiseUpdateContext, carrying what a source may need but a BandMeasurement does not have — the signal being measured, which PRNs are currently tracked on it, and the chunk index.\n\nThis is the software fill path and it has exactly one call site, inside downconvert_and_correlate!. A source whose observations arrive from outside (see append_noise_observation!) never reaches it, so the default here is a no-op: it mutates the window in place and leaves the struct alone, which is what lets TrackState hold it immutably.\n\n\n\n\n\nupdate_noise!(\n    estimator,\n    measurement,\n    first_sample,\n    last_sample,\n    context\n)\n\n\nMeasure this signal's noise over samples first_sample:last_sample of measurement and append the resulting observations, returning estimator.\n\nThe slice is split into num_sub equal sub-integrations,\n\nnum_sub = max(1, round(Int, slice_duration / code_period))\n\nwith code_period the primary code period of the signal being measured — see CorrelatorNoiseEstimator for why the sub-integration length is derived rather than configured. Equal slices rather than fixed-length ones so no remainder is wasted and every window entry is statistically identical; the max(1, …) matters, because a signal whose code period exceeds the chunk (a 10 ms code with 1 ms chunks) would otherwise yield no observations at all, forever. Each observation is pushed individually: pre-averaging would put one entry per chunk in the window, re-welding its time span to the Doppler update rate.\n\nEach sub-integration starts its replica at code phase 0 and runs for its slice. Nothing here follows a code-period boundary, and that is deliberate: the reference despreads with a wrong PRN at a wrong phase, so there is no signal to accumulate coherently and N̂₀ = |B|²/(N·A_c²·f_s) is unbiased for any N and any starting phase. Following boundaries would be actively harmful — a chunk rarely holds a whole number of code periods, so a boundary-aligned reference would have to carry a partial accumulator across calls, which is exactly the scalar state that has no per-signal anchor. Successive observations are independent because their sample ranges are disjoint; the replica repeating is irrelevant.\n\nAll of the correlator's taps are used and pooled into one power sum, at tap_code_shift chips apart so they are genuinely independent looks (see the type's docstring). The despread runs on the caller's backend kernel — the same one the prompt goes through — which is what makes the measurement model-free.\n\nThe reference is open-loop: the band's nominal IF as the carrier frequency, zero Doppler and zero carrier phase. ±5 kHz of Doppler is 0.5 % of a 1 MHz code band, so no satellite's state is needed and the reference is correct with nothing tracked at all.\n\n\n\n\n\n","title":"Tracking.update_noise!"},{"category":"function","location":"noise_estimator.html#Tracking.append_noise_observation!","page":"Noise Estimator","text":"append_noise_observation!(estimator, _)\n\n\nAppend one externally built NoiseObservation to estimator's sliding window and return estimator. This is the hardware fill path, parallel to append_correlator_output! — see there for how the two differ.\n\nThe window is mutated in place and the struct is not rebuilt, so this is allocation-free in steady state and works through the immutable TrackState.\n\nThe TrackState form selects the signal:\n\nappend_noise_observation!(track_state, obs)             # single-signal TrackState\nappend_noise_observation!(track_state, obs, :GPSL1CA)   # explicit signal id\nappend_noise_observation!(track_state, obs, GPSL1CA)    # or the signal type\n\n\n\n\n\nappend_noise_observation!(estimator, observation)\n\n\nAppend observation to the signal's sliding window, dropping entries off the front while the remainder still spans window_duration. Returns estimator.\n\nThe window is bounded in time, not in observation count, which is what lets one producer report 16-chip accumulations and another a single pre-averaged 0.2 s figure under the same configuration and with no scalar state — the span is computable from the FIFO itself.\n\n\n\n\n\nappend_noise_observation!(\n    track_state,\n    observation,\n    signal_id\n)\n\n\nAppend an externally built NoiseObservation to the addressed signal's noise estimator and return track_state:\n\nappend_noise_observation!(track_state, obs) — single-signal TrackState.\nappend_noise_observation!(track_state, obs, :GPSL1CA) — explicit signal id.\nappend_noise_observation!(track_state, obs, GPSL1CA) — or the signal type / an instance of it, which is what a caller usually has to hand.\n\nThis is the hardware/FPGA fill path, and it is symmetric with what such a producer already does for the taps: append_correlator_output! per signal, append_noise_observation! per signal, then estimate_dopplers_and_filter_prompt! to fold. The two are deliberately not the same mechanism — see append_noise_observation!'s estimator-level method for the table of differences.\n\nPer signal and not per band because the floor a record divides by is the post-correlation one, and that depends on the despreading modulation: a noise channel is a tracking channel with a wrong PRN, so it is configured with a code exactly like the ones it serves. See AbstractNoiseEstimator.\n\nThe signal must have a noise estimator; it has one whenever its C/N₀ estimator reads a density (see requires_noise_density), or whenever you declared one through TrackState's noise_estimators keyword.\n\n\n\n\n\n","title":"Tracking.append_noise_observation!"},{"category":"function","location":"noise_estimator.html#Tracking.get_noise_density","page":"Noise Estimator","text":"get_noise_density(_)\n\n\nThe signal's current noise density N₀, or nothing while the window holds nothing yet.\n\nA read, not a drain: the window keeps sliding across chunks and across track! calls, so every record of every satellite tracking that signal divides by the same figure. The returned quantity has dimension 1/Hz — see AbstractNoiseEstimator for why a density rather than a power, and why per signal rather than per band.\n\n\n\n\n\nget_noise_density(estimator)\n\n\nThe window's M-weighted mean density, or nothing while it is empty.\n\nWeighted by num_sub_integrations because that is the number of independent looks each entry represents: the density itself needs only the sample count, but its relative variance is 1/M, so a one-dump entry and a 64-dump entry combine correctly only when weighted this way.\n\n\n\n\n\n","title":"Tracking.get_noise_density"},{"category":"type","location":"noise_estimator.html#Tracking.CorrelatorNoiseEstimator","page":"Noise Estimator","text":"The signal's noise reference, measured by despreading an untracked PRN — the only AbstractNoiseEstimator Tracking ships, and the one to configure on a hardware path too (you simply fill it with append_noise_observation! instead of letting update_noise! fill it).\n\nWhy a despread and not a power meter\n\nThe reference traverses the identical quantise → downconvert → despread → accumulate path as the prompt, reusing the same kernel, the same replica generator and — because the estimator is keyed by signal — the same code. So the measured N₀ already contains every imperfection of that chain — the 1-bit / 2-bit quantisation loss, the quantiser's operating point under load, the input scaling an AGC step moves, the code amplitude of a CBOC replica — with no per-backend model and no closed-form correction. A Σ|x|² power meter would need one line per backend and would still miss the second-order coupling where a strong signal shifts that operating point.\n\nIt is also what makes the spectral weighting right rather than assumed. Sharing the consumer's code means the despread evaluates that signal's own ∫ S_I(f)·|G(f)|² df — the coloured-interference case a power meter, or a reference despread with some other signal's code, both get wrong. See AbstractNoiseEstimator.\n\nIt is open-loop\n\nThe reference has no feedback of any kind: it runs at the band's nominal IF with zero Doppler and an off-peak code phase from a rotating PRN, so there is no discriminator, no loop filter and no NCO update ever written to it. It is therefore correct with zero satellites tracked, and on an FPGA a noise channel is a strict subset of a tracking channel rather than an addition to one.\n\nFields / configuration\n\nwindow_duration — how far back the sliding window reaches (1 s by default). The one genuine tunable, because it trades: longer means lower variance, but a longer smear across AGC changes. At the default and one sub-integration per code period the window holds K_n ≈ 1000 looks per tap, which costs ≤0.08 dB against a variance-free reference at every C/N₀ — below which no further tuning is warranted.\ntap_code_shift — the reference correlator's tap spacing in chips (1.5 by default). Taps are only worth having if they are statistically independent, and at the tracking default of ±0.5 chip they are not: for jointly circular Gaussian accumulations corr(|Bᵢ|²,|Bⱼ|²) = |ρᵢⱼ|², and the sampled-code correlation at half a chip is ≈0.5, so three taps are worth 2.25 independent looks. At ≥1 chip they are worth 2.98. 1.5 chips sits in the autocorrelation null and clear of both the 1- and 2-chip sidelobe values.\nbuffered — the sliding window itself, a length-managed FIFO written in place. The Vector's own length is the position, so there is no ring index to write back and the struct is never rebuilt — which is what lets per-signal state live in an immutable TrackState.\ncode_replica — scratch for the software despread, grown once and reused. Held here rather than borrowed from the backend's ScratchBuffers so the reference can never alias the per-signal replica path.\n\nThe sub-integration length is deliberately not a field: it is the primary code period of the signal it measures, derived rather than configured. Coherent integration buys nothing for noise-power estimation — one dump carries 100 % relative error however long it is, because Var(|B|²) = (E|B|²)² — so all the information lives in the number of looks, and shortening the dump is the only way to buy more of them. Three things say not to: SIMD (64 kernel calls per 1 ms chunk instead of one, and a shorter dump would need a new kernel variant, forfeiting the bit-identical-arithmetic property the whole approach rests on), DC balance (a full-period Gold code is balanced, E[(Σc)²] ≈ 1; any sub-period window behaves like iid signs, ≈16 at 16 chips, so an ADC offset biases a short despread and not a full-period one), and cadence parity with a hardware correlator, which naturally dumps on the code epoch. The variance is bought back with window_duration instead, which is nearly free — a 1 s window is ~1000 Float64 densities per signal.\n\n\n\n\n\n","title":"Tracking.CorrelatorNoiseEstimator"},{"category":"method","location":"noise_estimator.html#Tracking.CorrelatorNoiseEstimator-Tuple{}","page":"Noise Estimator","text":"CorrelatorNoiseEstimator(\n;\n    window_duration,\n    tap_code_shift,\n    num_ants\n)\n\n\nConstruct a CorrelatorNoiseEstimator averaging over the last window_duration of observations, with the reference correlator's taps spaced tap_code_shift chips apart. See the type's docstring for what the two parameters buy and why nothing else is configurable.\n\nThe window is sizehint!-ed to four times the number of code-period observations it expects to hold. The headroom is what makes the FIFO's push!/popfirst! pair measure exactly zero bytes: at 1× or 2× Julia periodically shifts the front offset back and reallocates.\n\n\n\n\n\n","title":"Tracking.CorrelatorNoiseEstimator"},{"category":"method","location":"noise_estimator.html#Tracking.update_noise!-Tuple{CorrelatorNoiseEstimator, BandMeasurement, Integer, Integer, Tracking.NoiseUpdateContext}","page":"Noise Estimator","text":"update_noise!(\n    estimator,\n    measurement,\n    first_sample,\n    last_sample,\n    context\n)\n\n\nMeasure this signal's noise over samples first_sample:last_sample of measurement and append the resulting observations, returning estimator.\n\nThe slice is split into num_sub equal sub-integrations,\n\nnum_sub = max(1, round(Int, slice_duration / code_period))\n\nwith code_period the primary code period of the signal being measured — see CorrelatorNoiseEstimator for why the sub-integration length is derived rather than configured. Equal slices rather than fixed-length ones so no remainder is wasted and every window entry is statistically identical; the max(1, …) matters, because a signal whose code period exceeds the chunk (a 10 ms code with 1 ms chunks) would otherwise yield no observations at all, forever. Each observation is pushed individually: pre-averaging would put one entry per chunk in the window, re-welding its time span to the Doppler update rate.\n\nEach sub-integration starts its replica at code phase 0 and runs for its slice. Nothing here follows a code-period boundary, and that is deliberate: the reference despreads with a wrong PRN at a wrong phase, so there is no signal to accumulate coherently and N̂₀ = |B|²/(N·A_c²·f_s) is unbiased for any N and any starting phase. Following boundaries would be actively harmful — a chunk rarely holds a whole number of code periods, so a boundary-aligned reference would have to carry a partial accumulator across calls, which is exactly the scalar state that has no per-signal anchor. Successive observations are independent because their sample ranges are disjoint; the replica repeating is irrelevant.\n\nAll of the correlator's taps are used and pooled into one power sum, at tap_code_shift chips apart so they are genuinely independent looks (see the type's docstring). The despread runs on the caller's backend kernel — the same one the prompt goes through — which is what makes the measurement model-free.\n\nThe reference is open-loop: the band's nominal IF as the carrier frequency, zero Doppler and zero carrier phase. ±5 kHz of Doppler is 0.5 % of a 1 MHz code band, so no satellite's state is needed and the reference is correct with nothing tracked at all.\n\n\n\n\n\n","title":"Tracking.update_noise!"},{"category":"type","location":"noise_estimator.html#Tracking.NoiseObservation","page":"Noise Estimator","text":"One noise measurement, the producer-side value parallel to CorrelatorOutput. It carries the already-normalised density plus the two pieces of bookkeeping a sliding window needs, so the internal contract is one scalar and every unit conversion happens at the boundary where the caller knows their hardware.\n\nBuild one with noise_observation, noise_observation_from_correlator or noise_observation_from_samples rather than by hand — they are the three shapes a producer actually has, and all three reduce to the same N₀ on the same white input.\n\nFields:\n\nnoise_density — N₀, of dimension 1/Hz.\nnum_sub_integrations — M, the number of independent looks that went into it, and therefore its statistical weight. Not the sample count: for Σ_m |B_m|² over M sub-integrations of N samples each, the density needs only M·N, but its relative variance is 1/M. A producer handing back one 1 ms coherent despread and one handing back 64 16-chip despreads report the same total samples and wildly different precision, so the window is averaged weighted by M.\nduration — the wall-clock span of samples the observation covers, which is what bounds the sliding window. It is not M · N / f_s in general: the software source's M looks are the correlator's taps, which are simultaneous rather than consecutive.\nprn — the PRN whose code was used to measure it. Legitimate metadata (the observation really was measured with that code), and it is what carries the software source's PRN-rotation position without any scalar state.\n\n\n\n\n\n","title":"Tracking.NoiseObservation"},{"category":"function","location":"noise_estimator.html#Tracking.noise_observation","page":"Noise Estimator","text":"noise_observation(\n    accumulation,\n    integrated_samples,\n    sampling_frequency;\n    code_amplitude,\n    prn\n)\n\n\nBuild a NoiseObservation from one dump given as a raw complex accumulation Σ x·c over integrated_samples samples — the most faithful form a producer can report, since Tracking does the squaring, the scaling and all the averaging itself.\n\ncode_amplitude is the RMS amplitude of the sampled replica (see Tracking.normalize); leave it at 1 for a ±1 code, pass the code's RMS for a multi-level one (CBOC). prn records which code measured it.\n\n\n\n\n\n","title":"Tracking.noise_observation"},{"category":"function","location":"noise_estimator.html#Tracking.noise_observation_from_correlator","page":"Noise Estimator","text":"noise_observation_from_correlator(\n    accumulated_power,\n    num_sub_integrations,\n    total_samples,\n    sampling_frequency;\n    code_amplitude,\n    prn,\n    duration\n)\n\n\nBuild a NoiseObservation from M = num_sub_integrations dumps pre-summed on chip as Σ_m |B_m|², covering total_samples = M · N samples in all.\n\nPre-summing must be incoherent (Σ|B_m|², never |Σ B_m|²), which is why this builder takes a power rather than a complex value. M > 1 exists only so a high-rate producer can cut host traffic; the natural granularity is one observation per dump with M = 1, which noise_observation covers.\n\nduration defaults to total_samples / sampling_frequency, which is right when the M sub-integrations are consecutive in time. Pass it explicitly when they are simultaneous — the software source's M looks are the reference correlator's taps, which all integrate the same N samples, so their span is N / sampling_frequency and not M times that.\n\n\n\n\n\n","title":"Tracking.noise_observation_from_correlator"},{"category":"function","location":"noise_estimator.html#Tracking.noise_observation_from_samples","page":"Noise Estimator","text":"noise_observation_from_samples(\n    accumulated_power,\n    num_samples,\n    sampling_frequency;\n    prn\n)\n\n\nBuild a NoiseObservation from a front-end / AGC power monitor: Σ|x|² over num_samples raw samples.\n\nThis is noise_observation_from_correlator at a sub-integration of one sample — the replica degenerates to a single ±1 value, |x·c|² = |x|², and the window mean is exactly σ̂² — so M == num_samples and no code_amplitude applies. It is the minimum-variance, flat-weighting end of the same continuum the despread source sits at the spectral-fidelity end of; a tilted front end is the one thing that tells them apart.\n\n\n\n\n\n","title":"Tracking.noise_observation_from_samples"},{"category":"function","location":"noise_estimator.html#Tracking.noise_density_type","page":"Noise Estimator","text":"noise_density_type(_)\n\n\nThe concrete type get_noise_density returns for estimator once its window holds anything. Used to keep the fold's density argument a plain scalar — never a Union{Nothing,…} — so update stays devirtualised and allocation-free even while the window is still empty.\n\nDefaults to typeof(1.0/1.0Hz), which every shipped builder produces.\n\n\n\n\n\n","title":"Tracking.noise_density_type"},{"category":"type","location":"noise_estimator.html#Tracking.NoiseUpdateContext","page":"Noise Estimator","text":"Per-call side information handed to update_noise!: what a software noise source may need and a BandMeasurement does not carry.\n\nFields:\n\nsignal — the signal being measured, i.e. the one this estimator's key names. Its primary code period sets the sub-integration length and its code family is the one the reference despreads with, so the measurement carries that signal's own spectral weighting — which is the point of keying the estimator by signal (see AbstractNoiseEstimator). Nothing here is chosen: it is the consumer's signal, not a per-band stand-in for it.\ntracked_prn_sets — a tuple of the key sets of the satellite dictionaries of the groups tracking this signal, so a source can pick a PRN nobody is tracking without allocating a merged set. Scoped to this signal because \"in use\" is a question about this code family: PRN 5 tracked on Galileo E1B does not make GPS L1 C/A's PRN 5 code unavailable to the reference.\nchunk_index — the index of the chunk being measured, on the same grid downconvert_and_correlate! uses.\ndownconvert_and_correlator — the backend running this call. A software source must despread on it rather than on a kernel of its own choosing: the one- and two-bit accumulators are popcount counts rather than sample sums, so a float-kernel reference would compare |P|² and N̂₀ on incompatible scales, and the quantisation loss would drop out of the measurement instead of being carried by it.\n\nKept as one struct so that adding a field later is not a signature change.\n\n\n\n\n\n","title":"Tracking.NoiseUpdateContext"},{"category":"function","location":"noise_estimator.html#Tracking.requires_noise_density","page":"Noise Estimator","text":"requires_noise_density(_)\n\n\nDoes estimator read its signal's noise density out of its CN0UpdateContext? false for every estimator that infers its noise floor from the prompt stream (MomentsCN0Estimator, NWPRCN0Estimator, NoCN0Estimator); true only for NoiseRefCN0Estimator.\n\nIt is a trait rather than a hard-coded type check because update(::AbstractCN0Estimator, ::Any, ::CN0UpdateContext) is a documented extension point: a third-party estimator that wants a density must be able to say so, and one that does not must not pay for it.\n\nTwo things key off it, and both are compile-time constants on the estimator's type:\n\nProvisioning. TrackState gives a signal a CorrelatorNoiseEstimator only where that signal's estimator returns true. A signal that does not ask gets no entry at all, so its despread never runs and costs exactly zero — which is the answer for anyone who deliberately stays on NWPR.\nThe warm-up skip. While a configured source's window is still empty, the fold skips the C/N₀ update for the requiring signal only — its co-residents on the same band and the same satellite are untouched, because each has its own source. An NWPRCN0Estimator beside it would otherwise be corrupted: a record missing from the bit grid makes _update_nwpr drop its open narrowband window, so NWPR would silently degrade to its fallback.\n\nThe trait's real home is the type, and the instance method forwards to it. That is what lets provisioning be decided from a group's already-fixed slot type rather than from a satellite value: the whole noise_estimators NamedTuple — its keys included — then folds out of TrackState's type parameters instead of inferring as a union of \"provisioned\" and \"not\". A custom estimator may define either form; defining the type form is the one that keeps that folding.\n\n\n\n\n\nrequires_noise_density(_)\n\n\nThis estimator reads its signal's noise density, so a signal carrying it is provisioned with a CorrelatorNoiseEstimator. Declared on the type, so the provisioning decision folds out of a group's slot type — see requires_noise_density.\n\n\n\n\n\n","title":"Tracking.requires_noise_density"},{"category":"section","location":"tracking_state.html#Tracking-State","page":"Tracking State","text":"Tracking.jl uses a small hierarchy of state types to manage tracking across multiple satellites, multiple signals per satellite, multiple signal groups, and multiple RF bands.\n\nThe tracking state nests as TrackState → SignalGroup → TrackedSat → TrackedSignal:\n\nTrackState — top-level container; holds a NamedTuple of SignalGroups plus the Doppler-estimator configuration.\nSignalGroup — named group of sats that share the same signal-tuple shape (and therefore the same concrete TrackedSat value type, which is what gives the hot loop type stability). Each group also carries its RF band and antenna count.\nTrackedSat — per-satellite state: shared carrier/code Doppler and phase (one set of values per satellite, since all signals on a satellite share the same carrier), a tuple of TrackedSignals, and the per-satellite Doppler-estimator state.\nTrackedSignal — per-signal state: correlator, post-correlation filter, CN0 estimator, bit buffer, and integration-progress flags.","title":"Tracking State"},{"category":"section","location":"tracking_state.html#Estimator-driver-signal","page":"Tracking State","text":"The first signal in each group's tuple is the estimator-driver signal — the one the Doppler estimator uses to update the satellite-shared carrier and code Doppler. With the default ConventionalPLLAndDLL / ConventionalAssistedPLLAndDLL, signals[1]'s correlator is the input to the PLL/DLL discriminator, and the per-signal default loop bandwidths are sized off this signal's primary-code period. A user-supplied AbstractDopplerEstimator is free to use the other signals' state too — signals[1]'s privileged role is a convention of the conventional estimators, not a structural constraint of TrackedSat.\n\nThe driver signal is privileged for the Doppler estimator only. Bit synchronisation, the post-correlation filter and the CN0 estimator all run per signal, so a multi-signal satellite produces one C/N₀ per signal rather than one for the driver — see CN0 Estimator for what that costs and for NoCN0Estimator, the per-signal opt-out.","title":"Estimator-driver signal"},{"category":"section","location":"tracking_state.html#Choosing-a-TrackState-constructor","page":"Tracking State","text":"TrackState has several constructors. The right choice depends on when you know which satellites you'll track.","title":"Choosing a TrackState constructor"},{"category":"section","location":"tracking_state.html#One-shot-scripts:-acquire-once,-then-track","page":"Tracking State","text":"If you acquire once at the start of a script and hand the results straight to tracking, use the AcquisitionResults-aware constructors from the Acquisition extension. They derive everything (signal, default loop bandwidths, satellite parameters) from the acquisition results, so the whole acquire→track handoff is one line.\n\nusing Acquisition  # loads the extension; required for TrackState(acq...)\n\n# Single satellite\nacq = acquire(GPSL1CA(), data, sampling_frequency, 7)\ntrack_state = TrackState(acq)\n\n# Many satellites, one signal\nacqs = acquire(GPSL1CA(), data, sampling_frequency, 1:32)\ntrack_state = TrackState(filter(is_detected, acqs))\n\n# Many satellites, multiple signals\nacqs = vcat(\n    acquire(GPSL1CA(),    data, sampling_frequency, 1:32),\n    acquire(GalileoE1B(), data, sampling_frequency, 1:36),\n)\ntrack_state = TrackState(filter(is_detected, acqs);\n    signals = (gps = (GPSL1CA(),), gal = (GalileoE1B(),)),\n)\n\nYou can still call add_satellite! on a TrackState built this way later — but only for signals already declared by the acqs you handed to the constructor (the groups, and therefore the slot types, are frozen at construction). If you anticipate tracking a wider set of signals than the initial acquisition produced, use the empty-construct-then-populate pattern below instead.","title":"One-shot scripts: acquire once, then track"},{"category":"section","location":"tracking_state.html#Real-time-/-repeating-loops:-build-empty,-then-populate","page":"Tracking State","text":"If you re-acquire periodically (a typical receiver: re-search PRNs every few seconds, hand new detections off to tracking without rebuilding the whole TrackState), build an empty TrackState once with TrackState(; signal = ...) or TrackState(; signals = (...)), then add satellites later with add_satellite! (or remove them with remove_satellite!):\n\n# Build once\ntrack_state = TrackState(;\n    signals = (gps = (GPSL1CA(),), gal = (GalileoE1B(),)),\n)\n\n# In your acquisition loop\nwhile running\n    acqs = acquire(GPSL1CA(), latest_chunk, sampling_frequency, candidate_prns)\n    track_state = add_satellite!(track_state, filter(is_detected, acqs))   # routes each acq to the matching group\n    track_state = track(latest_chunk, track_state, sampling_frequency)\nend\n\nThis pattern keeps the TrackState's concrete type fixed across the loop — the satellite-dict's slot type is frozen at construction, so the tracking hot path stays type-stable as sats come and go.\n\nThe singular signal = GPSL1CA() keyword is the shortcut for the common one-group, one-signal case. It desugars internally to signals = (default = (GPSL1CA(),),), so the rest of the API can stay uniform. With one group, add_satellite! may omit the group = keyword.","title":"Real-time / repeating loops: build empty, then populate"},{"category":"section","location":"tracking_state.html#Power-user:-pre-built-TrackedSats","page":"Tracking State","text":"If you need to customize the correlator or post-correlation filter type (the slot type itself), build the TrackedSats yourself and hand them to the positional constructor TrackState(signal, sats) or the add_satellite!(track_state, group, sat) escape hatch. The kwarg-based constructors only let you customize the satellite's values, not its concrete type.\n\nThe single-signal TrackedSat constructor surface:\n\nTrackedSat(\n    signal,                # e.g. GPSL1CA()\n    prn::Int,\n    code_phase,\n    carrier_doppler;\n    # all kwargs below are optional and have signal-derived defaults\n    doppler_estimator     = ConventionalAssistedPLLAndDLL(...),\n    num_ants              = NumAnts(1),\n    correlator            = get_default_correlator(signal, num_ants),\n    carrier_phase         = 0.0,\n    code_doppler          = carrier_doppler * get_code_center_frequency_ratio(signal),\n    num_prompts_for_cn0_estimation = 100,\n    cn0_estimator         = Tracking.default_cn0_estimator(signal, num_prompts_for_cn0_estimation),\n    post_corr_filter      = DefaultPostCorrFilter(),\n)\n\ncn0_estimator takes any AbstractCN0Estimator — unlike the correlator and the post-corr filter it is free to change the sat's concrete type, since it is a type parameter of TrackedSignal. See CN0 Estimator for the estimators that ship with Tracking and for writing your own.\n\nA worked example combining a narrower-than-default correlator, a custom post-correlation filter (beamformer), and a larger CN0 buffer. The beamformer here is a trivial mean-of-antennas — a real receiver would plug in an actual beamforming algorithm:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz, NumAnts, AbstractPostCorrFilter\n\njulia> # Trivial beamformer — averages across antenna elements\n       struct MyBeamformer <: AbstractPostCorrFilter end\n\njulia> Tracking.update(f::MyBeamformer, prompt) = f;\n\njulia> (::MyBeamformer)(x::AbstractVector) = sum(x) / length(x);\n\njulia> (::MyBeamformer)(x) = x;\n\njulia> sat = TrackedSat(GPSL1CA(), 1, 50.0, 1000.0Hz;\n           num_ants                       = NumAnts(4),\n           correlator                     = EarlyPromptLateCorrelator(\n               num_ants = NumAnts(4),\n               preferred_early_late_to_prompt_code_shift = 0.1,\n           ),\n           post_corr_filter               = MyBeamformer(),\n           num_prompts_for_cn0_estimation = 200,\n       );\n\njulia> track_state = TrackState(GPSL1CA(), sat);\n\njulia> get_num_ants(track_state, 1)\n4\n\njulia> get_correlator(track_state, 1).preferred_early_late_to_prompt_code_shift\n0.1\n\nFor a multi-signal satellite, the empty TrackState(; signals = (group = (sig1, sig2, …),)) path will build a default template sat the first time you add_satellite! to that group; customize individual TrackedSignals afterwards via the TrackedSat kwarg-update constructor (TrackedSat(sat; signals = (...))).","title":"Power-user: pre-built TrackedSats"},{"category":"section","location":"tracking_state.html#Adding-satellites","page":"Tracking State","text":"Satellites are added to a TrackState via add_satellite!. The acquisition handoff values (prn, code_phase, carrier_doppler, optionally code_doppler and carrier_phase) get wired into a fresh TrackedSat with the library's default correlator and post-correlation filter. Adding a satellite with the same PRN again overwrites the existing entry (matching merge_sats semantics — no error).","title":"Adding satellites"},{"category":"section","location":"tracking_state.html#Multi-satellite-tracking","page":"Tracking State","text":"To track several satellites on the same signal, simply call add_satellite! repeatedly:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(; signal = GPSL1CA());\n\njulia> track_state = add_satellite!(track_state; prn = 1,  code_phase = 50.0,  carrier_doppler = 1000.0Hz);\n\njulia> track_state = add_satellite!(track_state; prn = 5,  code_phase = 120.0, carrier_doppler = -500.0Hz);\n\njulia> track_state = add_satellite!(track_state; prn = 17, code_phase = 890.0, carrier_doppler = 2000.0Hz);\n\njulia> get_carrier_doppler(track_state, 5)\n-500.0 Hz\n\njulia> get_code_phase(track_state, 17)\n890.0","title":"Multi-satellite tracking"},{"category":"section","location":"tracking_state.html#Multi-system-tracking-(different-signals-on-different-sats)","page":"Tracking State","text":"When different satellites carry different signal types, use multiple named groups. Each group has its own concrete TrackedSat value type, so type inference stays sharp across the heterogeneous mix.\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(;\n           signals = (\n               gps     = (GPSL1CA(),),\n               galileo = (GalileoE1B(),),\n           ),\n       );\n\njulia> track_state = add_satellite!(track_state; prn = 1,  group = :gps,     code_phase = 50.0,  carrier_doppler = 1000.0Hz);\n\njulia> track_state = add_satellite!(track_state; prn = 11, group = :galileo, code_phase = 200.0, carrier_doppler = -300.0Hz);\n\njulia> get_carrier_doppler(track_state, :gps, 1)\n1000.0 Hz\n\njulia> get_carrier_doppler(track_state, :galileo, 11)\n-300.0 Hz","title":"Multi-system tracking (different signals on different sats)"},{"category":"section","location":"tracking_state.html#Multi-signal-tracking-(one-satellite,-several-signals)","page":"Tracking State","text":"A modern GPS satellite transmits L1 C/A, L1C-D, and L1C-P simultaneously on the same carrier. Tracking.jl can track all three together on one satellite, sharing a single carrier downconvert per outer iteration:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(;\n           signals = (\n               modern_gps = (GPSL1C_P(), GPSL1C_D(), GPSL1CA()),\n           ),\n       );\n\njulia> track_state = add_satellite!(track_state;\n           prn = 11, group = :modern_gps,\n           code_phase = 0.0, carrier_doppler = 1234.0Hz,\n       );\n\njulia> get_carrier_doppler(track_state, :modern_gps, 11)\n1234.0 Hz\n\nPutting a pilot signal first (e.g. GPSL1C_P()) is encouraged with the conventional estimators when one is available: pilot signals carry no data-bit modulation, which lets the PLL run longer coherent integrations and reach lower phase-noise floors. The data-bearing signals (L1C-D, L1 C/A) still recover their navigation bits independently — each TrackedSignal carries its own bit_buffer regardless of which signal drives the estimator.\n\nWhen a satellite tracks signals with different primary-code lengths (e.g. L1 C/A at 1 ms vs L1C-P at 10 ms), each outer iteration integrates to the shortest signal's next primary-code boundary. The shorter signal's correlator completes every iteration; the longer signal's correlator accumulates across multiple iterations and only marks is_integration_completed = true on its own boundary. Doppler updates therefore happen at the shortest signal's cadence (1 ms in this example), and longer signals see their integration windows spanned by piecewise Doppler updates — the natural per-iteration-Doppler-correction behaviour of a real receiver.","title":"Multi-signal tracking (one satellite, several signals)"},{"category":"section","location":"tracking_state.html#Phased-array-tracking","page":"Tracking State","text":"To track signals coherently across an antenna array, pass a Matrix measurement (rows = samples, columns = antenna elements) and declare the number of antennas at TrackState construction:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(;\n           signal = GPSL1CA(),\n           num_ants = NumAnts(4),\n       );\n\njulia> track_state = add_satellite!(track_state; prn = 1, code_phase = 50.0, carrier_doppler = 1000.0Hz);\n\njulia> get_num_ants(track_state, 1)\n4\n\nBy default the track function uses the first antenna channel as the reference signal to drive the discriminators. An appropriate beamforming algorithm will probably suit better — construct a TrackedSat with a custom post_corr_filter and build the TrackState from it (so the slot type takes the custom filter type rather than the default):\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz, NumAnts, AbstractPostCorrFilter\n\njulia> # Same trivial mean-of-antennas filter as the power-user example above\n       struct MyBeamformer <: AbstractPostCorrFilter end\n\njulia> Tracking.update(f::MyBeamformer, prompt) = f;\n\njulia> (::MyBeamformer)(x::AbstractVector) = sum(x) / length(x);\n\njulia> (::MyBeamformer)(x) = x;\n\njulia> sat = TrackedSat(GPSL1CA(), 1, 50.0, 1000.0Hz;\n                        num_ants = NumAnts(4),\n                        post_corr_filter = MyBeamformer());\n\njulia> track_state = TrackState(GPSL1CA(), sat);\n\njulia> get_num_ants(track_state, 1)\n4","title":"Phased-array tracking"},{"category":"section","location":"tracking_state.html#Acquisition-handoff","page":"Tracking State","text":"When the Acquisition.jl extension is loaded (via using Acquisition), add_satellite! / add_satellite gain AcquisitionResults overloads that read prn / code_phase / carrier_doppler straight off the acq result. With group = nothing (the default) the routing is inferred by matching acq.system against each group's longest-primary-code signal; pass an explicit group = to bypass the inference. The batch form takes an AbstractVector{<:AcquisitionResults} and routes each entry independently — convenient for the filter(is_detected, acquire(...)) pipeline.\n\nusing Acquisition  # loads the extension\n\n# Single acq\nts = add_satellite!(ts, acq)                       # auto-route\nts = add_satellite!(ts, acq; group = :legacy_gps)  # explicit group, asserts match\n\n# Vector of acqs (mixed constellations OK)\nts = add_satellite!(ts, filter(is_detected, acqs))\n\nacq.system must match the longest-primary-code signal in the target group's tuple — its code phase is the only one that's unambiguous when the group tracks multiple signals on shared chips. Hand over an L1C-P acq (not L1 C/A) for a group tracking (GPSL1C_P(), GPSL1C_D(), GPSL1CA()).","title":"Acquisition handoff"},{"category":"section","location":"tracking_state.html#Removing-satellites","page":"Tracking State","text":"julia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(; signal = GPSL1CA());\n\njulia> track_state = add_satellite!(track_state; prn = 1,  code_phase = 50.0,  carrier_doppler = 1000.0Hz);\n\njulia> track_state = add_satellite!(track_state; prn = 23, code_phase = 500.0, carrier_doppler = 1500.0Hz);\n\njulia> track_state = remove_satellite!(track_state; prn = 1);\n\njulia> haskey(get_sat_states(track_state, :default), 23)\ntrue\n\njulia> haskey(get_sat_states(track_state, :default), 1)\nfalse","title":"Removing satellites"},{"category":"section","location":"tracking_state.html#Multi-band-tracking","page":"Tracking State","text":"A satellite often broadcasts on more than one RF band — GPS broadcasts on L1 (1575.42 MHz) and L5 (1176.45 MHz); Galileo broadcasts on E1 (L1) and E5a (L5). In a multi-band receiver these arrive from separate front-ends, generally at different sample rates, and need to be downconverted and correlated against their own carrier replicas. Tracking.jl exposes this as a multi-band TrackState where each group declares which RF band it sits on.\n\nWhy this matters. A single physical satellite tracked on two bands gives the receiver two near-independent observations of the same path. The classic uses:\n\nIonospheric correction via dual-frequency (iono-free) pseudorange combinations.\nWider effective bandwidth for code-phase observations (L5 carries far more chip-rate bandwidth than L1 C/A).\nCross-band-aided tracking: the carrier Doppler ratio between L1 and L5 is exactly the ratio of their RF carrier frequencies. A joint estimator can fuse the two bands' discriminators and produce a more accurate Doppler estimate than either band alone — particularly valuable at low CN0 where the wider data-aided integration on L5 helps the noisier L1 C/A.\n\nThis release ships the structural enablers for multi-band: per-band groups, per-band measurement routing, an estimation barrier that sees every band's correlator outputs at once. The cross-band joint-tracking algorithm (e.g. linking PRN-X-on-L1 with PRN-X-on-L5 in one estimator step) is a follow-up — see docs/plans/2026-05-15-multi-band-tracking-design.md for the design and the open mechanism question.","title":"Multi-band tracking"},{"category":"section","location":"tracking_state.html#Declaring-bands","page":"Tracking State","text":"The band field of each SignalGroup is inferred from get_band(signals[1]), so you don't normally type it. Mix signals from different bands in the same signals = (...) keyword and the bands fall out:\n\njulia> using Tracking, GNSSSignals\n\njulia> track_state = TrackState(;\n           signals = (\n               legacy_gps_l1 = (GPSL1CA(),),\n               modern_gps_l1 = (GPSL1C_P(), GPSL1C_D(), GPSL1CA()),\n               galileo       = (GalileoE1B(),),\n               gps_l5        = (GPSL5I(),),\n           ),\n       );\n\njulia> keys(track_state.groups)\n(:legacy_gps_l1, :modern_gps_l1, :galileo, :gps_l5)\n\nFour groups, two distinct bands — the first three groups all sit on L1 (GPS L1 and Galileo E1 share the 1575.42 MHz carrier), the fourth sits on L5. Two groups sharing a band is fine; the grouping partitions satellites by signal-tuple shape (the type-stability axis), not by band.","title":"Declaring bands"},{"category":"section","location":"tracking_state.html#Tracking-against-multiple-measurements","page":"Tracking State","text":"For multi-band tracking, build one BandMeasurement per band — bundling sample buffer and front-end metadata — and pass them as a NamedTuple keyed by the band's GNSSSignals.get_band_id (e.g. :L1, :L5):\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> using GNSSSignals: gen_code, get_code_frequency, get_code_center_frequency_ratio\n\njulia> function make_signal(sys, prn, carrier_doppler, num_samples, fs)\n           code_freq = carrier_doppler * get_code_center_frequency_ratio(sys) + get_code_frequency(sys)\n           range = 0:num_samples-1\n           cis.(2π .* carrier_doppler .* range ./ fs) .*\n               gen_code(num_samples, sys, prn, fs, code_freq, 0.0)\n       end;\n\njulia> track_state = TrackState(;\n           signals = (legacy_gps_l1 = (GPSL1CA(),), gps_l5 = (GPSL5I(),)),\n       );\n\njulia> track_state = add_satellite!(track_state; prn = 1, group = :legacy_gps_l1, code_phase = 0.0, carrier_doppler = 200Hz);\n\njulia> track_state = add_satellite!(track_state; prn = 1, group = :gps_l5, code_phase = 0.0, carrier_doppler = -150Hz);\n\njulia> buf_l1 = make_signal(GPSL1CA(),  1, 200Hz,  4000,  4e6Hz);  # 1 ms at 4 MHz\n\njulia> buf_l5 = make_signal(GPSL5I(),   1, -150Hz, 25000, 25e6Hz); # 1 ms at 25 MHz\n\njulia> track!((L1 = BandMeasurement(buf_l1, 4e6Hz),\n               L5 = BandMeasurement(buf_l5, 25e6Hz)), track_state);\n\njulia> get_carrier_doppler(track_state, :legacy_gps_l1, 1)\n200.00000359633913 Hz\n\nThe keys (:L1, :L5) come from GNSSSignals.get_band_id(L1()) and GNSSSignals.get_band_id(L5()) — nameof of the band type, so any band (including user-defined ones) has a key without Tracking-side registration. All measurements must cover the exact same observation duration — num_samples / sampling_frequency must compare equal across bands. An L1 chunk of 4000 samples at 4 MHz and an L5 chunk of 25000 samples at 25 MHz both cover 1 ms, so they're compatible; an L5 chunk of 25001 samples is rejected.","title":"Tracking against multiple measurements"},{"category":"section","location":"tracking_state.html#Per-band-antenna-counts","page":"Tracking State","text":"Different bands often come from different front-ends with different antenna arrangements. To declare per-band antenna counts, pass SignalGroup instances directly as the entries — the bare-tuple shortcut uses the constructor's single num_ants kwarg for all groups, but the SignalGroup form lets each group set its own:\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: NumAnts\n\njulia> track_state = TrackState(;\n           signals = (\n               legacy_gps_l1 = SignalGroup((GPSL1CA(),); num_ants = NumAnts(2)),\n               gps_l5        = SignalGroup((GPSL5I(),);  num_ants = NumAnts(1)),\n           ),\n       );\n\njulia> track_state.groups[:legacy_gps_l1].num_ants\nNumAnts{2}()\n\njulia> track_state.groups[:gps_l5].num_ants\nNumAnts{1}()\n\nTwo groups on the same band must declare the same num_ants — they share a physical front-end. The constructor errors at TrackState construction if they disagree.","title":"Per-band antenna counts"},{"category":"section","location":"tracking_state.html#Bare-buffer-compatibility","page":"Tracking State","text":"A single-band receiver doesn't need to type any of this. The bare-buffer call track!(buf, state, fs) keeps working for any TrackState that spans exactly one band — internally it wraps the buffer into a one-entry NamedTuple keyed by the lone band. Pass intermediate_frequency via the same kwarg as before, or move it onto a BandMeasurement when you migrate to multi-band.","title":"Bare-buffer compatibility"},{"category":"section","location":"tracking_state.html#SignalGroup","page":"Tracking State","text":"A group of satellites that all track the same tuple of GNSS signal types, on the same RF band, observed by the same antenna array. Groups are the unit of type stability — every TrackedSat inside a SignalGroup shares the same concrete signal-tuple shape, so the satellites dictionary has a concrete value type and the hot loop sees no dynamic dispatch.\n\nTwo groups may share a band: e.g. a :legacy_gps group tracking (GPSL1CA(),) and a :galileo group tracking (GalileoE1B(),) both report band = L1(). The grouping is by signal-tuple shape, not by band — band is metadata each group carries so track can route the right measurement to it.","title":"SignalGroup"},{"category":"section","location":"tracking_state.html#Band-routing","page":"Tracking State","text":"The Symbol keys used in multi-band measurement collections (see BandMeasurement) are the bands' ids as reported by GNSSSignals.get_band_id; band_keys lists the ids a given TrackState expects.","title":"Band routing"},{"category":"section","location":"tracking_state.html#Addressing-satellites-and-signals","page":"Tracking State","text":"To reach per-group state, index track_state.groups by the group's key (e.g. track_state.groups[:legacy_gps].satellites). The high-level accessors below (get_sat_states, get_sat_state, …) take the group key as an argument and fold to compile-time constants when the groups type is known.\n\nThe accessor's argument count tells the lookup where to stop:\n\nForm Meaning\nf(track_state) Single-group, single-sat — folds via only(...) at each level.\nf(track_state, prn) Single-group, multi-sat — picks the sat by PRN.\nf(track_state, group, prn) Multi-group — picks the sat in the named group.\nf(track_state, group, prn, sig) Per-signal — picks one TrackedSignal within a multi-signal sat.\n\nThe trailing sig selector is either:\n\nan Integer index into the sat's signals tuple (1 = first signal = estimator-driver signal) — the canonical form, unambiguous even when the same signal type appears twice in the tuple, or\na signal type like GPSL1CA (the bare type, not GPSL1CA()) — readable sugar that errors if the type appears zero or more than once in the tuple.","title":"Addressing satellites and signals"},{"category":"section","location":"tracking_state.html#What-you-can-read","page":"Tracking State","text":"Sat-level — shared across all signals on the sat (no sig selector):\n\nAccessor Returns\nget_prn PRN number.\nget_num_ants Number of antenna elements.\nget_code_phase Shared code phase (wraps at max_code_length).\nget_code_doppler Shared code Doppler.\nget_carrier_phase Shared carrier phase in radians.\nget_carrier_doppler Shared carrier Doppler.\nget_signal_start_sample Index of the next sample to integrate.\n\nPer-signal — pass sig (index or type) on multi-signal sats:\n\nAccessor Returns\nget_correlator The working correlator (in-flight accumulator).\nget_last_fully_integrated_correlator Correlator value from the last completed integration.\nget_last_fully_integrated_filtered_prompt Filtered prompt value from the last completed integration.\nget_last_fully_integrated_num_code_blocks Primary-code block count of the last completed integration.\nget_last_fully_integrated_integration_time Integration time T of the last completed integration. Post-integration SNR is estimate_cn0 × this; C/N₀ alone is processing-independent.\nget_post_corr_filter The post-correlation filter.\nget_cn0_estimator The CN0 estimator.\nestimate_cn0 CN0 estimate in dB-Hz.\nget_bit_buffer / get_soft_bits / get_num_bits Bit buffer, decoded soft bits (sign = the hard bit), bit count.\nget_integrated_samples Number of samples accumulated into the current integration so far.\nhas_bit_or_secondary_code_been_found true once bit/secondary-code synchronization has been achieved.\n\nThe per-signal form always names the group explicitly, even on a single-group TrackState. Use :default as the group key in that case — estimate_cn0(track_state, :default, 11, GPSL1C_P).\n\njulia> using Tracking, GNSSSignals\n\njulia> using Tracking: Hz\n\njulia> track_state = TrackState(;\n           signals = (modern_gps = (GPSL1C_P(), GPSL1C_D(), GPSL1CA()),),\n       );\n\njulia> track_state = add_satellite!(track_state; prn = 11, group = :modern_gps,\n                                   code_phase = 0.0, carrier_doppler = 1234.0Hz);\n\njulia> get_carrier_doppler(track_state, :modern_gps, 11)  # sat-level: same for all signals\n1234.0 Hz\n\njulia> get_num_bits(track_state, :modern_gps, 11, 1)  # per-signal by index (estimator-driver signal)\n0\n\njulia> get_num_bits(track_state, :modern_gps, 11, GPSL1CA)  # per-signal by type\n0","title":"What you can read"},{"category":"section","location":"tracking_state.html#Group-accessors","page":"Tracking State","text":"","title":"Group accessors"},{"category":"section","location":"tracking_state.html#TrackedSat","page":"Tracking State","text":"The sat-level accessors listed under Addressing satellites and signals all have a single-argument form that dispatches on a TrackedSat directly:","title":"TrackedSat"},{"category":"section","location":"tracking_state.html#TrackedSignal","page":"Tracking State","text":"The per-signal accessors in the table under Addressing satellites and signals all dispatch directly on a TrackedSignal too. Additionally:\n\nget_signal(tsig) — the GNSS signal instance (e.g. GPSL1CA()).\nget_filtered_prompts(tsig) — every filtered prompt produced during the most recent track call. The vector is reset at the start of each call and appended for every completed integration.","title":"TrackedSignal"},{"category":"section","location":"tracking_state.html#Bit-sync,-secondary-code-sync,-and-the-code-phase-wrap","page":"Tracking State","text":"The mechanics of bit / secondary-code synchronization — including the runtime widening of TrackedSat.code_phase from one primary code period to one full symbol period, the per-signal BitBuffer lifecycle, and the code-phase seeding from a recovered secondary-code phase — have a dedicated page: Bit and Secondary-Code Sync.\n\nFor day-to-day use, the only thing most users need is has_bit_or_secondary_code_been_found (per signal) to gate calls to get_soft_bits — both are listed in the per-signal accessor table.","title":"Bit sync, secondary-code sync, and the code-phase wrap"},{"category":"type","location":"tracking_state.html#Tracking.TrackState","page":"Tracking State","text":"Main tracking state container holding satellite states for multiple GNSS systems and the Doppler estimator (e.g., PLL/DLL). This is the primary struct used for tracking operations.\n\ngroups is a NamedTuple of SignalGroups. Each group bundles its per-group satellites dictionary, signal-instance tuple, band, and antenna count.\n\nnoise_estimators is a NamedTuple of AbstractNoiseEstimators keyed by signal id (GNSSSignals.get_signal_id — :GPSL1CA, :GalileoE1B, …), the same NamedTuple idiom BandMeasurements uses for bands, so a lookup folds to a compile-time constant. Keyed by signal and not by band because the floor a record divides by is the post-correlation one, which depends on the despreading modulation (see AbstractNoiseEstimator). A signal gets an entry only where its C/N₀ estimator reads a noise density (see requires_noise_density); signals with no such estimator get none, and then the noise measurement costs exactly nothing. Each estimator averages in place, so TrackState itself is never rebuilt for a noise update.\n\n\n\n\n\n","title":"Tracking.TrackState"},{"category":"function","location":"tracking_state.html#Tracking.add_satellite!","page":"Tracking State","text":"add_satellite!(track_state; prn, group, kwargs...)\n\n\nAdd (or replace) a satellite in track_state in place. Builds a multi-signal TrackedSat for the requested group using the library default correlator and post-corr filter, with the supplied acquisition-handoff values (prn, code_phase, code_doppler, carrier_phase, carrier_doppler) wired into each TrackedSignal. The per-satellite doppler-estimator state is initialized via init_estimator_state against the TrackState's configured estimator.\n\nWhen group is omitted, a single-group TrackState uses its only group (whatever it is named); a multi-group TrackState requires the key and otherwise throws an ArgumentError naming the available groups. If a satellite with the same prn already exists in that group's dictionary, it is overwritten.\n\nThe satellite dictionary is mutated in place, but callers should keep using the returned TrackState: when the configured estimator's update_estimator_on_handoff returns a rebuilt estimator (rather than mutating in place), the rebuilt estimator is carried by the returned TrackState — track_state.doppler_estimator cannot be replaced in place because TrackState is immutable. For estimators that update in place (including the default conventional ones), the very same track_state comes back.\n\ntrack_state = TrackState(; signals = (modern_gps = (GPSL1C_P(), GPSL1C_D(), GPSL1CA()),))\nadd_satellite!(\n    track_state;\n    prn = 11,\n    group = :modern_gps,\n    code_phase = 0.0,\n    carrier_doppler = 1234.0Hz,\n)\n\nTo use a non-default correlator or post-corr-filter type, construct the TrackedSat yourself and call the add_satellite!(track_state, group, sat) overload — see below.\n\n\n\n\n\nadd_satellite!(track_state, group, sat)\n\n\nIn-place add (or replace) with a pre-built TrackedSat — the escape hatch for power users who need non-default correlator or post-corr-filter types. The sat's type must match the group's slot type already fixed at TrackState construction; passing a sat of the wrong type errors at dispatch time.\n\nLike the keyword form, the returned TrackState carries the estimator returned by update_estimator_on_handoff — keep using the return value.\n\n\n\n\n\n","title":"Tracking.add_satellite!"},{"category":"function","location":"tracking_state.html#Tracking.add_satellite","page":"Tracking State","text":"add_satellite(track_state; prn, group, kwargs...)\n\n\nImmutable variant of add_satellite!. Returns a new TrackState with the satellite added; the input is left unchanged.\n\n\n\n\n\n","title":"Tracking.add_satellite"},{"category":"function","location":"tracking_state.html#Tracking.remove_satellite!","page":"Tracking State","text":"remove_satellite!(track_state; prn, group)\n\n\nRemove a satellite from track_state in place. Throws a KeyError if no satellite with the given prn exists in the named group (same contract as the immutable remove_satellite).\n\nremove_satellite!(track_state; prn = 11, group = :modern_gps)\n\nWhen group is omitted it is inferred the same way as add_satellite!: a single-group TrackState uses its only group; a multi-group TrackState requires the key. Returns track_state unchanged (the dictionary is mutated in place).\n\n\n\n\n\n","title":"Tracking.remove_satellite!"},{"category":"function","location":"tracking_state.html#Tracking.remove_satellite","page":"Tracking State","text":"remove_satellite(track_state; prn, group)\n\n\nImmutable variant of remove_satellite!. Returns a new TrackState with the satellite removed; the input is left unchanged. Errors if no satellite with the given prn exists.\n\n\n\n\n\n","title":"Tracking.remove_satellite"},{"category":"function","location":"tracking_state.html#Tracking.merge_sats","page":"Tracking State","text":"merge_sats(track_state, group_idx, tracked_sats)\n\n\nMerge already-built TrackedSats into the group_idx group of track_state, returning a new TrackState (the input is left unchanged). tracked_sats may be a single TrackedSat, a Vector, or a Dictionary keyed by PRN; existing PRNs in the group are overwritten.\n\nEach sat's doppler_estimator_state must match the type the track_state's configured estimator produces (checked up front), and the sat's signal-tuple shape must match the group's slot type. The estimator's update_estimator_on_handoff hook is invoked once with the incoming sats so estimators with cross-satellite shared state can grow it.\n\nFor a single-group TrackState the group_idx may be omitted.\n\n\n\n\n\n","title":"Tracking.merge_sats"},{"category":"type","location":"tracking_state.html#Tracking.SignalGroup","page":"Tracking State","text":"A group of satellites that all track the same tuple of GNSS signal types, on the same RF band, observed by the same antenna array.\n\nGroups are the unit of type stability: every TrackedSat inside a SignalGroup shares the same concrete Tuple{Vararg{TrackedSignal}} shape, so the dictionary's value type is concrete and the hot loop sees no dynamic dispatch.\n\nTwo groups may share a band (e.g. :legacy_gps tracking (GPSL1CA(),) and :galileo tracking (GalileoE1B(),) both on L1()). The grouping is by signal-tuple shape, not by band — band is metadata each group carries so the right measurement is routed to it during track.\n\nFields:\n\nband: an AbstractGNSSSignal Band instance (L1(), L5(), …)\nsatellites: Dictionary{Int, <:TrackedSat} keyed by PRN\nsignals: the signal-instance tuple (e.g. (GPSL1C_P(), GPSL1C_D(), GPSL1CA()))\nnum_ants: the antenna count for this group's band\n\n\n\n\n\n","title":"Tracking.SignalGroup"},{"category":"type","location":"tracking_state.html#Tracking.SignalGroups","page":"Tracking State","text":"Type alias: NamedTuple of SignalGroups — the storage shape inside TrackState. The N parameter is the number of groups.\n\n\n\n\n\n","title":"Tracking.SignalGroups"},{"category":"function","location":"tracking_state.html#Tracking.band_keys","page":"Tracking State","text":"band_keys(track_state)\n\n\nThe set of distinct band ids (GNSSSignals.get_band_id) used across all groups in track_state, returned as a tuple of Symbols in first-encounter order. These are the keys a multi-band measurements NamedTuple must use. Resolves at compile time when the groups type is known.\n\nts = TrackState(; signals = (legacy_gps = (GPSL1CA(),), gps_l5 = (GPSL5I(),)))\nband_keys(ts) == (:L1, :L5)\n\n\n\n\n\n","title":"Tracking.band_keys"},{"category":"type","location":"tracking_state.html#Tracking.SatelliteDicts","page":"Tracking State","text":"Type alias for a tuple or named tuple of per-group satellite dictionaries. Each entry is Dictionary{I, <:TrackedSat} — the keys are satellite identifiers (PRNs) and the values are the per-sat tracking state. The signal type for each group lives in the dictionary value type, accessed via only(sat.signals).signal at use sites.\n\n\n\n\n\n","title":"Tracking.SatelliteDicts"},{"category":"function","location":"tracking_state.html#Tracking.get_sat_states","page":"Tracking State","text":"get_sat_states(satellites, group_idx)\n\n\nGet the per-group satellite dictionary for a specific signal-group index.\n\n\n\n\n\n","title":"Tracking.get_sat_states"},{"category":"function","location":"tracking_state.html#Tracking.get_sat_state","page":"Tracking State","text":"get_sat_state(sats, identifier)\n\n\nGet the satellite state for a specific satellite identifier.\n\n\n\n\n\n","title":"Tracking.get_sat_state"},{"category":"function","location":"tracking_state.html#Tracking.get_signal","page":"Tracking State","text":"get_signal(track_state, group_idx)\n\n\nReturn the first signal of the given group — useful when the caller needs the signal instance (for gen_code, frequency lookups, …) but doesn't already have a sat in hand. The dictionary's value type carries the signal type, so this resolves at compile time when group_idx is a literal Symbol / Integer.\n\nFor a single-group TrackState the index can be omitted.\n\n\n\n\n\n","title":"Tracking.get_signal"},{"category":"type","location":"tracking_state.html#Tracking.TrackedSat","page":"Tracking State","text":"Holds the state of a single satellite being tracked. Carries the satellite- level carrier/code Doppler and phase (shared across all signals on this satellite), the per-signal correlator state in signals::Tuple{Vararg{TrackedSignal}}, and the per-satellite Doppler-estimator state in doppler_estimator_state.\n\nThe first signal in signals is the estimator-driver signal — the one the Doppler estimator uses to update the satellite-shared carrier and code Doppler. With the default ConventionalPLLAndDLL / ConventionalAssistedPLLAndDLL, that means signals[1]'s correlator is what the PLL/DLL discriminator runs on, and per-satellite Doppler updates happen at the rate of the first signal's integration boundary; other signals filter their own prompts and update their own CN0 estimates and bit buffers on their own boundaries. A user-supplied AbstractDopplerEstimator is free to use the other signals' state too — signals[1]'s privileged role is a convention of the conventional estimators, not a structural constraint of the type.\n\nThe shared code_phase wraps at the least common multiple of the signals' code periods including secondary code (see max_code_length).\n\n\n\n\n\n","title":"Tracking.TrackedSat"},{"category":"method","location":"tracking_state.html#Tracking.get_prn-Tuple{TrackedSat}","page":"Tracking State","text":"get_prn(s)\n\n\nGet the PRN (Pseudo-Random Noise) number of the satellite.\n\n\n\n\n\n","title":"Tracking.get_prn"},{"category":"method","location":"tracking_state.html#Tracking.get_code_phase-Tuple{TrackedSat}","page":"Tracking State","text":"get_code_phase(s)\n\n\nGet the satellite's shared code phase. Wraps at max_code_length of the signals tuple (in chips, including secondary code). To get the replica-relative phase for a specific signal, mod by that signal's primary code length.\n\n\n\n\n\n","title":"Tracking.get_code_phase"},{"category":"method","location":"tracking_state.html#Tracking.get_code_doppler-Tuple{TrackedSat}","page":"Tracking State","text":"get_code_doppler(s)\n\n\nGet the current code Doppler frequency.\n\n\n\n\n\n","title":"Tracking.get_code_doppler"},{"category":"method","location":"tracking_state.html#Tracking.get_carrier_phase-Tuple{TrackedSat}","page":"Tracking State","text":"get_carrier_phase(s)\n\n\nGet the current carrier phase in radians.\n\n\n\n\n\n","title":"Tracking.get_carrier_phase"},{"category":"method","location":"tracking_state.html#Tracking.get_carrier_doppler-Tuple{TrackedSat}","page":"Tracking State","text":"get_carrier_doppler(s)\n\n\nGet the current carrier Doppler frequency.\n\n\n\n\n\n","title":"Tracking.get_carrier_doppler"},{"category":"method","location":"tracking_state.html#Tracking.get_signal_start_sample-Tuple{TrackedSat}","page":"Tracking State","text":"get_signal_start_sample(s)\n\n\nGet the starting sample index in the signal for the next integration.\n\n\n\n\n\n","title":"Tracking.get_signal_start_sample"},{"category":"method","location":"tracking_state.html#Tracking.get_signals-Tuple{TrackedSat}","page":"Tracking State","text":"get_signals(s)\n\n\nGet the satellite's tuple of TrackedSignals.\n\n\n\n\n\n","title":"Tracking.get_signals"},{"category":"method","location":"tracking_state.html#Tracking.get_doppler_estimator_state-Tuple{TrackedSat}","page":"Tracking State","text":"get_doppler_estimator_state(s)\n\n\nGet the per-satellite Doppler estimator state (e.g. the loop-filter state for the conventional PLL/DLL).\n\n\n\n\n\n","title":"Tracking.get_doppler_estimator_state"},{"category":"type","location":"tracking_state.html#Tracking.TrackedSignal","page":"Tracking State","text":"Per-signal tracking state. One TrackedSignal exists for each signal being tracked on a satellite — for a satellite tracked on GPS L1 C/A only there is one; for a satellite tracked on GPS L1 C/A + L1C-D + L1C-P there are three.\n\nHolds the signal-specific state: the correlator, post-correlation filter, CN0 estimator, bit buffer, and the integration-progress flags. The per-satellite carrier/code Doppler and phase are shared across signals and live on the enclosing TrackedSat.\n\n\n\n\n\n","title":"Tracking.TrackedSignal"},{"category":"method","location":"tracking_state.html#Tracking.get_last_fully_integrated_integration_time-Tuple{TrackedSignal}","page":"Tracking State","text":"get_last_fully_integrated_integration_time(t)\n\n\nThe integration time of the most recently completed record: its primary-code block count times one code period.\n\nThis is the T the last correlator output was accumulated over, and the quantity estimate_cn0 divides its sample-normalized prompts by. C/N₀ itself is processing-independent, so anything asking a detectability question of that record — is the peak still above the noise, is the bit decision trustworthy — needs this T too: the post-integration SNR is C/N₀ · T, not C/N₀ alone.\n\nDo not confuse it with get_integrated_samples, which counts the record currently being accumulated and resets to zero each time one completes.\n\n\n\n\n\n","title":"Tracking.get_last_fully_integrated_integration_time"}]
}
